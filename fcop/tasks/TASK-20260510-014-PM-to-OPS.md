---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260510-014
sender: PM
recipient: OPS
priority: P0
thread_key: codeflow-v0.2.0-beta-2-merged-commit-mt2-mt3-mt4-and-tag
references:
  - REPORT-20260510-013-DEV-to-PM
  - REPORT-20260510-012-DEV-to-PM
  - TASK-20260510-013-PM-to-DEV
  - TASK-20260510-012-PM-to-DEV
layer: worker
---

# TASK-20260510-014：v0.2.0-beta.2 三 fix 合并 commit + 本地 tag

## §一 一句话指令

按 [REPORT-20260510-013-DEV-to-PM §七](REPORT-20260510-013-DEV-to-PM.md) 的 commit 范围与 message 草稿，**一次性双 commit**（fix + docs）出 `v0.2.0-beta.2` 本地 annotated tag。**不推 origin/backup tag**，main 分支推 origin + backup 同步，gitee 不动。

## §二 Commit A — fix(v0.2.0-beta.2，三 fix 合并)

### 范围（DEV §七 给的 10 项 modified）

```text
M  codeflow-shell/.env.example                                        # MT-3
M  codeflow-shell/.gitignore                                          # MT-2
M  codeflow-shell/README.md                                           # MT-2/3/4 What's new
M  codeflow-shell/package.json                                        # MT-2/3/4 description
M  codeflow-shell/src/main.ts                                         # MT-2 VERSION + MT-2/3/4 ref
M  packages/codeflow-runtime/package.json                             # MT-2/4 description
M  packages/codeflow-runtime/src/registry/AgentSdkAdapter.ts          # MT-2
M  packages/codeflow-runtime/src/registry/__tests__/AgentSdkAdapter.test.ts  # MT-2
M  packages/codeflow-runtime/src/review/ReviewEngine.ts               # MT-4
M  packages/codeflow-runtime/src/review/__tests__/ReviewEngine.test.ts # MT-4
```

### Commit message（HEREDOC，DEV §七 给的稍精简）

```text
fix(s6-v0.2-sprint0-mt2-mt3-mt4): three back-to-back hotfixes for v0.2.0-beta.2

MT-2 (BUG-SDK-002) — TASK-012 / REPORT-012:
  Pass `local: { force: true }` to agent.send() for local-mode sends so
  the persisted active-run record from Agent.create() is expired before
  the next send tries to start a new run. Closes the v0.2.0-beta.1
  regression where every real-SDK task drop failed with
  "Agent <uuid> already has active run" (QA-011 §六, 100% reproducible).
  Cloud-mode sends omit the local field (SDK type system rejects `local`
  on cloud agents).
  + 2 tests: TS-RUN-1 (local opts shape), TS-RUN-2 (cloud opts empty).

MT-3 (BUG-SDK-003) — TASK-013 / REPORT-013:
  Default .env.example CURSOR_DEFAULT_MODEL from `auto` to `default`.
  Cursor SDK rejects `auto` with allowlist error; `default` is the SDK
  sentinel that lets Cursor pick a recommended model for the account.
  Comments updated with common ids list + how to discover live allowlist.

MT-4 (BUG-SDK-004) — TASK-013 / REPORT-013:
  Extend ReviewEngine.extractText() with probe 4 to walk
  SDKAssistantMessage.message.content[] array (TextBlock concat,
  ToolUseBlock skip). Without this, every real-SDK reviewer session
  produced a 0-character buffer regardless of how chatty the LLM was —
  parseVerdict() then threw VerdictParseError → NeedsHumanGate fallback
  → decision=needs_human + trigger_reason=verdict_parse_failed.
  See node_modules/@cursor/sdk/dist/cjs/messages.d.ts:23-31 for the real
  SDKAssistantMessage shape.
  + 3 tests: TS-6.12 (real shape + markdown bold), TS-6.13 (tool_use
  unpolluting), TS-6.14 (multi-chunk streaming concat).

Versions: codeflow-shell + @codeflow/runtime → 0.2.0-beta.2 (single tag).
Tests: 99 → 104 (MT-1) → 106 (MT-2) → 109 (MT-3+4) all green.

Closes BUG-SDK-001 (MT-1, retroactive), BUG-SDK-002 (MT-2),
BUG-SDK-003 (MT-3), BUG-SDK-004 (MT-4). Surfaces BUG-SDK-005 (SDK
ripgrep stderr noise; non-blocking) and BUG-SDK-006 (redux smoke
exposed reviewer-not-dispatching + double-dispatch race; needs
follow-up micro-task).

Refs: TASK-012, REPORT-012, TASK-013, REPORT-013.
```

### 本地 annotated tag（**不推 origin/backup**）

```bash
git tag -a v0.2.0-beta.2 -m "CodeFlow v0.2.0-beta.2 - MT-2/3/4 hotfix bundle: 4 BUG closed (SDK-001/002/003/004); 2 informational surfaced (SDK-005/006). Real SDK + real verdict end-to-end works (unit-tested 109/109; smoke partial due to BUG-SDK-006 race, follow-up planned)."
```

## §三 Commit B — docs(v0.2.0-beta.2-archive)

### 范围（8 项 untracked docs）

```text
?? docs/agents/tasks/REPORT-20260510-006-PM-to-ADMIN.md     # PM v0.2.0-beta.1 落 + BUG-002 hotfix 通报
?? docs/agents/tasks/REPORT-20260510-011-OPS-to-PM.md       # OPS v0.2.0-beta.1 commit 回执
?? docs/agents/tasks/REPORT-20260510-011-QA-to-PM.md        # QA v0.2.0-beta.1 验收（揭示 BUG-002）
?? docs/agents/tasks/REPORT-20260510-012-DEV-to-PM.md       # DEV MT-2 完工（揭示 BUG-003+004）
?? docs/agents/tasks/REPORT-20260510-013-DEV-to-PM.md       # DEV MT-3+4 完工（揭示 BUG-005+006）
?? docs/agents/tasks/TASK-20260510-011-PM-to-QA.md          # PM QA 派单
?? docs/agents/tasks/TASK-20260510-012-PM-to-DEV.md         # PM MT-2 派单
?? docs/agents/tasks/TASK-20260510-013-PM-to-DEV.md         # PM MT-3+4 派单
```

注：本 TASK-014 自身（`TASK-20260510-014-PM-to-OPS.md`）由 OPS commit B 之后下一轮 archive 处理（不入本次 commit B）。

### Commit message

```text
docs(s6-v0.2-sprint0-mt2-mt3-mt4-archive): hotfix dispatch and reports

- REPORT-006-PM-to-ADMIN: v0.2.0-beta.1 ship + BUG-SDK-002 + MT-2 dispatch
- REPORT-011-OPS-to-PM: v0.2.0-beta.1 commit + tag (cd6fb28 / ee3207e)
- REPORT-011-QA-to-PM: v0.2.0-beta.1 acceptance + BUG-SDK-002 surfacing
- REPORT-012-DEV-to-PM: MT-2 hotfix done (75 min) + BUG-SDK-003/004 surfacing
- REPORT-013-DEV-to-PM: MT-3+4 hotfix done (80 min) + 4 BUG closed +
  BUG-SDK-005/006 surfacing
- TASK-011-PM-to-QA / TASK-012-PM-to-DEV / TASK-013-PM-to-DEV: dispatch records
```

## §四 安全 HARD GATE（stage 前 + stage 后双跑）

### 4.1 stage 前

```powershell
git diff | Select-String -Pattern 'crsr_[0-9a-f]{8,}|ck_[0-9a-f]{8,}|sk-[A-Za-z0-9]{20,}'
```

期望：**0 匹配**。注：DEV-013 §四 #4 已自跑 = 0 匹配。OPS 复跑兜底。

### 4.2 stage 后

```powershell
git diff --cached | Select-String -Pattern 'crsr_[0-9a-f]{8,}|ck_[0-9a-f]{8,}|sk-[A-Za-z0-9]{20,}'
```

期望：**0 匹配**。

### 4.3 `.env*` 不在 stage

```powershell
git diff --cached --name-only | Select-String -Pattern '^codeflow-shell/\.env(\.tmp_smoke_t12)?$'
```

期望：**0 匹配**。

### 4.4 `.smoke-*` 不在 stage（验证 MT-2 gitignore 防御生效）

```powershell
git diff --cached --name-only | Select-String -Pattern '\.smoke-[a-zA-Z0-9_-]+'
```

期望：**0 匹配**。

## §五 推送策略（同前）

| 仓库 | main 推送 | tag 推送 |
|---|---|---|
| origin (codeflow-pwa)  | ✅ 推 Commit A + B | ❌ **不推** v0.2.0-beta.2 tag |
| backup (codehouse)     | ✅ 推 Commit A + B | ❌ 不推 |
| gitee                  | ❌ 不动（保持 G3 = `62532a7`）| ❌ 不推 |

## §六 10 项验收输出（本 REPORT 必填）

```powershell
# 1
git log --oneline -5

# 2
git tag --list "v0.2.*"

# 3
git show v0.2.0-beta.2 --stat | Select-Object -First 30

# 4
git ls-remote --tags origin | Select-String "v0.2"

# 5
git ls-remote --tags backup | Select-String "v0.2"

# 6 - origin/main = local
$local = (git rev-parse HEAD).Trim()
$origin = (git ls-remote origin main | ForEach-Object { $_.Split()[0] }).Trim()
"local : $local"; "origin: $origin"; if ($local -eq $origin) { "MATCH" } else { "MISMATCH" }

# 7 - backup/main = local
$backup = (git ls-remote backup main | ForEach-Object { $_.Split()[0] }).Trim()
"backup: $backup"; if ($local -eq $backup) { "MATCH" } else { "MISMATCH" }

# 8 - gitee 仍 G3
git ls-remote gitee main 2>&1 | Select-Object -First 1

# 9 - runtime 109/109
cd packages/codeflow-runtime; npm test 2>&1 | Select-String "tests|pass|fail" | Select-Object -First 4; cd ../..

# 10 - post-commit git status
git status --short
```

## §七 不动手清单

- ❌ 不推 v0.2.0-beta.2 tag 到 origin/backup
- ❌ 不动 gitee
- ❌ 不动 server/relay、Nginx、防火墙
- ❌ 不 npm publish
- ❌ 不读 `codeflow-shell/.env`（同 SAFETY）
- ❌ 不动 working tree 中 OPS 范围外的文件（含本 TASK 文件 / DEV-014 后续若有）

## §八 SLA + 回执

| 项 | 值 |
|---|---|
| SLA | ≤ 30 min |
| 回执 | `docs/agents/tasks/REPORT-20260510-014-OPS-to-PM.md` 含上述 10 项验收 + 安全 HARD GATE 四段输出 |

## §九 完工后链式

OPS-014 commit + tag 落地 → PM 立即派 [TASK-20260510-014-PM-to-QA](TASK-20260510-014-PM-to-QA.md) 跑 v0.2.0-beta.2 完整验收 + 4 BUG closed 判定 + BUG-SDK-006 race 试探。

PM-01
2026-05-10 23:36 (UTC+8)
