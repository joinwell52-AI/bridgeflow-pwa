---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-012
sender: PM
recipient: OPS
priority: P0
thread_key: s3-phase-a-done-checkpoint-commit
references:
  - REPORT-20260509-015-PM-to-ADMIN
  - REPORT-20260509-009-DEV-to-PM
  - REPORT-20260509-010-QA-to-PM
  - REPORT-20260509-011-OPS-to-PM
  - REPORT-20260509-014-PM-to-ADMIN
  - TASK-20260509-013-PM-to-DEV
  - TASK-20260509-014-PM-to-QA
layer: worker
---

# Sprint S3 Phase A done checkpoint commit + push origin/backup（gitee 按 G3 暂不动）

## ADMIN 5/9 14:00 拍板

= **「按推荐」** = REPORT-015 §四 的 4.1 (Phase A 通过) + 4.2 = B-2（先 commit Phase A，再启 Phase B）。

详见 [`REPORT-20260509-015-PM-to-ADMIN.md` §四](./REPORT-20260509-015-PM-to-ADMIN.md)。

## 待 commit 文件清单（21 项）

按 `git status --short` 当前可见 + PM 即将落盘的 3 份 Phase B 派单文件。**OPS 无需 hardcode 文件名**——只需保证 `git add` 时 scope 落在 `packages/codeflow-runtime/` 和 `docs/agents/tasks/` 两个目录内（参见 §"加文件原则"）。

### A. `packages/codeflow-runtime/` 改动（DEV Phase A 产出 + QA 测策） — 13 项

```
M  packages/codeflow-runtime/README.md
M  packages/codeflow-runtime/package-lock.json
M  packages/codeflow-runtime/package.json
M  packages/codeflow-runtime/src/index.ts
M  packages/codeflow-runtime/src/registry/AgentRegistry.ts
M  packages/codeflow-runtime/src/registry/PersistentStore.ts
M  packages/codeflow-runtime/src/registry/index.ts
M  packages/codeflow-runtime/src/types/state.ts
?? packages/codeflow-runtime/docs/test-strategy-s3.md
?? packages/codeflow-runtime/src/registry/AgentSdkAdapter.ts
?? packages/codeflow-runtime/src/registry/RuntimeBootstrap.ts
?? packages/codeflow-runtime/src/registry/errors.ts
?? packages/codeflow-runtime/src/registry/__tests__/   (目录含 4 个 .test.ts + helpers.ts)
```

### B. `docs/agents/tasks/` 流程文档 — 8 项

```
?? docs/agents/tasks/REPORT-20260509-009-DEV-to-PM.md
?? docs/agents/tasks/REPORT-20260509-010-QA-to-PM.md
?? docs/agents/tasks/REPORT-20260509-011-OPS-to-PM.md
?? docs/agents/tasks/REPORT-20260509-014-PM-to-ADMIN.md
?? docs/agents/tasks/REPORT-20260509-015-PM-to-ADMIN.md
?? docs/agents/tasks/TASK-20260509-012-PM-to-OPS.md   (本文件)
?? docs/agents/tasks/TASK-20260509-013-PM-to-DEV.md   (即将由 PM 写)
?? docs/agents/tasks/TASK-20260509-014-PM-to-QA.md    (即将由 PM 写)
```

OPS 执行前**必须**用 `git status --short | Measure-Object` 验证总数 = **21**（如果 PM 还没写完 TASK-013/014，这个数字会少；OPS 应在 PM 通知"3 单全部落盘"后再触发）。

## 加文件原则（避免误入 / 误漏）

```powershell
# 只加这两个目录里的所有改动（保险路径）
git add docs/agents/tasks/
git add packages/codeflow-runtime/

# 验证 staged 范围
git diff --cached --name-only
```

期望：staged 文件**全部**位于上述两个目录之下，**不**包含：
- `_ignore/` / `private/`（按 .gitignore 已被排除，但 OPS 应再扫一眼）
- `.codeflow/state/`（如果 DEV 跑过测试遗留临时文件——按 PersistentStore 用 `os.tmpdir()` 应该不产生，但 OPS 应确认）
- `node_modules/`（已被 .gitignore 排除）
- 其他 packages（`packages/codeflow-protocol/` 本批不应有改动）

## 高危操作判定

按 [`ops-bridge.mdc`](../../.cursor/rules/ops-bridge.mdc) "高危操作必须二次确认" 4 类：
- 重启生产服务 ❌ 不涉及
- 修改 Nginx 配置 ❌ 不涉及
- 清理数据库或日志 ❌ 不涉及
- 变更防火墙规则 ❌ 不涉及

→ **本任务不属于高危**。push 是远程仓库写操作，遵守 [HANDOFF §6.3 git safety](./HANDOFF-20260509-001-PM-session-close-to-next-sprint.md)：不擅自 force-push / hard reset / push --all / 动 .git/config / push gitee（G3 保持）。

## 执行命令（按顺序）

### Phase 1：dev env 健康检查（≤ 3 min）

```powershell
# 1.1 验证 staged scope（commit 前）
git status --short
# 期望：~21 行 ?? + M 行（确认无误后再 add）

# 1.2 Node 版本（PM-to-OPS 教训：本仓 OPS 当前用 v24.14.0）
node --version
# 期望：major >= 20（≥ 20 即可）

# 1.3 packages/codeflow-protocol 仍稳
Set-Location packages/codeflow-protocol
npm install
npm test
Set-Location ../..
# 期望：5 valid + 3 invalid 全部如预期，exit 0

# 1.4 packages/codeflow-runtime typecheck + 16 单元测试
Set-Location packages/codeflow-runtime
npm install
npx tsc --noEmit
# 期望：exit 0

npm test
# 期望：tests 16 / pass 16 / fail 0
Set-Location ../..
```

**PM-to-OPS 教训记录**：本批不再使用 Linux `&&` 链；改用 `Set-Location ... ; npm install ; npm test ; Set-Location ...` 或上面的逐步语法。

如果 Phase 1 任一项失败 → **停下不 commit**，立刻在 `REPORT-012-OPS-to-PM` 描述失败现象。

### Phase 2：commit bundle（≤ 1 min）

```powershell
# 2.1 加文件
git add docs/agents/tasks/
git add packages/codeflow-runtime/

# 2.2 验证 staged 范围（保险检查）
git diff --cached --name-only
# 必须看到：所有改动文件都在 docs/agents/tasks/ 或 packages/codeflow-runtime/ 之下

# 2.3 commit
git commit -m "feat(s3-phase-a): AgentRegistry + PersistentStore + RuntimeBootstrap + 16 unit tests + S3 test strategy"

# 2.4 验证 commit 落地
git log -1 --oneline
# 期望：新 hash，message 正确

git show --stat HEAD | Select-Object -First 5
# 期望：~21 files changed，主要 added，少量 modified
```

**若 commit 失败**（pre-commit hook 拦 / lint 错）：不要 amend、不要 force——直接修问题后做*新* commit。按 git safety，amend 仅在 PM 显式授权时才允许。

### Phase 3：push origin + backup（≤ 2 min；gitee 跳过）

```powershell
# 3.1 fetch 检查 ahead
git fetch --all
git status -sb
# 期望：包含「ahead 1」（local 领先 origin 1 commit）

# 3.2 push origin
git push origin main

# 3.3 push backup
git push backup main

# 3.4 gitee 显式跳过（G3）
Write-Host "gitee push skipped per HANDOFF-001 G3 decision (gitee remains diverged at 62532a7)"

# 3.5 三仓 HEAD 对比
$localHead = git rev-parse HEAD
$originHead = (git ls-remote origin main) -split "`t" | Select-Object -First 1
$backupHead = (git ls-remote backup main) -split "`t" | Select-Object -First 1
$giteeHead  = (git ls-remote gitee main)  -split "`t" | Select-Object -First 1
Write-Host "local : $localHead"
Write-Host "origin: $originHead"
Write-Host "backup: $backupHead"
Write-Host "gitee : $giteeHead   (expected to lag at 62532a7…)"
```

期望：
- local / origin / backup 三者 HEAD 一致（新 commit hash）
- gitee 仍是 `62532a7`（前 7 位）— G3 decision

## 失败处理

| 错误 | 处理 |
|---|---|
| Phase 1.4 npm test 16 测试出现 fail | 报告 PM；不 commit；让 PM 协调 DEV 排查 |
| Phase 1.4 typecheck 失败 | 同上（不应该发生 — DEV REPORT-009 已自测过）|
| Phase 1.3 protocol 测试失败 | 同上（不应该发生 — protocol 包未受 Phase A 影响）|
| Phase 2.3 commit 被 pre-commit hook 拦 | 看 hook 输出 → 报告 PM；不 `--no-verify` |
| Phase 3.2 origin `non-fast-forward` | `git fetch origin` → 看 origin 是否有新 commit；如有，**停手**报告 PM（不擅自 rebase）|
| Phase 3.3 backup 鉴权失败 | 重试 1 次；仍失败 → 报告，但不阻塞总任务（origin 成功 = 主备份就绪）|
| 网络超时 | 重试 1 次；仍失败 → 报告 |

## 验收（OPS 必出 9 条验证）

| # | 验收项 | 命令 |
|---|---|---|
| 1 | commit 前文件总数 ≈ 21 | `git status --short` 行数（实际可能 18-22 都接受） |
| 2 | Node 版本 ≥ 20 | `node --version` |
| 3 | `@codeflow/protocol` 测试通过 | `cd packages/codeflow-protocol; npm test` exit 0 + 8/8 PASS |
| 4 | `@codeflow/runtime` typecheck 通过 | `cd packages/codeflow-runtime; npx tsc --noEmit` exit 0 |
| 5 | `@codeflow/runtime` 16 单元测试通过 | `cd packages/codeflow-runtime; npm test` 显示 `tests 16 / pass 16 / fail 0` |
| 6 | commit 全部 staged 文件 | `git show --stat HEAD` 文件数 ≈ 21 |
| 7 | commit message 正确 | `git log -1 --pretty=%s` = `feat(s3-phase-a): AgentRegistry + PersistentStore + RuntimeBootstrap + 16 unit tests + S3 test strategy` |
| 8 | origin / backup HEAD = local | `git ls-remote origin main` + `git ls-remote backup main` 头部 hash = local HEAD |
| 9 | gitee 仍在 `62532a7…` | `git ls-remote gitee main` 头部 hash 前 7 位 = `62532a7` |

## 不动手清单

- ❌ 不创建 tag（v0.1-alpha.1 等不在本任务范围；S3 完成后单独考虑）
- ❌ 不 push gitee（G3）
- ❌ 不创建 GitHub Release / Gitee Release
- ❌ 不打包 EXE
- ❌ 不动 CHANGELOG.md（S3 完成后做）
- ❌ 不动 main.py VERSION（CodeFlow Desktop 与 v2 runtime 解耦，不在本批）
- ❌ 不 push 任何分支以外的 ref（如 `--all` / `--mirror` / `--tags`）
- ❌ 不修改 `.git/config`
- ❌ 不 commit `_ignore/` / `private/` / `.env*` / `node_modules/`（.gitignore 已排除，但 OPS 应在 `git show --stat HEAD` 时再扫一眼确认零误入）
- ❌ 不 commit `.codeflow/state/` 下任何 runtime 实际产物（如果存在）

## 时间预算

≤ 7 分钟（Phase 1: 4min + Phase 2: 1min + Phase 3: 2min）

## 启动条件（OPS 等什么）

OPS 在以下两件都为真后开干：

1. ✅ **PM 已通知"3 个 TASK 文件全部落盘"**——可用 `Test-Path` 4 项验证：
   - `docs/agents/tasks/TASK-20260509-012-PM-to-OPS.md`（本文件）
   - `docs/agents/tasks/TASK-20260509-013-PM-to-DEV.md`
   - `docs/agents/tasks/TASK-20260509-014-PM-to-QA.md`
2. ✅ **DEV / QA 已经从各自接单文件读到内容**——这一步可由 ADMIN 在群里通知，或 PM 在 chat 里说 "OPS 开干"

## 完成后回执要求

按 `ops-bridge` 规则写 `REPORT-20260509-012-OPS-to-PM.md`，**必须**包含：

1. **9 条验收命令的实际输出**（粘贴 stdout）
2. **三仓最终 HEAD hash 对比表**（local / origin / backup / gitee）
3. **本次 commit 的 SHA-1**（前 7 位 + 完整 hash）
4. **任何 push / commit 失败的根因**（如有）
5. **特别确认**：`git show --stat HEAD` 的文件数 + 文件路径列表（防止误入 `_ignore/` 等）
6. **下一步建议**：origin/backup 双备份是否就绪？建议是否 tag 此 checkpoint 为 `v0.1-alpha.1-phase-a-done`（仅建议，不擅自 tag）

不能只写"已完成"——必须附验证命令输出（按 `ops-bridge` "回执必须含验证结果"）。

---

PM-01 派单。OPS-01 等 PM "3 单落盘"信号 + DEV/QA 接单确认信号后立即执行。回执请落 `REPORT-20260509-012-OPS-to-PM.md`。
