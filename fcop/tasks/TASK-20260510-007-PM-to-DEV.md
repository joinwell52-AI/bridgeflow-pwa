---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260510-007
sender: PM
recipient: DEV
priority: P0
thread_key: codeflow-v0.2-sprint-0-p2-exe-packaging-and-mt-2
references:
  - REPORT-20260510-002-DEV-to-PM
  - TASK-20260510-002-PM-to-DEV
  - TASK-20260510-005-PM-to-OPS
layer: worker
---

# TASK-20260510-007：v0.2 sprint 0 P2 启动 + MT-2 atomic-write retry

## §一 PM 接受 P1（含 6 决策点 + 4 surprises）

PM 全部接受 [REPORT-20260510-002-DEV-to-PM](REPORT-20260510-002-DEV-to-PM.md) 的：

- ✅ 5 主交付 + 1 顺手修（sample-task 文件名一致 = surprise 3 修复，PM 接受作为 v0.1 文档错误的合理顺手修）
- ✅ 6 决策点 DEV 选择全部正确
- ✅ 4 surprises 全部认同；处置见 §二 micro-tasks 调度
- ✅ 6 自检条款 全部 ✅

**NeedsHumanGate fake-loop 评审输出 `decision: needs_human` 是 v0.1 fake adapter 预期行为**，与 v0.2 P4 后 deprecated 路径一致；不是 bug，DEV-002 §四 #3.fallback 实证正确。

## §二 3 micro-tasks 调度（PM 自决插队节奏）

| MT | 内容 | DEV 估时 | PM 调度 | 合并到 |
|---|---|---|---|---|
| **MT-1** `CursorSdkAdapterOptions.defaultModel` + register/send wire-through | ~60 行 | 0.5-1 工作日 | **推到 P3 (relay-bridge)** | v0.2.0-rc.1 commit |
| **MT-2** `_internal/atomic-write.ts` retry-on-EPERM patch | ~30 行 | 0.5 工作日 | **本 TASK 期间**（与 P2 EXE 评估并行）| v0.2.0-beta commit |
| **MT-3** task schema 显式约束 `task_id == filename stem` | doc-only + schema | 0.5 工作日 | **推到 P4 (schema 重写)** | v1.0-rc.1 commit |

理由：
- MT-2 是已知 cross-cutting bug（REPORT-028 + REPORT-002 累计 3 次复现），影响每次 governance loop 概率性失败 → 优先级最高
- MT-1 涉及 `AgentRegistry.register` + `SessionManager.startSession` 跨包改动 → 与 P3 relay-bridge 同 commit 合理（都是「runtime 选项流」相关）
- MT-3 与 P4 schema 重写紧耦合 → 一起做，避免 P4 重写时还要再次审视

## §三 P2 启动条件（PM 自决放宽：不必等 OPS-005 commit）

DEV REPORT-002 §七-4 写「P2 启动条件 = OPS commit 落地后 10 min」。**PM 自决放宽**：DEV 立即开始 P2 §一 评估矩阵 spike（5 方案对比文档 `docs/design/spike-exe-packaging.md`），不必等 OPS-005 完工 — 评估是只读分析，不会引入 staged conflict。

## §四 P2 主交付（按 TASK-002 §四 P2）

### P2 §1 评估矩阵 spike

`docs/design/spike-exe-packaging.md` 含 5 方案对比（pkg / nexe / bun build --compile / Tauri sidecar / Node SEA + 不打 @cursor/sdk）：

| 方案 | DEV 实测可行性 | EXE 体积 | 启动时间 | ESM 兼容 | 推荐度 |
|---|---|---|---|---|---|

**PM 推荐先试 bun build --compile**（ESM 友好 + 单文件），失败再退 pkg。

### P2 §2 第一个 EXE

- `codeflow-shell/pack.cmd`（Windows）改写或重写
- 如成功：双击 EXE 启动 + banner ≤ 3s + drop sample-task 闭环 + Ctrl+C 优雅退出
- 如失败：spike-exe-packaging.md 标记「全 5 方案不可行」+ 文档化 `npm start` 作为正式 fallback（与 v0.1 Surprise 1 一致），不阻塞 P3

### P2 §3 + MT-2 atomic-write retry-on-EPERM

`packages/codeflow-runtime/src/_internal/atomic-write.ts` 加 retry-on-EPERM（rename 阶段 EPERM 时 sleep 50ms 重试，max 3 次）。

需新增测试：
- `packages/codeflow-runtime/src/_internal/__tests__/atomic-write.test.ts`（如已存在则扩展）含 1-2 个 EPERM retry 场景
- 期望 runtime 测试 94 → 95+ pass

### P2 §4 完工出 `v0.2.0-beta` tag（本地，不推 origin）

DEV 自测通过后派 OPS commit + 本地 tag `v0.2.0-beta`，不推 tag。

## §五 自测要求

| # | 项 | 期望 |
|---|---|---|
| 1 | spike-exe-packaging.md 5 方案完成 | ✅ |
| 2 | 第一个 EXE 双击启动（如成功路径）| banner ≤ 3s |
| 3 | drop sample-task 闭环 | governance loop 完整 |
| 4 | Ctrl+C 优雅退出 | exit 0 |
| 5 | MT-2 atomic-write 测试 | 94 → 95+ pass |
| 6 | typecheck 0 错 | `npx tsc --noEmit` |
| 7 | runtime 94/95+ 测试 | npm test 全过 |

## §六 不动手清单

- ❌ 不动 `packages/codeflow-protocol/schemas/*` v0.1 schemas（P4 才动）
- ❌ 不动 `_ignore/spike_sdk_doorbell/`
- ❌ 不动 `codeflow-desktop/`
- ❌ 不 npm publish
- ❌ 不推 `v0.2.0-beta` tag 到 origin

## §七 回执

```
docs/agents/tasks/REPORT-20260510-007-DEV-to-PM.md
```

含：spike-exe-packaging.md 链接 + EXE 实测结果 + MT-2 patch 描述 + 7 自测项输出 + 任何新 surprise。

## §八 SLA

5/12 EOD（含 P2 §1 评估 + §2 EXE 尝试 + MT-2 patch）。任何阶段卡 ≥ 4h 必须打开 BLOCKER 回执。

PM-01
2026-05-10 02:00 (UTC+8)
