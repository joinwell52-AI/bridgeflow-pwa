---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260511-017
sender: PM
recipient: DEV
priority: P1
thread_key: codeflow-p4-day-5-schema-alignment
references:
  - TASK-20260511-007-PM-to-DEV
  - TASK-20260511-013-PM-to-DEV
  - REPORT-20260511-007-DEV-to-PM
  - REPORT-20260511-003-PM-to-ADMIN
layer: worker
risk_level: low
status: dispatched
---

# TASK-20260511-017：P4 Day 5（修正版）— Schema 对齐 + Drift 核验 + 注释升级

## §0 一句话

P4 Day 5 修正版 — **不删任何 schema**（PM 第 18 次错误自披露：原 TASK-007 §四 Day 5 三处计划全部过时）；本 sprint 仅做 **drift 核验 + Charter 5.4 注释升级 + CHANGELOG 增量**。DEV 自决路径 / SLA，预期 30-90 min。

## §1 PM 第 18 次错误自披露（path 三件套三连击）

**事实**（path 三件套首次「在派 Day 5 前」拦截 PM TASK-007 §四 三处错误）：

| 错误 | TASK-007 §四 原文 | 真实情况 |
|---|---|---|
| 18-A | 「Day 5.3 删除 `task.schema.ts` / `review.schema.ts`」 | 这些文件**根本不存在**。CodeFlow schemas 位于 `packages/codeflow-protocol/src/types.ts`（329 行单文件 mirror）+ `packages/codeflow-protocol/schemas/*.json`（5 JSON SSOT） |
| 18-B | 「Day 5.4 TS interface 改为 shape inferred from fcop@1.1.0 runtime」 | 不可行 — D7=P pythonia 返回 **JS-side dict proxy**，TS 类型必须由 CodeFlow 一侧维护，无法从 runtime 推断 |
| 18-C | 「Day 5.1 `AgentRegistry` 团队查询走 fcop `get_team_status$()` / `init_project$()`」 | 已被 PM #15 撤回 — `fcop@1.1.0` 无 `list_agents` / `get_team_status`，agent registry 是 Charter 5.4 涌现物 |

**根因**：TASK-007 §四 是 5/11 上午起草的，**在 DEV-005 spike 之前**，PM 凭想象规划 Day 5。spike 之后真实 fcop@1.1.0 schema 矩阵揭示，原计划过时。**这是 PM 第 5 次类似失误**（path 三件套自约束 9 又一次成功事前拦截）。

**沉淀**：emergence-log §3 将新增 #18 条目（PM 入档）；本 TASK 主动公开认错，DEV **不必为 PM 错误买单**。

## §2 真实 schema 矩阵（DEV 读懂这张表，Day 5 范围就清晰了）

### 2.1 fcop@1.1.0 与 CodeFlow schemas 对比

| # | Schema | fcop@1.1.0 | CodeFlow | 关系 | Day 5 处理 |
|---|---|---|---|---|---|
| 1 | agent | ✅ `D:\FCoP\src\fcop\_data\schemas\agent.schema.json` | ✅ `packages/codeflow-protocol/schemas/agent.schema.json` + types.ts §3.2 | **共同 — 需 drift 核验** | 5.1 |
| 2 | review | ✅ 同上 | ✅ 同上 | **共同 — 需 drift 核验** | 5.1 |
| 3 | skill | ✅ 同上 | ✅ 同上 | **共同 — 需 drift 核验** | 5.1 |
| 4 | **task** | ❌ fcop 不管 | ✅ 仅 CodeFlow 有 | **CodeFlow 独有（Charter 5.4 涌现物）** | 5.2 加注释 |
| 5 | **session** | ❌ fcop 不管 | ✅ 仅 CodeFlow 有 | **CodeFlow 独有（Charter 5.4 涌现物）** | 5.2 加注释 |
| 6 | boundary | ✅ 仅 fcop 有 | ❌ | **fcop 独有（治理层）** | 不动 |
| 7 | encoding | ✅ 仅 fcop 有 | ❌ | **fcop 独有（系统层）** | 不动 |
| 8 | event | ✅ 仅 fcop 有 | ❌ | **fcop 独有** | 不动 |
| 9 | failure | ✅ 仅 fcop 有 | ❌ | **fcop 独有** | 不动 |
| 10 | ipc-envelope | ✅ 仅 fcop 有 | ❌ | **fcop 独有** | 不动 |

**结论**：
- CodeFlow 5 schemas = 3 共同 + 2 独有；fcop 8 = 3 共同 + 5 独有
- **不删 task/session**：Charter 5.4「CodeFlow 仍持有应用层涌现物（agent PCB / session 状态等）」
- **共同 3 schemas 是核心 Day 5 工作**

### 2.2 Charter 5.4 哲学

| Charter 5 子条款 | 内容 | Day 5 应用 |
|---|---|---|
| 5.1 | CodeFlow 永不重发明 FCoP 已实现的能力 | → 不抢 fcop 的 boundary/encoding/event/failure/ipc-envelope |
| 5.2 | 涌现需求先内部消化 ≥ 3 次才报 fcop | → task/session 不报，CodeFlow 自留 |
| 5.3 | 涌现的协议补充走"建议"姿态 | → 不主动推 fcop 加 task/session schema |
| **5.4** | **CodeFlow 仍持有应用层涌现物（agent 运行态 PCB / SDK session 状态 / Windows EPERM retry）** | **→ task/session 是这一类，types.ts 应明确标注** |

## §3 Day 5 实际范围（3 子任务，DEV 自决执行顺序）

### 5.1 共同 3 schemas drift 核验（read-only，估 15-30 min）

**目标**：检查 CodeFlow `agent/review/skill` 3 个 JSON schemas 与 fcop@1.1.0 对应 schemas 的字段差异。

**方法（DEV 自决）**：
- 选项 A：用 `pythonia` 实测加载 fcop schemas + JSON diff（参考 spike 的 inspect-fcop-schemas.py 模式）
- 选项 B：手工对照 `packages/codeflow-protocol/schemas/{agent,review,skill}.schema.json` vs `D:\FCoP\src\fcop\_data\schemas\{agent,review,skill}.schema.json`
- 选项 C：写一个一次性脚本 `packages/codeflow-runtime/src/_spike/fcop-schema-diff.ts` 自动 diff，跑完即删（不入主代码）

**产出**：`docs/internal/p4-day5-schema-drift.md`（新建，~80-150 行），表格列出：
- 共同字段 ✅
- CodeFlow 多/少的字段
- 类型 / enum / pattern 差异
- 是否 breaking change（pass / breaking / additive-only）

### 5.2 types.ts 注释升级（Charter 5.4 视角，估 10-20 min）

**目标**：在 `packages/codeflow-protocol/src/types.ts` 顶部 + 每个 schema 节加 Charter 5/6 视角注释。

**示例修改**（DEV 可自行措辞）：

在 types.ts 第 25 行附近（现有 SCOPE & RULES 之后）追加：

```typescript
/**
 * SCHEMA OWNERSHIP MATRIX (Charter 5.4):
 *
 * | Schema  | SSOT                                          | Why                                              |
 * |---------|-----------------------------------------------|--------------------------------------------------|
 * | Agent   | D:\FCoP\src\fcop\_data\schemas\agent...       | FCoP-governed; mirror only, drift = bug          |
 * | Review  | D:\FCoP\src\fcop\_data\schemas\review...      | FCoP-governed; mirror only, drift = bug          |
 * | Skill   | D:\FCoP\src\fcop\_data\schemas\skill...       | FCoP-governed; mirror only, drift = bug          |
 * | Task    | packages/codeflow-protocol/schemas/task...    | CodeFlow-owned (Charter 5.4 emergence)           |
 * | Session | packages/codeflow-protocol/schemas/session... | CodeFlow-owned (Charter 5.4 emergence)           |
 *
 * Task/Session schemas live entirely in CodeFlow because fcop@1.1.0
 * does not model task PCB or agent session runtime state — these are
 * application-layer concerns per Charter 5.4. Any changes to Task/Session
 * stay in this repo and do NOT propagate to D:\FCoP.
 */
```

在 §3.3 Task / §3.5 Session 章节标题旁，加 `// Charter 5.4: CodeFlow-owned emergence schema`。

### 5.3 CHANGELOG 增量（estimated 5-15 min）

在 `docs/releases/v0.3.0-alpha.md`（DEV-013 Day 4 已建 baseline）的 "Schema layer" 段，追加 Charter 5.4 一段：

```markdown
### Schema ownership (Charter 5.4)

CodeFlow v0.3.0-alpha aligns its 5-schema TS mirror with fcop@1.1.0's 8 schemas:

- **Mirrored from fcop SSOT (3)**: Agent / Review / Skill — single source of truth
  lives at `D:\FCoP\src\fcop\_data\schemas\*.json`. Drift = bug (filed upstream).
- **CodeFlow-owned (2)**: Task / Session — Charter 5.4 emergence schemas.
  Not modeled by fcop because they encode application-layer task PCB and SDK
  session runtime state. Changes stay in `packages/codeflow-protocol/`.
- **Not mirrored (5)**: Boundary / Encoding / Event / Failure / IPC-Envelope —
  fcop-internal governance / transport schemas, no CodeFlow-side coupling.

See `docs/internal/p4-day5-schema-drift.md` for the 3-schema drift report.
```

DEV 可自行调整措辞 / 列表顺序。

## §4 不在范围（防 DEV 抢跑）

- ❌ **不删任何 schema**（task/session 是 Charter 5.4 涌现物，agent/review/skill 是 mirror）
- ❌ **不动 `AgentSdkAdapter.ts`**
- ❌ **不动 `SessionManager` / `SessionStore` / `SessionRun`**
- ❌ **不动 fcop-client.ts / TaskParser / ReviewWriter / NeedsHumanGate / InboxWatcher**（Day 1-4 已稳）
- ❌ **不写 fcop@1.1.0 缺失 schemas 的 TS mirror**（boundary/encoding/event/failure/ipc-envelope 与 CodeFlow 无关）
- ❌ **不打 v0.3.0-alpha tag**（OPS 在 Day 6 后另派）
- ❌ **不动版本号**（package.json bump 留给 Day 6）
- ❌ **不动 release notes 的其他段**（仅在 schema 段追加 Charter 5.4 一段）

## §5 path 三件套（DEV 派单前已 PM 核对，DEV 自行复验）

| # | 核对项 | PM 验证结果 |
|---|---|---|
| 1 | `packages/codeflow-protocol/src/types.ts` 存在 | ✅ 329 行 |
| 2 | `packages/codeflow-protocol/schemas/{agent,review,skill,task,session}.schema.json` 存在 | ✅ 5 文件 |
| 3 | `D:\FCoP\src\fcop\_data\schemas\{agent,review,skill}.schema.json` 存在 | ✅ 实测过 |
| 4 | `docs/releases/v0.3.0-alpha.md` 存在（DEV-013 Day 4 baseline）| ✅ commit `9506a91` 已落 |
| 5 | DEV 可写权限 | ✅ |

如 DEV 复验发现任何 path 不存在或权限问题，**立即停止派人，回报 PM**（自约束 9 / 9.1 派单后版需 DEV 协同执行）。

## §6 测试与验收

### 6.1 必跑

- `cd packages/codeflow-runtime && npx tsc --noEmit` → exit 0
- `cd packages/codeflow-protocol && npx tsc --noEmit` → exit 0
- `cd codeflow-shell && npx tsc --noEmit` → exit 0
- `cd packages/codeflow-runtime && npm test` → 141/141 pass（不要求新增 test）
- types.ts 注释 markdown 表格 lint 过（visual check ok 即可）

### 6.2 可选（DEV 自决）

- 如 5.1 选项 A/C 跑 spike 脚本，跑完即删，不入 commit
- 如发现 drift（agent/review/skill），列入 `p4-day5-schema-drift.md` 但**不修**（Day 5 仅做核验，修 drift 是 Day 5.4 → 推迟到 D11/v0.3.0-beta）

### 6.3 不必跑

- ❌ smoke shell（Day 5 没改主代码路径）
- ❌ runtime 任何业务测试

## §7 SLA + 加速预期

| 子任务 | PM SLA | DEV 加速预期（参考 Day 3/4 30x） |
|---|---|---|
| 5.1 Drift 核验 | 1-2h | 15-30 min |
| 5.2 types.ts 注释 | 30-60 min | 10-20 min |
| 5.3 CHANGELOG 增量 | 30-60 min | 5-15 min |
| **总计** | **2-4h** | **30-90 min** |

如 DEV 完工超 3h，告 PM 拦截（可能遇到 PM 未预见的 drift / 注释生成困难）。

## §8 回报要求

写 `fcop/tasks/REPORT-20260511-017-DEV-to-PM.md`，包含：

1. **5.1 drift 核验结果**：3 schemas 是否 drift / drift 字段列表 / `p4-day5-schema-drift.md` 路径
2. **5.2 types.ts 注释 diff**：注释行数 +N，截图或 diff 块
3. **5.3 CHANGELOG 增量**：v0.3.0-alpha.md 行数 +N
4. **测试矩阵**：3 workspace tsc + 141 tests 全过截图
5. **时间**：开始 → 完工 → SLA 实际 vs 估算
6. **surprises**：任何 PM 未提及的发现 — 即便细小（DEV-005/007/009/011/013 已建立优秀的「surprise self-discovery」模式）

写完即 DEV-017 完工 — ADMIN 看到 DEV 回报后会**手动通知 OPS** 启动下一步：
- 选项 P1：OPS-018 commit Day 5 snapshot（5-10 min，类似 OPS-014/012）
- 选项 P2：PM 起草 TASK-018-PM-to-DEV 启动 Day 6（全量回归 + release notes + 版本 bump）

## §9 DEV 高自由度声明

- 5.1 三选项（A pythonia / B 手工 / C spike script）DEV 自决
- 5.2 注释措辞 DEV 自决（PM §3.2 仅是示例）
- 5.3 CHANGELOG 措辞 DEV 自决
- 如发现 PM §2.1 矩阵有错（fcop schemas 列表不全 / drift 判定错误），**立即告 PM**（path 三件套 9.1 派单后版 — DEV 是最后一道防线）
- 如发现 Day 5 真实工作量 ≠ PM §7 SLA（无论快慢），告 PM（DEV-009 D2-S2、DEV-011 D3-S1 等已建立「反 PM 推断」优秀传统）

## §10 给 ADMIN 的备录

本 TASK 不需要 ADMIN 拍板任何 D 决策。完工后 PM 会写 REPORT-20260511-004-PM-to-ADMIN 报告：
- Day 5 完工
- v0.3.0-alpha 出厂条件矩阵 C5/C6 更新
- 推荐下一步（OPS-018 commit + DEV-018 Day 6）

---

— PM-01, 5/11 17:05
