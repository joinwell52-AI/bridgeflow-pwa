---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260511-003
sender: PM
recipient: OPS
priority: P0
thread_key: codeflow-v0.2.0-beta-3-mt5-commit-and-tag-not-pushed
references:
  - REPORT-20260511-001-DEV-to-PM
  - REPORT-20260511-002-OPS-to-PM
  - TASK-20260511-001-PM-to-DEV
  - REPORT-20260510-014-OPS-to-PM
layer: worker
---

# TASK-20260511-003：v0.2.0-beta.3 双 commit + 本地 tag（不推 origin tag）

## 一、TL;DR

DEV-001 MT-5 hotfix 完工（BUG-SDK-007 closed，runtime 109→112 全绿，smoke-2 在 QA-014 三轮 100% crash 精确配置下完美 banner）。

按 DEV-001 REPORT §九 提供的 commit message 草稿（PM 全文采纳，仅微调措辞）执行双 commit + 本地 tag `v0.2.0-beta.3`，**不**推 tag 到 origin / backup，**不**动 gitee。

与 OPS-014（v0.2.0-beta.2）同结构，可直接复用熟练度。

## 二、必交付清单

### 2.1 Commit A — hotfix bundle（7 文件）

stage 范围**严格 7 项**：

```text
codeflow-shell/.env.example
codeflow-shell/README.md
codeflow-shell/package.json
codeflow-shell/src/main.ts
packages/codeflow-runtime/package.json
packages/codeflow-runtime/src/registry/AgentSdkAdapter.ts
packages/codeflow-runtime/src/registry/__tests__/AgentSdkAdapter.test.ts
```

**注意**：与 OPS-014 的 10 文件不同 — 本次 ReviewEngine.ts + ReviewEngine.test.ts **没被改**（BUG-SDK-007 仅动 create-path，不动 review-path），所以 7 项而非 10 项。

commit message 模板（PM 采用 DEV §九，措辞微调）：

```text
fix(s6-v0.2-sprint0-mt5): Agent.create() drops model arg to unblock ADMIN-class API keys (v0.2.0-beta.3)

MT-5 (BUG-SDK-007) — TASK-20260511-001:
  CursorSdkAdapter.create() no longer forwards spec.modelId or
  this._opts.defaultModel to Agent.create({ model }) on ANY API key
  tier. QA-014 ran three real-key smokes on ADMIN's Cursor API key
  (CURSOR_DEFAULT_MODEL = default / claude-sonnet-4 / claude-sonnet-4-5);
  all three crashed identically at registerDefaultAgentKitIfEmpty() with:

    Error: Agent.create failed: Cannot use this model: <name>.
    Available models: ..., <name>, ...

  The rejected name appears in the "Available models" list — that is
  the signature of a per-API-key ACL on programmatic model spec rather
  than a bad-model-name issue. Control evidence: ADMIN key + no-model
  Agent.create() succeeds (QA-011, QA-014); DEV key + with-model
  Agent.create() also succeeds (DEV-013).

  The resume-path model wire-through (Agent.resume({ model })) is
  preserved so BUG-SDK-001 (Local SDK agents require an explicit
  model) stays fixed on the half of the pipeline where it actually
  fires. CURSOR_DEFAULT_MODEL env var becomes send/resume-time only;
  .env.example comments and main.ts banner WARNING example updated.
  TS-MODEL-1/2 flipped (Agent.create receives NO model even when
  defaultModel/spec.modelId set); new TS-MODEL-6 sweeps the 2x2
  (defaultModel x spec.modelId) matrix; new TS-MODEL-7/8 regression-
  guard the resume-path wire-through.

Smoke verification (DEV-001):
  - smoke-1 (no CURSOR_DEFAULT_MODEL): banner OK + WARNING block +
    Status: running + agents.json with 2 real sdk_agent_id UUIDs.
  - smoke-2 (CURSOR_DEFAULT_MODEL=claude-sonnet-4 — QA-014 exact
    100% crash config): banner OK + Status: running + agents.json
    with 2 real sdk_agent_id UUIDs. Pre-MT-5 this path was 0/3.

Versions: codeflow-shell + @codeflow/runtime -> 0.2.0-beta.3 (single tag).
Tests: 109 -> 112 (all green, 0 fail).

Closes BUG-SDK-007. BUG-SDK-001/002/003/004 stay closed (regression-
guarded). BUG-SDK-005 (ripgrep stderr noise) and BUG-SDK-006 (reviewer
dispatch race) are not in TASK-001 scope and need separate v0.2.0-beta.3
e2e smokes by QA to re-observe under the post-hotfix flow.

Refs: TASK-20260511-001, REPORT-20260510-014-QA-to-PM,
      REPORT-20260510-014-OPS-to-PM, REPORT-20260510-013-DEV-to-PM.
```

### 2.2 本地 tag

```text
git tag v0.2.0-beta.3
```

**不**推到 origin / backup（internal preview policy 不变，与 v0.2.0-beta.x 全系列一致）。

annotated 还是 lightweight：用 **lightweight**（与 v0.2.0-beta / -beta.1 / -beta.2 同款，避免与已存 v0.2.0-alpha 的 annotated 风格混）。

### 2.3 Commit B — docs archive（9 文件）

stage 范围**严格 9 项**（不含 TASK-003 / REPORT-003 自身 — 与 OPS-014 同样的「task 自身不入本次 commit」惯例）：

```text
docs/agents/tasks/TASK-20260510-014-PM-to-OPS.md
docs/agents/tasks/TASK-20260510-014-PM-to-QA.md
docs/agents/tasks/REPORT-20260510-014-OPS-to-PM.md
docs/agents/tasks/REPORT-20260510-014-QA-to-PM.md
docs/agents/tasks/REPORT-20260511-001-PM-to-ADMIN.md
docs/agents/tasks/TASK-20260511-001-PM-to-DEV.md
docs/agents/tasks/REPORT-20260511-001-DEV-to-PM.md
docs/agents/tasks/TASK-20260511-002-PM-to-OPS.md
docs/agents/tasks/REPORT-20260511-002-OPS-to-PM.md
```

commit message：

```text
docs(s6-v0.2-sprint0-mt5-archive): hotfix dispatch and reports

Archives TASK + REPORT files from MT-5 (BUG-SDK-007) hotfix cycle
plus the v0.2.0-beta.2 closing docs (TASK/REPORT-014) and the
repo-hygiene fix (TASK/REPORT-20260511-002).

Refs: TASK-20260511-003.
```

### 2.4 推送范围

| 仓库 | main | tag |
|---|---|---|
| origin | ✅ push `094a0b2..<new-HEAD>` | ❌ **不**推 v0.2.0-beta.3 |
| backup | ✅ push | ❌ |
| gitee | ❌ **不**碰（保持 G3 隔离）| ❌ |

## 三、Safety HARD GATE（OPS 必做）

| 检查 | 期望 |
|---|---|
| stage 前 `git diff` 含 `crsr_[0-9a-f]{16,}` | 0 hit |
| stage 后 `git diff --cached` 含同 pattern | 0 hit |
| `.env*` 进 staged | 0 hit |
| `.smoke-*` 进 staged | 0 hit（OPS-002 加的 `.gitignore` 应已覆盖；额外核验）|
| Commit A staged file count | 严格 **7** |
| Commit B staged file count | 严格 **9** |
| `.smoke-qa014-20260511-080830/` 是否已删 | 若仍被进程占用，**不强杀**，写进 REPORT 即可（与 OPS-002 §二 一致） |

## 四、不在范围（PM 明令）

- ❌ 不动任何代码文件（除 PM 列的 7 个 modified）
- ❌ 不推 v0.2.0-beta.3 tag（同 v0.2.0-beta.x 全系列）
- ❌ 不动 gitee
- ❌ 不动 codeflow-pwa Dependabot vulns
- ❌ 不动 BUG-SDK-005/006（等 QA-003 验证后另议）
- ❌ 不动 `.smoke-qa014-20260511-080830/`（仍 locked，等占用进程退出后下一轮 OPS micro-task）

## 五、SLA

| 阶段 | 预期 |
|---|---|
| Safety check + stage Commit A | 3 min |
| commit A | 1 min |
| tag | 1 min |
| stage Commit B | 2 min |
| commit B | 1 min |
| push origin main | 2 min |
| push backup main | 2 min |
| 验证（git log / show / ls-remote）| 3 min |
| REPORT 撰写 | 10 min |
| **合计** | **~25 min** |

可在 DEV-002 (P3 relay-bridge) 启动前完工。

## 六、回执要求

`REPORT-20260511-003-OPS-to-PM.md`，必含：

- 一句话结论
- §一 Commit A 验证（`git log -1 --stat`）
- §二 本地 tag 验证（`git show v0.2.0-beta.3 --stat`）
- §三 Commit B 验证（`git log -1 --stat`）
- §四 Safety HARD GATE 6 项核验输出
- §五 推送验证（`git rev-parse origin/main` vs local + `git rev-parse backup/main` vs local + `git ls-remote --tags origin v0.2.0-beta.3` 应空 + gitee HEAD 仍 `62532a7`）
- §六 post-commit `git status --short`（应只剩 OPS-003 自身的 TASK + REPORT 两文件）
- §七 任何 surprise / git hygiene 观察

## 七、Charter / 自约束遵守证明

- **Charter 5**：纯仓库操作不动协议层
- **自约束 5**：DEV-001 完工后派 OPS commit 是常规推荐 / sprint 内动作，PM 自决
- **自约束 6/7**：N/A（与上游协议无关）

---

PM-01
2026-05-11 10:15 (UTC+8)
