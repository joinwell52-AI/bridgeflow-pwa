---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260511-004
sender: PM
recipient: QA
priority: P1
thread_key: codeflow-v0.2.0-beta-3-d8-bug-comparison-matrix
references:
  - REPORT-20260511-001-PM-to-ADMIN
  - REPORT-20260511-001-DEV-to-PM
  - REPORT-20260510-014-QA-to-PM
  - REPORT-20260511-003-OPS-to-PM
layer: governance
---

# TASK-20260511-004：D8 自动化回归审计 — v0.2.0-beta.3 验收 + BUG-SDK-001/002/003/004/007 前后对比矩阵

## 一、TL;DR

接 ADMIN 5/11 10:21 D8 授权「自动化回归审计 + Bug 修复前后对比数据」。OPS-003 已落 v0.2.0-beta.3 commit + tag，**立即可验**。

本任务 4 大目标：
1. v0.2.0-beta.3 完整 A-07~A-10 smoke 验收（**核心**：验证 BUG-SDK-007 已修，BUG-SDK-001/002/003/004 仍 closed）
2. **5 版本 BUG 前后对比矩阵**（beta / beta.1 / beta.2 / beta.3 / + 各 BUG 引入版本）
3. 复观察 BUG-SDK-005（ripgrep noise）+ BUG-SDK-006（reviewer race）
4. Cursor SDK 失败模式总结（QA 视角）

**关键澄清**：PM 之前在 ADMIN 引用的 D8 指令中提到 `patrol_mailbox` 工具 — 该工具**不存在于 fcop-mcp v1.1.0 工具集**。本任务用以下工具组合替代：
- (a) fcop 现存：`list_tasks` / `list_reports` / `fcop_check` / `get_team_status` / `list_reviews`
- (b) CodeFlow 自身：runtime 112 tests / smoke 脚本 / `agents.json` 直读

## 二、必交付清单

### 2.1 主交付 1：v0.2.0-beta.3 完整 A-07~A-10 smoke 验收（必做）

按 QA-014 已建立的 A 系列验收 case 重跑 v0.2.0-beta.3，验证 BUG-SDK-007 修复：

| Case | 输入 | 期望 |
|---|---|---|
| **A-07 (no model)** | `CURSOR_DEFAULT_MODEL` 未设置 + ADMIN key | banner OK + WARNING 显示 + `Status: running` |
| **A-07 (with model)** | `CURSOR_DEFAULT_MODEL=claude-sonnet-4` + ADMIN key（QA-014 三轮 100% crash 配置）| banner OK + NO WARNING + `Status: running` ← **核心 acceptance（BUG-SDK-007 关闭判定）** |
| **A-08 (real verdict)** | A-07 with-model 启动后 drop `TASK-X-PM-to-DEV.md` 到 inbox + drop sample review-trigger | DEV transcript 产出 + REVIEW transcript 产出 + verdict 解析 OK（`decision != verdict_parse_failed`，BUG-SDK-004 仍 closed） |
| **A-09 (UUID format)** | A-07 / A-08 跑后 | `agents.json` 含 ≥2 个 `sdk_agent_id: "agent-<uuid>"` |
| **A-10 (transcript)** | A-08 跑后 | `transcript-<session_id>.jsonl` 文件生成 + 内容含 SDK message + tool-use + result events |

**Safety HARD GATE**（与 QA-014 同款）：
- 全程不读 `.env` 内容
- 全程不 echo key
- REPORT 不含 key / 无 key 在 stack
- 删除 smoke 后用 `git status` 验证 `.env*` 未泄漏
- `.smoke-*` 不进 staged

### 2.2 主交付 2：5 版本 BUG 前后对比矩阵（必做，**D8 ADMIN 明示要求**）

**SDK BUG 矩阵**（D8 「Bug 修复前后对比数据」的直接产出）：

| 版本 | tag | BUG-SDK-001 | BUG-SDK-002 | BUG-SDK-003 | BUG-SDK-004 | BUG-SDK-005 | BUG-SDK-006 | BUG-SDK-007 |
|---|---|---|---|---|---|---|---|---|
| pre-MT-1 | v0.2.0-beta | ❌ 触发 | ❌ 触发 | ❌ 触发 | ❌ 触发 | ⏸ 待观察 | ⏸ 待观察 | ❌ 触发 |
| post-MT-1 | v0.2.0-beta.1 | ✅ 修 | ❌ 触发 | ❌ 触发 | ❌ 触发 | ⏸ | ⏸ | ❌ 触发 |
| post-MT-2/3/4 | v0.2.0-beta.2 | ✅ | ✅ 修 | ✅ 修 | ✅ 修 | ⏸ | ⏸ | ❌ 触发 |
| post-MT-5 | **v0.2.0-beta.3** | ✅ | ✅ | ✅ | ✅ | ❓ 待验 | ❓ 待验 | ✅ **修** |

→ QA **实测**填 5×7 矩阵（最少 4 cell：beta.3 列实测 5 BUG / + 历史回填用 commit message + 历史 REPORT 引用）。

**输出**：`docs/agents/tasks/REPORT-20260511-004-QA-to-PM.md` §二「BUG 前后对比矩阵」表 + 各 cell 给出**关键证据片段**（stderr 摘录 / agents.json 摘录 / transcript 摘录，每段 ≤ 5 行）。

### 2.3 主交付 3：BUG-SDK-005 + BUG-SDK-006 复观察（条件性）

BUG-SDK-007 修复后，可进入 A-08 完整流程 → **此时**才有机会观察：

| BUG | 复观察期望 |
|---|---|
| **BUG-SDK-005**（ripgrep stderr noise）| A-08 drop task 后 stderr 是否含 `Ripgrep path not configured` / `LocalIgnoreService` 噪音 |
| **BUG-SDK-006**（reviewer 0 dispatch + task double dispatch race）| A-08 跑 ~5 次（每次新 dataDir + drop task），统计 race 出现频率 |

判定：
- 5/5 出现 → P1，立即升级
- 1-2/5 出现 → P2 间歇性，记录
- 0/5 出现 → 暂归档（DEV-013 + DEV-001 间接 smoke 可能未踩）

### 2.4 主交付 4：Cursor SDK 失败模式总结（QA 视角）

基于 BUG-SDK-001/002/003/004/007 五个已发现失败的根因，撰写一份 ~1-2 页摘要（写进 REPORT §六）：

| 失败类别 | 触发条件 | 检测建议（CodeFlow runtime 应该做什么）|
|---|---|---|
| 模型 ACL（BUG-007）| key tier × `Agent.create({model})` | banner 启动时实测 model 兼容性 |
| Run 重入（BUG-002）| 本地 mode `Agent.create + resume + send` | 默认 `local: { force: true }` |
| Verdict 解析（BUG-004）| SDK content[] 数组型响应 | `extractText()` 多 probe 兼容 |
| 模型字符串（BUG-003）| `.env.example` 文案错（`auto` vs `default`）| 启动校验 model 名 |
| 模型缺失（BUG-001）| no `defaultModel` + no per-task | banner WARNING 路径 |

**不在范围**（明令）：
- ❌ 不调用 `patrol_mailbox`（工具不存在）
- ❌ 不向 fcop 提 issue / PR（Charter 5 + 自约束 7）
- ❌ 不动 v0.2.0-beta.3 代码 / 测试 / commit
- ❌ 不动 BUG-SDK-005/006 的修复（仅观察 + 记录）

## 三、SLA + 时间表

| 阶段 | 预期 |
|---|---|
| A-07 (no model) smoke | 5 min |
| A-07 (with model) smoke | 10 min（关键 acceptance）|
| A-08 (real verdict drop task) smoke ×5 | 30-45 min（含 race 观察）|
| A-09 / A-10 验证 | 5 min |
| 5 版本对比矩阵填表 | 30 min（含历史回填）|
| Cursor SDK 失败模式总结 | 20 min |
| REPORT 撰写 | 30 min |
| **合计** | **2-2.5 小时** |
| **PM Deadline** | 今天 EOD（5/11 18:00 UTC+8）|

## 四、回执要求

`REPORT-20260511-004-QA-to-PM.md`，必含：

- §一 一句话结论（v0.2.0-beta.3 是否 ship-fully-ready 还是仅 ship-unit-ready）
- §二 **5 版本 × 7 BUG 矩阵表**（D8 核心产出）
- §三 A-07/08/09/10 完整实测 stdout/stderr（关键行，每段 ≤ 10 行）
- §四 BUG-SDK-005 复观察判定（实测次数 / 频率）
- §五 BUG-SDK-006 复观察判定（实测次数 / 频率）
- §六 Cursor SDK 失败模式总结（5 类）
- §七 Safety HARD GATE 5 项核验输出
- §八 BUG-SDK-001/002/003/004/007 closed 最终 sign-off（QA 视角）
- §九 给 PM 的下一步建议（如发现新 BUG / 工具不足）

## 五、Charter / 自约束遵守证明

- **Charter 5**：用 fcop 现存工具（`list_tasks` 等），不主动设计「自动巡检 mailbox」类需求
- **自约束 7**：BUG-SDK-005/006 单点不构成涌现，**不**向 fcop / Cursor 提 issue
- **自约束 8 草拟**：本任务给最便宜方案（人工跑 smoke），不预先工程化为自动化 CI
- **自约束 9 草拟**（task 级）：本 TASK 的 API 调用示例 PM 未现场核对 — 若 QA 发现错请按 DEV-005 风格在 REPORT 中指出，PM 接受错误自披露

---

PM-01
2026-05-11 11:30 (UTC+8)
