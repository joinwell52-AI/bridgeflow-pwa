---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-015
sender: PM
recipient: OPS
priority: P1
thread_key: s3-followup-docs-patch-commit
references:
  - REPORT-20260509-012-OPS-to-PM
  - REPORT-20260509-014-QA-to-PM
  - REPORT-20260509-016-PM-to-ADMIN
  - TASK-20260509-012-PM-to-OPS
layer: worker
---

# Sprint S3 follow-up：docs patch commit（test-strategy TS-2.8 update + §11 v2 packaging spec）+ origin/backup push

## 背景

[`TASK-012`](./TASK-20260509-012-PM-to-OPS.md) commit `407cfa5` 落地后，又出现了**两份独立的文档级改动**：

1. **QA-14 post-commit diff**（`packages/codeflow-runtime/docs/test-strategy-s3.md`）= QA 在 OPS commit 之后才完成 TS-2.8 + TS-4.x 的细化，按 §"特别确认"OPS 之前已识别但未追加 commit
2. **ADMIN 5/9 14:33 拍板的 §11 设计章节落档**（`docs/design/codeflow-v2-on-fcop-sdk.md`）= PM 已起草完成（+261 行净增 / -47 行）

加上 PM 写完的两份配套报告/任务，本批一起打 patch commit。

## 待 commit 文件清单（7 项）

按 `git status --short` 当前可见：

```
M  packages/codeflow-runtime/docs/test-strategy-s3.md         (QA 5/9 14:25 post-commit diff，未进 407cfa5)
M  docs/design/codeflow-v2-on-fcop-sdk.md                    (PM 5/9 14:35 §11 落档 +261 行净增)
?? docs/agents/tasks/REPORT-20260509-012-OPS-to-PM.md         (OPS 自己写的 TASK-012 回执，未进 407cfa5)
?? docs/agents/tasks/REPORT-20260509-014-QA-to-PM.md          (QA 自己写的 TASK-014 回执，未进 407cfa5)
?? docs/agents/tasks/REPORT-20260509-016-PM-to-ADMIN.md       (PM 5/9 14:50 巡检 + W1/W2/W3 落档)
?? docs/agents/tasks/TASK-20260509-015-PM-to-OPS.md           (本文件)
?? docs/agents/tasks/TASK-20260509-016-PM-to-DEV.md           (PM 5/9 14:48 已落盘，TS-1.6 follow-up)
```

OPS 执行前**必须**用 `git status --short` 验证总数 = **7**（前 6 项已落，第 7 项 = 本文件 PM 已写完即可触发）。

> **注**：commit `407cfa5` 落地时，OPS 自己的 `REPORT-012` 和 QA 自己的 `REPORT-014` 都还没生成——回执是在 commit 完成**之后**才写的——所以这两份自然落到 follow-up 批次。

## 加文件原则

```powershell
# 只加这两个目录里的所有改动
git add docs/
git add packages/codeflow-runtime/docs/

# 验证 staged 范围（必须只在 docs/ 路径下）
git diff --cached --name-only
```

期望：staged 文件**全部**位于 `docs/` 之下（包括 `packages/codeflow-runtime/docs/`），**不**包含：
- `packages/codeflow-runtime/src/`（DEV Phase B 的工作区，绝不能把未完成的 SessionManager 误入）
- `packages/codeflow-protocol/`（本批不应有改动）
- `_ignore/` / `private/` / `node_modules/` / `.codeflow/state/`

## 高危操作判定

按 [`ops-bridge.mdc`](../../.cursor/rules/ops-bridge.mdc) 4 类高危：
- 重启生产服务 ❌ 不涉及
- 修改 Nginx 配置 ❌ 不涉及
- 清理数据库或日志 ❌ 不涉及
- 变更防火墙规则 ❌ 不涉及

→ **本任务不属于高危**。

## 执行命令（按顺序）

### Phase 1：准备 + 验证 staged scope（≤ 1 min）

```powershell
# 1.1 验证当前文件总数
git status --short
# 期望：5 行（2 M + 3 ??），全部在 docs/ 之下

# 1.2 验证不动 DEV Phase B 工作区
git diff --stat -- packages/codeflow-runtime/src/
# 期望：空输出（DEV 还在改 src/session/ 但还未保存到工作区，或者 OPS 应等 DEV 也在 saving 之外的窗口期跑此任务）
```

如果 `packages/codeflow-runtime/src/` 有未保存的 modified 文件 = DEV 在改——**OPS 应等待 DEV 进入下一个 milestone 或主动通知 PM 协调**。

### Phase 2：commit（≤ 1 min）

```powershell
# 2.1 加文件（只加 docs/）
git add docs/
git add packages/codeflow-runtime/docs/

# 2.2 验证 staged 范围
git diff --cached --name-only
# 必须看到：5 个文件全部在 docs/ 路径下

# 2.3 commit
git commit -m "docs(s3-followup): test-strategy TS-2.8 update + section 11 v2 packaging spec (codeflow-shell + Node SEA)"

# 2.4 验证
git log -1 --oneline
git show --stat HEAD | Select-Object -First 10
```

### Phase 3：push origin + backup（≤ 2 min；gitee 跳过）

```powershell
git fetch --all
git status -sb
# 期望：包含「ahead 1」

git push origin main
git push backup main

Write-Host "gitee push skipped per HANDOFF-001 G3 decision (gitee remains diverged at 62532a7)"

# 三仓 HEAD 对比
$localHead  = git rev-parse HEAD
$originHead = (git ls-remote origin main) -split "`t" | Select-Object -First 1
$backupHead = (git ls-remote backup main) -split "`t" | Select-Object -First 1
$giteeHead  = (git ls-remote gitee  main) -split "`t" | Select-Object -First 1
Write-Host "local : $localHead"
Write-Host "origin: $originHead"
Write-Host "backup: $backupHead"
Write-Host "gitee : $giteeHead   (expected: 62532a7…)"
```

期望：
- local / origin / backup 三者 HEAD 一致（新 commit hash）
- gitee 仍 `62532a7…`

## 失败处理

| 错误 | 处理 |
|---|---|
| Phase 1.2 `packages/codeflow-runtime/src/` 有未保存改动 | 等待 / 通知 PM；不强制 commit |
| Phase 2.3 commit 被 pre-commit hook 拦 | 看 hook 输出；不 `--no-verify` |
| Phase 3.2 origin `non-fast-forward` | 停手，报告 PM（不擅自 rebase）|
| Phase 3.3 backup 鉴权失败 | 重试 1 次；仍失败 → 报告（origin 成功 = 不阻塞总任务）|
| 网络超时 | 重试 1 次；仍失败 → 报告 |

## 验收（OPS 必出 6 条）

| # | 验收项 | 命令 |
|---|---|---|
| 1 | commit 前文件 = 7 项，全部在 `docs/` 之下 | `git status --short` |
| 2 | DEV Phase B 工作区未触及 | `git diff --stat -- packages/codeflow-runtime/src/` 为空 |
| 3 | commit message 正确 | `git log -1 --pretty=%s` 含 `docs(s3-followup):` |
| 4 | commit 包含 7 项 | `git show --stat HEAD` 显示 `7 files changed` |
| 5 | origin / backup HEAD = local | `git ls-remote` 头部 hash 对得上 |
| 6 | gitee 仍 `62532a7…` | `git ls-remote gitee main` |

## 不动手清单

- ❌ 不创建 tag
- ❌ 不 push gitee
- ❌ 不 commit `packages/codeflow-runtime/src/` 任何 src 文件（DEV Phase B 工作区）
- ❌ 不动 `_ignore/` / `private/` / `node_modules/` / `.codeflow/state/`
- ❌ 不修改 `.git/config`
- ❌ 不动 `packages/codeflow-protocol/`

## 时间预算

≤ 4 分钟（Phase 1: 1min + Phase 2: 1min + Phase 3: 2min）

## 启动条件

OPS 在以下两件都为真后开干：

1. ✅ **PM 已通知"7 单全部落盘"**（即 PM 完成本任务 + TASK-016 + REPORT-016 写入后）—— 5/9 14:50 PM 通知已发出
2. ✅ **DEV 在 milestone 边界**（已保存当前进度，未 actively saving src/session/ 文件）——OPS 可在执行 Phase 1.2 时确认

## 完成后回执要求

按 `ops-bridge` 规则写 `REPORT-20260509-015-OPS-to-PM.md`，含：

1. 6 条验收命令的实际输出
2. 三仓最终 HEAD 对比表
3. 本次 commit 的 SHA-1 + 完整 hash
4. 任何 push / commit 失败的根因（如有）
5. **特别确认**：DEV Phase B 工作区未受影响（`packages/codeflow-runtime/src/` 的 git diff 状态在 commit 前后对比）

不能只写"已完成"——必须附验证命令输出。

---

PM-01 派单。OPS-01 等 PM "5 单落盘"信号 + DEV 在 milestone 边界后立即执行。回执请落 `REPORT-20260509-015-OPS-to-PM.md`。
