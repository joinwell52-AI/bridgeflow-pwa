---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260511-009
sender: PM
recipient: DEV
priority: P0
thread_key: codeflow-p4-main-sprint-fcop-pythonia-integration
references:
  - TASK-20260511-007-PM-to-DEV
  - REPORT-20260511-007-DEV-to-PM
  - REPORT-20260511-008-OPS-to-PM
layer: worker
---

# TASK-20260511-009：P4 主 sprint — Day 2 加速启动（TaskDispatcher / TaskParser 改造）

## 一、TL;DR

ADMIN 5/11 12:53 拍 **「按推荐」/「B」/「干」** = 选项 B 加速 = **Day 2 现在启动**（不等明天 5/12）。

Day 1 commit `f559904` 已落 + origin/backup MATCH + worktree clean。DEV 38min 完工 Day 1（vs SLA 0.5-1d，8-16x 快），节奏档案显示 DEV 状态极佳，spike + Day 1 经验新鲜，**今天 EOD 完成 Day 2 是合理预期**。

## 二、PM 第 10 次错误自披露（task 级 path/method 核对失败 — 第二次）

PM 已用 ripgrep 核对了 TASK-007 §四 Day 2 的实际代码，**发现 2 处事实错误**：

| TASK-007 §四 Day 2 PM 写 | 实际事实 | 影响 |
|---|---|---|
| `TaskDispatcher.dispatchOne()` 删手写 frontmatter / parse | TaskDispatcher 不 parse；parse 在独立类 `packages/codeflow-runtime/src/scheduler/TaskParser.ts`（Step 1 通过 `this._parser.parse(filepath)` 调用） | Day 2 改造点 = TaskParser 而非 TaskDispatcher 内部 |
| `TaskDispatcher.archive()` 改用 fcop `archive_task$()` | TaskDispatcher **没有 archive() 方法**。v0.2.0-beta.3 当前 task 文件不被移动/重命名，只写 state_history.jsonl | archive 概念是**新涌现需求**，Day 2 内不引入 |

**第 9 条自约束 path 版（已生效）**：本 TASK 全部路径已 ripgrep 核对，确认存在。

## 三、Day 2 真实范围（PM 修正版）

### 3.1 主目标（必交付）

**让 task 文件读取从「CodeFlow 自有 TaskParser」切换到「fcop@1.1.0 + pythonia」。**

具体实施由 DEV 自决；PM 提两条**可选**路径：

**路径 A（推荐 — DEV 自决）**：在 `TaskParser.ts` 内部用 `FcopProjectClient.readTask()` 替代 yaml 手 parse。
- 优点：调用方 (TaskDispatcher) 0 修改
- 优点：保留 `ParsedTask` interface，旧测试可改可不删（Day 5 再统一收编）
- 缺点：多一层 TaskParser 包装

**路径 B**：删 `TaskParser.parse()`；让 TaskDispatcher 直接调 `FcopProjectClient.readTask()`，改 Step 1 实现。
- 优点：调用链短
- 缺点：TaskDispatcher.test.ts 大改

**DEV 自由度**：选 A、选 B、或第三条路（如 keep TaskParser 作为 fcop client thin proxy）。**REPORT-009 §自决审计**说明即可。

### 3.2 次目标（可选交付 — DEV 自决推迟到 Day 3 / Day 5）

| 子任务 | DEV 自决推迟到 | PM 建议 |
|---|---|---|
| `InboxWatcher` 接 fcop `inspect_task$()` 启动期 schema 健康检查 | Day 3 OR Day 5 | 建议 Day 3 一起做 |
| 引入「archive task」概念（fcop `archive_task$()` 调用点）| Day 3 OR Day 5 | 建议**Day 5 schema 清理时一并设计**，因为这是新涌现需求，需要先想清楚 codeflow-shell 何时 archive：session_ended? state=ended? 用户手动? |
| 删除 `TaskParser.test.ts` 已过时的测试 | Day 5 | 建议 Day 5（schema 清理时）|

### 3.3 不动（明令）

- ❌ 不动 `AgentSdkAdapter.ts` / `SessionManager` / `RuntimeBootstrap` / `atomic-write.ts`
- ❌ 不动 `FcopProjectClient.ts`（Day 1 已 ship；Day 2 是**调用方**改造）
- ❌ 不动 `Runtime.ts` 高层
- ❌ 不动 PWA / relay-bridge / install.ps1
- ❌ **不引入 archive 概念**（推到 Day 5 设计）
- ❌ 不 commit（PM 决定 Day 3 / Day 6 阶段性 commit）
- ❌ 不 bump 版本号（仍 `0.2.0-beta.3`）

## 四、与 Day 1 集成方式

DEV-007 Day 1 已 ship 的 `FcopProjectClient`：
- `client.readTask(filepath)` → returns `FcopTask`（Promise）— **Day 2 主用接口**
- `client.listTasks(...)` → returns `FcopTask[]` — Day 2 可能用作 fallback
- `assertFcopReady()` → Day 1 已在 codeflow-shell main.ts 启动时调

Day 2 需要解决的字段映射（基于 DEV-005 §五 + DEV-007 fcop-client.ts）：

| ParsedTask (CodeFlow) | FcopTask (fcop@1.1.0) | 状态 |
|---|---|---|
| `task_id` | `task_id` | ✅ 同名 |
| `sender` | `sender` | ✅ 同名 |
| `recipient` | `recipient` | ✅ 同名 |
| `priority` | `priority` | ✅ 同名（fcop 已 decode enum）|
| `thread_key` | `thread_key` | ✅ 同名 |
| `layer` | `layer` | ✅ 同名 |
| `frontmatter` (raw Record) | n/a | ⚠️ fcop 不提供 raw frontmatter；DEV 自决 → 用 fcop 字段重组 OR 改 ParsedTask interface 删此字段 |
| `body` | ⚠️ 需 ripgrep 看 fcop Task 是否暴露 body | ⚠️ DEV 确认 |

**DEV-007 fcop-client.ts** 已经实现了 `readTask(taskProxy)` 平铺逻辑（见 REPORT-007 §二代码片段），Day 2 直接复用 — 但 `body` 字段确实是要 DEV 在 spike 经验之外**新确认**的点。

## 五、SLA

DEV-007 Day 1 实际 38min（vs PM SLA 0.5-1d / 4-8h）= **8-16x 快**。

Day 2 PM SLA = **1.5 工作日（12-14h）**，DEV 实际预期：
- **乐观（保 8x 加速）**：1.5-2h
- **保守（保 4x 加速）**：3-4h
- **基线**：< 5h

**预期完工时间**：今天 (5/11) 14:30-17:30 之间。

若 DEV 卡住 ≥ 90min（占 SLA 12.5%），写 in-progress REPORT 自交流，PM 决定是否调整路径。

## 六、回执要求

DEV 写 **同一文件 `REPORT-20260511-007-DEV-to-PM.md` 追加 `## Day 2 (5/11 EOD)` 段**（按 TASK-007 §八的追加式约定）。

最低必含：
- §一 Day 2 一句话结论（路径 A/B/C 自决 + ship-ready 与否）
- §二 完工证据（diff stat + 121→新 tests 数 + tsc clean × 3 + 关键 smoke 输出）
- §三 surprise 揭示（Day 2 至少 1 段）
- §四 与 PM §三.2 三个可选子任务的对照（DEV 决定推迟到 Day 3 / Day 5 / 提前做完）
- §五 给 PM 的 Day 3 启动方向预告
- §六 self-justification（修改的文件 / 影响范围 / 是否需要重启）

## 七、与其他 lane 的关系

| lane | 状态 | 备注 |
|---|---|---|
| OPS | idle（Day 1 commit 已落）| Day 3 / Day 6 才需再 commit |
| QA | sign-off 完成（v0.2.0-beta.3 ship-ready）| Day 6 才验 v0.3.0-alpha |
| PM | 与 DEV 并行干 emergence-log + Charter 5 入档 | 不阻塞 |
| ADMIN | 接收 PM 通报，仍是 push 总线 | Day 2 需通知 DEV 一次 |

## 八、PM 自决审计

| 决策 | 性质 | 处置 |
|---|---|---|
| 接 ADMIN「按推荐」加速指令 | ADMIN 明示战略 | ✅ 执行 |
| Day 2 今天启动（不等 5/12）| 工程节奏 | ✅ 自决（DEV 状态 + 经验新鲜，与 ADMIN「加速推进」5/10 战略一致）|
| 主动暴露 PM 第 10 次错误自披露 | Charter 5「不重发明」+ 自约束 9 path 版 | ✅ 自决（与第 7/8 次自披露一致 — 透明优于掩饰）|
| 范围只锁主目标 + 给 DEV 极高自由度 | DEV-007 Day 1 完工质量证明 DEV 自决能力强 | ✅ 自决 |
| archive 推迟到 Day 5 | 涌现需求需要先想清 codeflow-shell 何时调，非 Day 2 范围 | ✅ 自决 |
| 仍不 commit Day 2 | 累积到 Day 3 / Day 6 阶段性 commit | ✅ 自决 |

## 九、Charter / 自约束遵守证明

| 项 | 立场 |
|---|---|
| Charter 5「CodeFlow = FCoP 应用」 | ✅ Day 2 是 Charter 5 第二步落地 — 删 CodeFlow 自有 task parse 代码 |
| 自约束 5「重大变更上交」 | ✅ Day 1 提前完工 + Day 2 提前启动属节奏调整，不属战略变更，PM 自决（已通报）|
| 自约束 7「不主动设计协议需求」 | ✅ archive 概念暂不引入；若 Day 5 设计时发现 fcop archive_task$ 不便，先内部消化，**≥ 3 次涌现才报 fcop** |
| 自约束 9 path 版 | ✅ 本 TASK 全部路径已 ripgrep 核对 |
| 自约束 10「不承诺 ADMIN 不需通知」 | ✅ §七明示「ADMIN 仍是 push 总线，Day 2 需通知 DEV 一次」|

---

PM-01
2026-05-11 12:57 (UTC+8)
