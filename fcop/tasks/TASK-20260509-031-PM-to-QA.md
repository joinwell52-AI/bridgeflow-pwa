---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-031
sender: PM
recipient: QA
priority: P1
thread_key: codeflow-v2-sprint-s6-e2e-acceptance-three-scenarios
references:
  - REPORT-20260509-028-DEV-to-PM
  - REPORT-20260509-027-QA-to-PM
  - TASK-20260509-030-PM-to-OPS
  - codeflow-shell/README.md
layer: governance
---

# v0.1.0-rc.1 internal RC E2E acceptance — 3 场景验收 + ADMIN 试用前置确认

## 背景

DEV-028 已完工 S6 codeflow-shell MVP（详见 [REPORT-028](./REPORT-20260509-028-DEV-to-PM.md)）。OPS-030 即将 commit + 本地 tag `v0.1.0-rc.1`（不推 origin tag = internal RC 不公开发布）。

按 PM 自约束，QA 接 v0.1 完工前最后一关：**E2E acceptance 三场景**（在 OPS-030 commit baseline 上跑），确认 ADMIN 试用前一切就绪。

## §一 工作内容（3 场景 + 1 跨阶段回归 + 1 推荐）

### 场景 1：双击启动场景（fallback = `npm start` 替代）

按 DEV REPORT-028 §三 surprise 1，Node SEA EXE 走 fallback 路径。**ADMIN 试用入口 = `npm start`**（不是双击 EXE）。

QA 验证步骤：

```powershell
cd D:\Bridgeflow\codeflow-shell
npm install
npm start  # 期望：banner + skills loaded=3 + agents mounted + 等待 inbox
```

验收点：
- ✅ banner 正确显示「CodeFlow v0.1.0-rc.1 — internal preview」
- ✅ Data dir / Inbox / Reviews / Skills loaded 路径输出
- ✅ MCP injector 模式 `stub`（**不是** live）
- ✅ Bootstrap report 正确（`success=0, failed=0, kernel_failures=0` 是 expected — DEV-01/REVIEW-01 是 register 阶段加，不在 bootstrap reconcile 里）
- ✅ 进程持续 running，等待 inbox

### 场景 2：drop sample-task.md → governance loop 完整跑通

```powershell
# 在另一个 PowerShell 窗口（保持场景 1 进程 running）：
cd D:\Bridgeflow\codeflow-shell
copy examples/hello-world/sample-task.md ".smoke-test-state\inbox\TASK-20260509-999-PM-to-DEV.md"
# 看场景 1 主窗口 stdout 应输出：
#   [InboxWatcher] add ... 999 ...
#   [TaskDispatcher] dispatching ...
#   [SessionManager] starting session for DEV-01
#   [SessionManager] session ended
#   [ReviewEngine] starting review ...
#   [ReviewWriter] wrote REVIEW-...md
#   [NeedsHumanGate] human approval required (cli sink)
#   [StateHistoryWriter] appended dispatched / running / done / review_done
```

验收点：
- ✅ stdout 显示完整 governance loop 链路
- ✅ `.smoke-test-state\reviews\REVIEW-20260509-999-REVIEW-on-TASK-20260509-999-PM-to-DEV.md` 落档
- ✅ REVIEW 文件内容含 `decision: needs_human` + `human_approval` 子结构（B.1 决策实证）
- ✅ `.smoke-test-state\sessions\session-*.json` × 4 落档（DEV + REVIEW 各 2 个 session）
- ✅ `.smoke-test-state\transcripts\run-mem-*.md` × 2 落档
- ✅ inbox 文件被消费（FIFO move 到 history 或 delete）
- ⚠️ Surprise 4 (EPERM 偶发) 可能出现 stderr — **零功能影响**，QA 标 known-issue 即可

### 场景 3：Ctrl+C 优雅退出

回到场景 1 的 PowerShell 窗口：

```
# 直接在窗口里按 Ctrl+C
```

验收点：
- ✅ stdout 输出「[shell] received SIGINT, stopping runtime...」
- ✅ stdout 输出「[shell] runtime stopped cleanly. Goodbye.」
- ✅ 进程退出码 = 0
- ⚠️ DEV REPORT-028 §三 surprise 2 — 注意 child_process spawn 出来的进程 SIGINT 不工作，但**交互式 PowerShell 直接 Ctrl+C 工作正常**

### §1.4 跨阶段回归确认

QA 已在 REPORT-027 跑过 30x 0 flaky 复核（OPS-026 baseline）。**本任务 OPS-030 引入 codeflow-shell + 4 个 README 修订 + version bump，但 runtime src/ 完全不动**。

QA 1x 复核：

```powershell
cd D:\Bridgeflow\packages\codeflow-runtime
npm test 2>&1 | Select-String "tests \d+|pass \d+|fail \d+"
# 期望：tests 94 / pass 94 / fail 0
```

无需 30x（DEV REPORT-028 + PM 独立复核已验过 OPS-030 baseline）。

### §1.5 v0.1.0-rc.1 internal RC 发布前置确认

| 约束 | 验证方式 |
|---|---|
| 本地 tag `v0.1.0-rc.1` 已创建 | `git tag --list "v0.1.0-rc.1"` |
| **origin/backup tag 仍空**（internal RC 不公开）| `git ls-remote --tags origin / backup` 不含 `v0.1.0-rc.1` |
| `package.json` version = `0.1.0-rc.1` | `Select-String -Path packages/codeflow-runtime/package.json -Pattern '"version":'` |
| 顶层 README + protocol README + runtime README 含 v1.0 alignment block | 文档查 |
| `docs/releases/v0.1.0-rc.1.md` release notes 完整 | 文档查 |

## §二 ADMIN 试用引导（QA 整理供 PM 引用 — 不必 QA 写完整版本）

QA 完成场景 1+2+3 后，在回执 §五给出 **ADMIN 试用一行命令清单**：

```powershell
# 在 D:\Bridgeflow 目录
cd codeflow-shell
npm install
npm start
# 第二个 PowerShell 窗口：
copy examples/hello-world/sample-task.md "$env:USERPROFILE\.codeflow\v2\inbox\TASK-20260509-999-PM-to-DEV.md"
# 主窗口看 governance loop 跑完
# Ctrl+C 退出
```

包含 ADMIN 该看到的关键 stdout 片段（5-10 行）+ 哪些目录出新文件 + 已知 known-issue。

## §三 不做（out-of-scope）

- ❌ 不写自动化测试代码（DEV 写）
- ❌ 不修改任何 src/
- ❌ 不修改任何 schema
- ❌ 不修改 design doc
- ❌ 不动 PM 4 文档（DRAFT-001 / REPORT-024 / v0.2-roadmap / TASK-028）
- ❌ **不**实施 v0.1.0-rc.1 公开发布动作

## §四 验收标准（6 项）

| # | 项 | 验证方式 |
|---|---|---|
| 1 | 场景 1 `npm start` 启动成功 | stdout 截图 |
| 2 | 场景 2 governance loop 完整跑通 | stdout 截图 + REVIEW + sessions + transcripts 落档清单 |
| 3 | 场景 3 Ctrl+C 优雅退出 | stdout 截图 + exit code 0 |
| 4 | 94/94 tests baseline 仍 PASS | npm test stdout |
| 5 | 5 项 internal RC 发布前置确认 | 命令输出 |
| 6 | ADMIN 试用一行命令清单 | 回执 §五 完整段落 |

## §五 时间预算

- **30-45 分钟**（3 场景 + 1 回归 + 写引导）
- 场景 1+2+3 约 15 min（含 npm install）
- 跨阶段回归 1 min
- 写引导 + 回执 ~15-30 min

## §六 启动条件

QA 见到本任务可立即开始，但**实施 wait until** OPS-030 commit + 本地 tag 完成（约 ~10 min 后）。

## §七 完成后回执要求

按 `qa-bridge` 规则写 `REPORT-20260509-031-QA-to-PM.md`，必须包含：

1. 3 场景实测 stdout（≥ 6 验收点逐项 ✅/❌）
2. §1.5 5 项发布前置确认
3. §1.4 baseline 1x 复核结果
4. ADMIN 试用一行命令清单（§二）
5. 任何阻塞项（如发现）
6. v0.1.0-rc.1 internal RC ADMIN 试用 ✅/❌ 推荐

---

PM-01 派单。QA-01 等 OPS-030 commit 后立即执行。回执请落 `REPORT-20260509-031-QA-to-PM.md`。
