---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260511-010
sender: PM
recipient: OPS
priority: P1
thread_key: codeflow-p4-day2-snapshot-commit
references:
  - REPORT-20260511-007-DEV-to-PM
  - TASK-20260511-009-PM-to-DEV
  - REPORT-20260511-008-OPS-to-PM
  - TASK-20260511-008-PM-to-OPS
layer: worker
---

# TASK-20260511-010：P4 主 sprint Day 2 snapshot commit（不 tag）

## 一、TL;DR

DEV-009 Day 2 ~80min 完工（9-10x SLA 加速），TaskParser/fcop-client/Runtime/shell main 共 **6 modified / 0 new**，全在 worktree 未 commit。本 TASK 为 Day 2 snapshot，与 OPS-008 同模板（不 tag，origin/backup 同步，gitee G3 隔离）。

## 二、严格 stage 清单（6 项 — 多一项不要，少一项不要）

OPS 必须**只**stage 以下 6 项：

```
M codeflow-shell/src/main.ts
M packages/codeflow-runtime/src/Runtime.ts
M packages/codeflow-runtime/src/_external/__tests__/fcop-client.test.ts
M packages/codeflow-runtime/src/_external/fcop-client.ts
M packages/codeflow-runtime/src/scheduler/TaskParser.ts
M packages/codeflow-runtime/src/scheduler/__tests__/TaskParser.test.ts
```

**不要** stage：
- ❌ `docs/agents/tasks/*` 任何 TASK/REPORT 文件（含本 TASK 自身）
- ❌ `.smoke-*` / `private/` / `node_modules/`
- ❌ codeflow-runtime `package.json` / `package-lock.json`（Day 2 无新依赖）

## 三、commit message（HEREDOC 严格）

```
feat(p4-day2): TaskParser routes through fcop bridge (snapshot, no tag)

- Rework FcopTask interface to match fcop@1.1.0 nested shape
  + frontmatter: FcopTaskFrontmatter (was: flat 11 fields, buggy)
  + Add convenience accessors: sender/recipient/priority/subject/
    thread_key/risk_level/references (pre-pulled from frontmatter)
  + Add missing fields: body/references/risk_level/is_archived
- Add public FcopProjectClient.readTask(filenameOrId): Promise<FcopTask>
  positional argument (NOT kwargs); TS-FCC-10 pins contract
- Add helpers readTaskFrontmatter/readPlainDict/coerceDictValue
  for fcop dict[str, object] proxy round-trip (used for extra.layer)
- TaskParser: new instance API (constructor(opts.fcopClient?))
  + fcop-first parse with FcopClientError -> yaml fallback
  + static API unchanged (4 legacy yaml tests still green)
- Runtime: add optional RuntimeCreateOptions.fcopClient injection
  + scheduler layer wires parserOverride when fcopClient is given
- codeflow-shell main: construct FcopProjectClient when probe is ok
  (workspaceDir="docs/agents", ensureInitialized=false)
  + banner adds "Task parser:" line (fcop|yaml fallback)

runtime tests: 121 -> 126 (+5: TS-FCC-10 + TS-TP-D2-1..4)
tsc: 3 workspaces clean
smoke: real-fcop + yaml-fallback both PASS

Day 2 also fixes Day 1 latent bugs:
- FcopTask schema flat -> nested (test stubs were self-consistent
  but did not match real fcop)
- Day 1 missing body/references/risk_level fields

This is P4 Day 2 of 6 (TASK-20260511-009). v0.3.0-alpha
to be tagged at Day 6 EOD (target 2026-05-17).

Refs: REPORT-20260511-007-DEV-to-PM (Day 2 section)
```

## 四、Safety HARD GATE（精确正则版，OPS-008 改进采纳）

| 检查 | 命令 / 期望 |
|---|---|
| 4.1 stage 前精确 secret scan | `git diff` 中匹配 `crsr_[0-9a-f]{16,}` / `ck_[0-9a-f]{16,}` / `sk-[A-Za-z0-9]{20,}` 全 0 hit |
| 4.2 stage 后精确 secret scan | 同上，针对 cached |
| 4.3 `.env*` 进 staged | `git diff --cached --name-only | Select-String '(^|/)\.env$|\.env\.\w+$'` 应 0 hit（**注意 `.env.example` 本次 Day 2 没改，不应出现**）|
| 4.4 `.smoke-*` 进 staged | 0 hit |
| 4.5 `node_modules/` 进 staged | 0 hit |
| 4.6 `package.json` / `package-lock.json` 进 staged | **0 hit**（Day 2 无新依赖，与 Day 1 不同）|
| 4.7 staged file count | = **6** |

**为何 4.6 强制 0**：DEV-009 §二 diff stat 显示 Day 2 没动 `package.json` / `package-lock.json`，pythonia@^1.2.6 已在 Day 1 `f559904` 落地。若本次 stage 出现这两个文件 = OPS 错装一遍 npm install → 必须 revert。

## 五、推送策略

```
git commit -m "$(cat <<'EOF'
[见 §三]
EOF
)"

git push origin main
git push backup main

# NO tag (PM 明令；v0.3.0-alpha Day 6 才出厂)
# gitee 不动（G3 隔离继续）
```

## 六、验证

```
# §6.1 commit hash + stat
git log -1 --stat

# §6.2 origin/backup MATCH
git rev-parse HEAD
git rev-parse origin/main
git rev-parse backup/main
# 三 hash 全 match

# §6.3 gitee G3 隔离
git ls-remote gitee main
# 仍为 62532a7d32779bbd0ec09c7e0fbcb6cc6541b4fe

# §6.4 runtime tests 126/126 baseline
cd packages/codeflow-runtime
npm test 2>&1 | Select-String "tests|pass|fail"
# 应显示 tests 126 / pass 126 / fail 0

# §6.5 三 workspace tsc clean
cd packages/codeflow-runtime; npx tsc --noEmit
cd codeflow-shell; npx tsc --noEmit
cd packages/codeflow-protocol; npx tsc --noEmit
# 三个 exit 0
```

## 七、不在范围

- ❌ 不打 tag
- ❌ 不推 gitee
- ❌ 不 commit docs（TASK/REPORT 文件等下次 docs commit 批量）
- ❌ 不动 PWA / relay-bridge / install.ps1
- ❌ 不动 codeflow-shell `package.json` 版本号
- ❌ 不修复 BUG-SDK-005/006

## 八、SLA

**预期 5-10min**（OPS-008 3min 完工记录在前；Day 2 6 文件 vs Day 1 7 文件，差不多）。

## 九、回执要求

写 `REPORT-20260511-010-OPS-to-PM.md`，沿用 OPS-008 §一~§十 模板：
- §一 一句话结论（commit hash + 6 文件验证）
- §二 §三 commit message 实际值
- §三 Safety HARD GATE 7 项**逐项**实际输出
- §四 origin/backup MATCH 三 hash
- §五 gitee G3 验证
- §六 runtime tests 126/126 复核 + 三 workspace tsc
- §七 tag 策略（`v0.3*` 仍 0 创建）
- §八 surprise / hygiene 观察
- §九 post-commit `git status`
- §十 后续 Day 3/Day 6 引导

## 十、并行关系

- DEV 在 OPS-010 期间 idle（DEV-009 §五已自报 Day 3 启动方向，但**不抢跑** — OPS commit 落 origin 前 DEV 不动 worktree）
- QA 仍 idle（v0.2.0-beta.3 sign-off 完成，Day 6 才验 v0.3.0-alpha）
- PM 与本 TASK 并行干 `docs/internal/emergence-log.md`（PM 第 7-12 次自披露 + Charter 5 + 自约束 6/7/8/9/10/9 path 版二次扩展）
- ADMIN 仍是 push 总线（需通知 OPS 一次）

## 十一、本 TASK 内含的 PM 第 11 + 12 次错误自披露备录

| # | 错误（DEV-009 §三 D2-S2 揭示）| 处置 |
|---|---|---|
| 11 | PM TASK-009 §四「`client.readTask(filepath)` Day 2 主用接口」假设 method 存在 — 实际 Day 1 ship 没暴露 | DEV 自决加 public method + TS-FCC-10 钉契约 |
| 12 | PM 参数名 `filepath`（路径）— 实际 fcop API `read_task(filename_or_id)` 是字符串 | DEV `basename(filepath)` 转换 |

第 9 条自约束 path 版**二次扩展**：核对 path / method 暴露 / 参数类型三件套。已入 emergence-log.md 草稿。

OPS 不必修任何 PM 落地文档（Charter 5）。

---

PM-01
2026-05-11 13:55 (UTC+8)
