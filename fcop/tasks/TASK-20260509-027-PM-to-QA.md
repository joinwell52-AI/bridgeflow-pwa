---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-027
sender: PM
recipient: QA
priority: P1
thread_key: codeflow-v2-sprint-s5-phase-e-regression-and-release-recommendation
references:
  - REPORT-20260509-025-QA-to-PM
  - REPORT-20260509-024-DEV-to-PM
  - TASK-20260509-026-PM-to-OPS
  - packages/codeflow-runtime/docs/test-strategy-s3.md
layer: governance
---

# Sprint S5 Phase E 回归 + Phase D 回归 + v0.1-alpha 发布推荐 + S6 启动推荐

## 背景

DEV-024 47 分钟内全交付 S5 — 94/94 全过 + 30x 0 flaky + Phase D race-loop bug 顺手修复（详见 [`REPORT-024-DEV-to-PM.md`](./REPORT-20260509-024-DEV-to-PM.md) §三附加决策）。
PM 独立复核 100% 一致。

按第 5 句宪法 PM 自约束 + QA-025 §五给的预设条件「**S5 完成 + OPS S5 commit 后，QA 给出正式发布推荐**」，本任务派 QA 跑 v0.1 发布前最后一轮验证。

## 工作内容（4 件）

### 工作 1：Phase D 回归 71/71（启动条件 = OPS-026 commit 落地后）

按 [`REPORT-025-QA-to-PM.md`](./REPORT-20260509-025-QA-to-PM.md) §一 §1.2 工作 4 已答应：写入 `test-strategy-s3.md §8 Phase D 回归测试结果`。

**注意**：DEV-024 在 S5 commit 里包含 `ReviewEngine.whenSettled` race-loop 修复。Phase D 的 13 个测试**之前**在 OPS-023 commit (1ba2aa6) 上 70/71 间歇失败，现在在 OPS-026 commit baseline 上应该**稳定 71/71**。QA 跑 30x 验证 0 flaky。

```powershell
cd D:\Bridgeflow\packages\codeflow-runtime
npm test                    # 期望：tests 94 / pass 94 / fail 0
# 30x 回归（DEV REPORT-024 §二的 0 flaky 数据点 PM 已独立验过 1x）
1..30 | ForEach-Object { npm test --silent 2>&1 | Select-String "fail \d" } | Group-Object | Format-Table
# 期望：fail 0 出现 30 次
```

### 工作 2：Phase E 回归 94/94（含 17 TS-7.x）

按 §3.7 17 个场景（13 派单 + 4 bonus）逐项核对实测 stdout，写入 `test-strategy-s3.md §9 Phase E 回归测试结果`。

```
✔ TS-7.1: load N valid skills → loaded.length === N, skipped=[]
✔ TS-7.2: schema-invalid skill file → skipped, others still load
✔ TS-7.3: tolerant-read filters skip .tmp / non-.json / invalid JSON
✔ TS-7.4: getById / listForRole / list indexes consistent
✔ TS-7.5: agent with fcop@>=1.0 skill → null (no failure)
✔ TS-7.6: agent without fcop skill → reason="no_fcop_skill"
✔ TS-7.7: agent references missing skill_id → "skill_not_found"
✔ TS-7.8: skill not local-compatible → "no_compatible_runtime"
✔ TS-7.9: MCPInjector stub mode → no spawn, just log
✔ TS-7.10: MCPInjector live mode → ctor eager throw
✔ TS-7.11: RuntimeBootstrap missing fcop agent → report.kernel_failures populated
✔ TS-7.12: AgentRegistry.register pre-hook rejects → KernelDependencyError + sdk.calls.create.length === 0
✔ TS-7.13 (bonus): agent.skills=[] → "no_fcop_skill" (fast path)
+ 4 bonus: re-load idempotent / missing dir / filename mismatch / validateAll aggregate
```

### 工作 3：v0.1-alpha 发布前置检查

按 [`REPORT-025-QA-to-PM.md`](./REPORT-20260509-025-QA-to-PM.md) §五给的发布前置约束：

| 约束 | QA 验证 |
|---|---|
| **fcop 强依赖闸不可绕过** | `KernelDependencyValidator` 在 `register` + `bootstrap` 双路径都拦（TS-7.11/7.12）✅ DEV 决策 S 锁定 schema → kernel → SDK 三道闸顺序 |
| **协作宇宙不会因为试用者注册无 fcop agent 而崩** | TS-7.6 + TS-7.11 + TS-7.12 全过 → 试用者**任何路径**注册无 fcop agent 都被拒 ✅ |
| **Phase A/B/C/D 全部无回归** | 工作 1 + 工作 2 跑完后给定论 |
| **30x 0 flaky** | DEV REPORT-024 §二已自报 + PM 独立 1x；QA 在工作 1 内做 30x 二次复核 |

按 v0.1 §0.8 6 条硬约束逐项核：

| # | 硬约束 | v0.1-alpha 现状 |
|---|---|---|
| 1 | 零 UI | ✅ npm/Node lib 形态 + cli stdout |
| 2 | 状态全文件化 | ✅ agents.json + sessions/*.json + reviews/*.md + transcripts/*.md + state_history append-only |
| 3 | 进程能恢复 | ✅ RuntimeBootstrap reconcile loop（TS-2.1~2.8）|
| 4 | 每步有 reviewer | ✅ ReviewEngine governance loop（TS-6.6~6.13）|
| 5 | 全本地 | ✅ no cloud agent；MCPInjector live=eager throw |
| 6 | fcop-mcp 强依赖 | ✅ S5 Phase E 闭环（KernelDependencyValidator + skill.schema.json `required_kernel.contains: "^fcop@.+"`）|

### 工作 4：S6 + v0.1-alpha 双推荐

写入回执 §推荐节，明确：

| 议题 | QA 推荐 |
|---|---|
| S6 codeflow-shell 立刻启动 | ✅ / ❌ + 主要依据（参考 QA-025 §四已经预表态："等工作 4 完成给定论"） |
| v0.1-alpha 是否可发布给 ADMIN 试用 | ✅ / ❌ + 前置条件（如还需 S6 EXE 包装才发，明说）|
| **任何 v0.1 阻塞项**（如有）| 列出 + 严重性 |

### 工作 5：自己 commit 自己的 docs 范围

DEV REPORT-024 + PM TASK-026 已经把 QA 范围排除在 OPS-026 commit 外。**QA 在工作 1+2 完成后**，把以下文件作为单独 commit 落地：

```
M  packages/codeflow-runtime/docs/test-strategy-s3.md   (TS-5.12/5.13 修正 + §3.6/3.7/§5d/§8/§9 全部内容)
?? docs/agents/tasks/REPORT-20260509-025-QA-to-PM.md
?? docs/agents/tasks/REPORT-20260509-027-QA-to-PM.md   (本任务回执)
```

### 5.1 选项 A（推荐）：QA 写完 §8/§9 后请 PM 派 OPS 单独 commit

QA 完成工作 1+2 + 把内容写入 §8/§9 后**不自己 commit**，写 REPORT-027-QA-to-PM 通报 PM。PM 派 TASK-028-PM-to-OPS 让 OPS 跑 docs commit（保持 OPS 是唯一 commit 角色，同前 6 轮）。

### 5.2 选项 B（备选）：QA 自己 commit

如 QA 偏好自己 commit（同 DEV/OPS 模式），在回执末尾明示「申请 self-commit 权」，PM 会在审核 REPORT-027 时回应。

**默认采用选项 A**——保持流程一致性。

## 不做（out-of-scope）

- ❌ 不写自动化测试代码（DEV 写）
- ❌ 不修改 `@codeflow/protocol` schema
- ❌ 不修改任何 docs/agents/tasks/ 已有 task 文件
- ❌ 不修改任何 src/ 下的代码
- ❌ 不动 §0.0 宪法 5 句
- ❌ 不实施 v0.1-alpha 发布动作（npm publish / GitHub Release 等 — 这是 PM 仍请示 ADMIN 的"重大变更"项）

## 验收标准（6 项）

| # | 项 | 验证方式 |
|---|---|---|
| 1 | npm test 在 OPS-026 baseline 跑过 + 94/94 PASS | stdout 截图入回执 |
| 2 | 30x 回归 0 flaky | stdout 截图（30x ForEach 输出）|
| 3 | §8 Phase D 回归 71/71 + §9 Phase E 回归 94/94 写入 test-strategy-s3.md | 文档查 |
| 4 | v0.1-alpha 发布推荐明确 ✅/❌ + 主要依据 | 回执明文 |
| 5 | S6 启动推荐明确 | 回执明文 |
| 6 | v0.1 §0.8 6 条硬约束逐项 ✅/❌ 状态确认 | 回执表格 |

## 时间预算

- 预计 **1.5-2 小时**（30x 回归 + 写 §8/§9 + 写推荐）
- 30x 回归约 30 min（每次 6.3s × 30 + 启动开销）

## 启动条件

工作 1+2+3+4 全部等 OPS-026 commit 落地后立即开始。
工作 5 由 PM 在收到 REPORT-027 后派 OPS-028（默认选项 A）。

## 完成后回执要求

按 `qa-bridge` 规则写 `REPORT-20260509-027-QA-to-PM.md`，必须包含：

1. 工作 1+2 实测 stdout（≥ 94/94 + 30x 0 flaky）
2. 工作 3 v0.1 §0.8 6 条硬约束逐项确认
3. 工作 4 双推荐（S6 + v0.1-alpha）+ 主要依据
4. 任何阻塞项（如发现）+ 严重性
5. 选项 A 或 B 的偏好（如选 B 明示 self-commit 申请）

---

PM-01 派单。QA-01 等 OPS-026 commit 后立即执行。回执请落 `REPORT-20260509-027-QA-to-PM.md`。
