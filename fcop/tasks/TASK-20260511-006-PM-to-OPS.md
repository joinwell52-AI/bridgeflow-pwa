---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260511-006
sender: PM
recipient: OPS
priority: P2
thread_key: codeflow-p4-spike-commit-baseline
references:
  - REPORT-20260511-005-DEV-to-PM
  - TASK-20260511-005-PM-to-DEV
  - REPORT-20260511-003-OPS-to-PM
layer: worker
---

# TASK-20260511-006：DEV-005 P4 spike + schema 映射 + tsconfig exclude commit

## 一、TL;DR

DEV-005 P4 调研 spike 完工（pythonia + fcop@1.1.0 viable，5 调用 1-7ms 延迟，12 surprise 揭示）。按 DEV §后置追加建议**前者**（commit spike 入仓作 P4 baseline），将以下 11 项一次 commit + push origin/backup main。

**不打 tag**（spike 不是发布物）。**不动 gitee**（G3 隔离）。

## 二、必交付清单

### 2.1 单 commit — P4 spike baseline（11 项）

stage 范围**严格 11 项**：

```text
1 modified:
  M  packages/codeflow-runtime/tsconfig.json   ← +1 行 "exclude": ["src/_spike/**/*"]

8 new (spike 隔离目录):
  A  packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/.gitignore
  A  packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/README.md
  A  packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/package.json
  A  packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/tsconfig.json
  A  packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/hello-fcop.ts
  A  packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/demo-fcop-api.ts
  A  packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/probe-surprises.ts
  A  packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/inspect-fcop-schemas.py

2 new (docs):
  A  docs/internal/p4-schema-mapping-v1.1.md
  A  docs/agents/tasks/REPORT-20260511-005-DEV-to-PM.md
```

commit message 模板（PM 推荐措辞，OPS 可微调）：

```text
chore(p4-spike): add pythonia + fcop@1.1.0 spike baseline for P4 sprint

DEV-005 (TASK-20260511-005) — pythonia + fcop integration spike for
D7=P decision (ADMIN approved 2026-05-11 10:44).

All four deliverables PASS on Windows + Node v24 + Python 3.12.9:
  1) hello-fcop: fcop 1.1.0 import succeeds, cold start 376 ms
  2) demo-fcop-api: 5 core fcop@1.1.0 calls @ 1-7 ms / call
     (Project / write_task / list_tasks / write_review /
      mark_human_approved) — well under 50 ms PM threshold
  3) schema-mapping-v1.1.md: 174-line CodeFlow v0.1 5-schemas vs
     fcop@1.1.0 8-schemas field-by-field map
  4) probe-surprises: 5 probes including Windows paths with spaces,
     parallel write_task, fcop_mcp auto-import check, workspace_dir
     escape hatch, GIL serialization

Twelve surprises documented in REPORT-20260511-005-DEV-to-PM.md,
including 5 corrections to PM TASK-005 §3.2 fcop API code examples
(PM error self-disclosure #6, pythonia kwarg syntax: `Project$(path,
{strict: false})` not `Project(path, {strict$: false})`).

Spike isolated under packages/codeflow-runtime/src/_spike/. The single
modification to the main code tree is one line in
packages/codeflow-runtime/tsconfig.json adding `"exclude":
["src/_spike/**/*"]` so the spike's experimental TS does not pollute
runtime tsc --noEmit. Runtime 112/112 tests still pass.

P4 main sprint (TASK-006-PM-to-DEV) will start 2026-05-13 with DEV's
revised 7-8 working-day estimate (vs PM's earlier 4-5d).

Refs: TASK-20260511-005, REPORT-20260511-005-DEV-to-PM,
      REPORT-20260511-001-PM-to-ADMIN (Charter 5 + D7=P).
```

### 2.2 验证 + 推送

```text
# 守卫
git status --short
git diff | Select-String -Pattern "crsr_[0-9a-f]{16,}" -CaseSensitive:$false

# 期望 status 与 PM §2.1 完全对齐（1 modified + 10 untracked，REPORT-005 + TASK-006 自身另算）

# stage
git add packages/codeflow-runtime/tsconfig.json
git add packages/codeflow-runtime/src/_spike/fcop-pythonia-spike/
git add docs/internal/p4-schema-mapping-v1.1.md
git add docs/agents/tasks/REPORT-20260511-005-DEV-to-PM.md

# 自检（cached 应为 11 项）
git diff --cached --name-only

# commit
git commit -m "$(cat <<'EOF'
chore(p4-spike): add pythonia + fcop@1.1.0 spike baseline for P4 sprint

[full body above]
EOF
)"

# 推送
git push origin main
git push backup main
```

### 2.3 Safety HARD GATE

| 检查 | 期望 |
|---|---|
| stage 前 `git diff` 含 `crsr_[0-9a-f]{16,}` | 0 hit |
| stage 后 `git diff --cached` 含同 pattern | 0 hit |
| `.env*` 进 staged | 0 hit |
| `.smoke-*` 进 staged | 0 hit |
| `node_modules/` 进 staged | 0 hit（spike `.gitignore` 已覆盖）|
| **staged file count** | 严格 **11** |
| **不打 tag**（spike 不是发布物）| ✅ |
| **不动 gitee** | ✅ G3 保持 |

## 三、不在范围

- ❌ 不动 v0.2.0-beta.3 主代码 / runtime 112 tests / tag
- ❌ 不打新 tag
- ❌ 不动 gitee
- ❌ 不动 codeflow-pwa Dependabot
- ❌ 不动 `.smoke-qa014-20260511-080830/`（仍 locked，已 gitignore 覆盖）

## 四、SLA

| 阶段 | 预期 |
|---|---|
| Safety check + stage | 3 min |
| commit | 1 min |
| push origin main | 2 min |
| push backup main | 2 min |
| 验证（git log / show / ls-remote）| 3 min |
| REPORT 撰写 | 10 min |
| **合计** | **~20 min** |

可与 QA-004 并行（无依赖）。

## 五、回执要求

`REPORT-20260511-006-OPS-to-PM.md`，必含：

- 一句话结论
- §一 `git log -1 --stat`（11 文件验证）
- §二 Safety HARD GATE 8 项核验
- §三 推送验证（origin/backup match local）
- §四 远端 tag 核验（应**无** v0.2.0-beta.3 在 remote tags / 应**无**新 tag）
- §五 gitee 仍 G3
- §六 post-commit `git status --short`（应只剩 TASK-004 + TASK-006 + REPORT-006 自身 ± REPORT-004 if QA 已写）
- §七 任何 surprise

## 六、Charter / 自约束遵守证明

- **Charter 5**：纯仓库操作不动协议层
- **自约束 5**：常规推荐 / DEV 完工后 OPS 自动 commit 是流水线动作 / PM 自决
- **自约束 6/7**：N/A

---

PM-01
2026-05-11 11:30 (UTC+8)
