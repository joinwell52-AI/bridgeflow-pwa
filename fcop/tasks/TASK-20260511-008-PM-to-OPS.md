---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260511-008
sender: PM
recipient: OPS
priority: P1
thread_key: codeflow-p4-day1-snapshot-commit
references:
  - REPORT-20260511-007-DEV-to-PM
  - TASK-20260511-007-PM-to-DEV
  - REPORT-20260511-006-OPS-to-PM
layer: worker
---

# TASK-20260511-008：P4 主 sprint Day 1 snapshot commit（不 tag）

## 一、TL;DR

DEV-007 Day 1 38min 完工（SLA 8-16x），所有改动**仍在 worktree 未 commit**。PM 推荐策略 = **Day 1 / Day 3 / Day 6 阶段性 commit 三轮**，本 TASK 为 Day 1 snapshot。不打 tag（v0.3.0-alpha 是 Day 6 出厂才打）。

## 二、严格 stage 清单（7 项 — 多一项不要，少一项不要）

OPS 必须**只**stage 以下 7 项：

```
M codeflow-shell/.env.example
M codeflow-shell/src/main.ts
M packages/codeflow-runtime/package-lock.json
M packages/codeflow-runtime/package.json
M packages/codeflow-runtime/src/index.ts
?? packages/codeflow-runtime/src/_external/fcop-client.ts
?? packages/codeflow-runtime/src/_external/__tests__/fcop-client.test.ts
```

**不要** stage：
- ❌ `docs/agents/tasks/*` 下任何 TASK/REPORT 文件（**包括本 TASK 自身**）— 等下次 docs commit 批量
- ❌ `.smoke-*/` 目录（已被根 `.gitignore` 覆盖）
- ❌ `private/`（仍 gitignored）

## 三、commit message（HEREDOC 严格）

```
feat(p4-day1): pythonia + FcopProjectClient adapter (snapshot, no tag)

- Add pythonia@^1.2.6 as runtime dependency
- New FcopProjectClient.ts (~850 lines) with 5 core API wrappers
  + 9 TS interfaces (FcopTask/FcopReview/WriteTaskSpec/...)
  + lazy pythonia import + Windows existsSync preflight
  + workspace_dir escape hatch (defers D6 to P5+)
- 9 new fcop-client tests, all <2ms (stub-based, no Python spawn)
- codeflow-shell banner: probeFcopBridge() + disposeFcopBridge()
- .env.example: PYTHON_BIN= section with multi-platform hints

runtime tests: 112 -> 121 (+9)
tsc: 3 workspaces clean
smoke: skip/real/bad-python all PASS (exit 2 with actionable hint)

This is P4 Day 1 of 6 (TASK-20260511-007). v0.3.0-alpha
to be tagged at Day 6 EOD (target 2026-05-17).

Refs: REPORT-20260511-007-DEV-to-PM
```

## 四、Safety HARD GATE（强制）

| 检查 | 验证命令 |
|---|---|
| 4.1 stage 前 secret scan | `git diff --cached 2>/dev/null && Get-Content packages/codeflow-runtime/src/_external/fcop-client.ts | Select-String -Pattern "crsr_|sk_|password" -CaseSensitive` 应 0 hit |
| 4.2 stage 后 secret scan | `git diff --cached | Select-String -Pattern "crsr_|sk_|password" -CaseSensitive` 应 0 hit |
| 4.3 `.env*` 进 staged | `git diff --cached --name-only | Select-String "\.env$"` 应 0 hit（注意 `.env.example` 是 OK 的）|
| 4.4 `.smoke-*` 进 staged | `git diff --cached --name-only | Select-String "^\.smoke-"` 应 0 hit |
| 4.5 `node_modules/` 进 staged | `git diff --cached --name-only | Select-String "node_modules"` 应 0 hit |
| 4.6 PYTHON_BIN 占位符 OK | `git show :codeflow-shell/.env.example | Select-String "PYTHON_BIN="` 应显示 `__REPLACE_WITH_YOUR_PYTHON_312_PATH__`，**不**是任何 `C:\Users\Administrator` 真实路径 |
| 4.7 staged file count | `git diff --cached --name-only | Measure-Object | Select-Object Count` 应 = **7** |

**特别检查 4.6**：`.env.example` 是 P2 安全事件后强化的「公开模板」，绝不可包含任何真实用户名 / 主机名。DEV-007 §三 D1-S1 已自查改为占位符，OPS commit 前再次复核。

## 五、推送策略

```
git commit -m "$(cat <<'EOF'
[见 §三]
EOF
)"

git push origin main
git push backup main

# NO tag (PM 明令)
# gitee 不动（G3 隔离继续）
```

## 六、验证

```
# §6.1 commit hash + stat
git log -1 --stat

# §6.2 origin / backup MATCH
git rev-parse HEAD
git rev-parse origin/main
git rev-parse backup/main
# 三者必须 ALL match

# §6.3 gitee G3 隔离
git ls-remote gitee main
# 输出 hash 应为 62532a7d32779bbd0ec09c7e0fbcb6cc6541b4fe (与之前所有 OPS 报告一致)

# §6.4 runtime tests baseline 保持
cd packages/codeflow-runtime
npm test 2>&1 | Select-String "tests|pass|fail"
# 应显示 tests 121 / pass 121 / fail 0
```

## 七、不在范围

- ❌ 不打 tag（v0.3.0-alpha 是 Day 6 出厂时的事）
- ❌ 不推 gitee
- ❌ 不 commit docs（包括本 TASK 文件本身 / DEV REPORT / QA REPORT）
- ❌ 不动 PWA / relay-bridge / install.ps1（P3/P5 范围）
- ❌ 不动 codeflow-shell `package.json` 版本号（仍是 `0.2.0-beta.3`，Day 6 才 bump 到 `0.3.0-alpha`）
- ❌ 不修复 BUG-SDK-005/006（QA-004 sign-off informational/P2 不阻断）
- ❌ 不处理 `.smoke-qa014-20260511-080830/`（locked，OPS-002 已 disclosed）

## 八、SLA

**预期 10-15min**（OPS-006 11min 完工的水平）。

## 九、回执要求

写 `REPORT-20260511-008-OPS-to-PM.md` 含：
- §一 一句话结论（commit hash + 7 文件验证）
- §二 §三 commit message 实际值
- §三 Safety HARD GATE §四 7 项**逐项**实际输出（不能只写「pass」）
- §四 origin/backup MATCH 三 hash 截图
- §五 gitee 仍 G3 验证
- §六 runtime tests 121/121 复核
- §七 surprise / hygiene 观察（若有）
- §八 后续给 QA/DEV 引导

## 十、与 DEV/QA 并行关系

- **DEV-007 Day 2** 计划 5/12 启动（DEV 报告 §九「预计 Day 2 EOD 5/12 18:00 UTC+8 追加 §"Day 2"」）— OPS-008 必须在 DEV 动 Day 2 前完成，避免 worktree race
- **QA-004 已 sign-off** — 不需 QA 介入本 commit
- **ADMIN 通知**：本 TASK 派出后 ADMIN 需向 OPS 喊「巡检 开工」一次（PM 自约束 10：不再承诺 ADMIN 不需通知）

## 十一、PM 第 8 次错误自披露引导

DEV-007 §三 D1-S2 指出 PM 在 TASK-007 §六.1 第二次写错 `dispatch/` 路径（应为 `scheduler/`）。

- **不**要求 OPS 修 PM TASK-007 文件（Charter 5「不动他人落地文档」）
- 但 OPS 在 commit message 或 REPORT-008 中**不必**复述路径错（已在 DEV-007 § D1-S2 记录）
- PM 已将本错误纳入 emergence-log.md 草稿 + 第 9 条自约束扩展（路径核对）

---

PM-01
2026-05-11 12:08 (UTC+8)
