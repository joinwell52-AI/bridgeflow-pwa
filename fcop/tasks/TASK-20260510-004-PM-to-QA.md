---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260510-004
sender: PM
recipient: QA
priority: P1
thread_key: codeflow-v0.2-acceleration-test-fixtures-and-strategy
references:
  - REPORT-20260510-002-PM-to-ADMIN
  - TASK-20260510-002-PM-to-DEV
  - REPORT-20260509-031-QA-to-PM
layer: governance
---

# TASK-20260510-004：v0.2 加速节奏的测试 fixture + 策略预制

## §一 一句话指令

ADMIN 已下「加速」令；DEV-002 将在 5/11-5/22 出 4 个版本（v0.2.0-alpha → v0.2.0-beta → v0.2.0-rc.1 → v1.0-rc.1）。**QA 提前预制 fixture + 测试策略**，避免每个版本到了才临时拼。

## §二 交付（3 件，可并行）

### Item 1：写 `docs/test-strategy-v0.2-acceleration.md`（≤ 1.5h）

**章节**：

1. v0.2.0-alpha 验收清单（真 Cursor SDK 接入）
   - 不设 `CURSOR_API_KEY` → fake adapter ✅
   - 设 `CURSOR_API_KEY=invalid` → 期望报错
   - 设 `CURSOR_API_KEY=valid`（**等 ADMIN 给 key**）→ 真 SDK 评审，verdict 不再是 needs_human
   - config.json + .env + env vars 三种配置加载优先级测试
2. v0.2.0-beta 验收清单（EXE 打包）
   - 双击 EXE → banner ≤ 3s
   - drop sample-task → governance loop 完整
   - Ctrl+C 优雅退出 ≤ 2s
   - EXE 体积、首启时间 baseline
3. v0.2.0-rc.1 验收清单（relay-bridge）
   - relay 连接 / 重连
   - PWA wscat 模拟 → inbox 出 TASK
   - task_event / agent_status 推送到 PWA 端
   - 端到端：手机模拟（wscat）→ relay → v2 → 真 SDK → 评审 → 手机看到 reply_summary
4. v1.0-rc.1 验收清单（7 schemas + Boundary）
   - 7 schemas validate（含旧 5 schemas backward compat）
   - boundary.can / cannot 强制边界验证
   - NeedsHumanGate → BOUNDARY_VIOLATED 通道改写实证
5. 跨版本回归矩阵（v0.1.0-rc.1 → v0.2.0-* → v1.0-rc.1 三档兼容）

### Item 2：准备 `codeflow-shell/examples/` 测试 fixture（≤ 1h）

```
codeflow-shell/examples/
├── hello-world/
│   ├── README.md (已有)
│   └── sample-task.md (已有)
├── real-sdk-smoke/        ← 新增
│   ├── README.md
│   └── sample-task-with-cursor-sdk.md   # 触发真 SDK 评审的 fixture
├── relay-bridge-smoke/    ← 新增
│   ├── README.md
│   ├── wscat-payload-command.json       # PWA → relay 模拟载荷
│   └── expected-task-event.json         # v2 应该推回的 task_event
└── boundary-violation-smoke/  ← 新增（v1.0-rc.1 用）
    ├── README.md
    └── sample-task-violating-boundary.md
```

每个 fixture README 含：
- 触发条件
- 期望输出
- 失败诊断

### Item 3：v0.1.0-rc.1 ADMIN 试用质量监控（持续）

ADMIN 当前已可试用 v0.1.0-rc.1。如果 ADMIN 试用过程中遇到问题，**QA 直接接收并写 BUG 回执**：

```
docs/agents/tasks/REPORT-20260510-XXX-QA-to-PM.md（kind: report，sender: QA, priority: P1）
```

含：
- ADMIN 报告的现象
- QA 复现步骤
- 预计影响（v0.1.0-rc.1 / v0.2.0-* / 都受影响）
- 推荐处置（v0.2 sprint 修复 / 紧急 v0.1.0-rc.2 patch）

## §三 不动手清单

- ❌ **不**写测试代码（packages/codeflow-runtime/src/**/__tests__/）— 那是 DEV 工作
- ❌ **不**改 v0.1 schemas / runtime src
- ❌ **不**等到版本到了才开始 — 提前预制是这个 TASK 的核心

## §四 回执

```
docs/agents/tasks/REPORT-20260510-004-QA-to-PM.md
```

含：3 件交付物链接 + Item 1 文档摘要 + Item 2 fixture 清单。

## §五 SLA

预计 ≤ 3 小时（≤ 半个工作日）。完工后 QA 进入「待命模式」，等 DEV 每个版本完工 → 立即 verify。

PM-01
2026-05-10 01:00 (UTC+8)
