---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260511-011
sender: PM
recipient: DEV
priority: P0
thread_key: codeflow-p4-main-sprint-fcop-pythonia-integration
references:
  - TASK-20260511-007-PM-to-DEV
  - TASK-20260511-009-PM-to-DEV
  - REPORT-20260511-007-DEV-to-PM
  - REPORT-20260511-010-OPS-to-PM
layer: worker
---

# TASK-20260511-011：P4 主 sprint — Day 3 加速启动（ReviewWriter / NeedsHumanGate / InboxWatcher 改造）

## 一、TL;DR

ADMIN 5/11 14:21 拍 **「加速推进！！」** = P4 sprint 选 C 继续加速 = **Day 3 现在启动**（不等明天 5/12）。

Day 2 commit `bc9179a` 已落 + origin/backup MATCH + worktree clean + 126/126 tests。DEV 三连节奏（spike 30min → Day 1 38min → Day 2 80min）健康，**今天 EOD (18:00) 完成 Day 3 是合理预期**。

## 二、PM 自约束 9 path 版三件套（首次正式落地 — ripgrep 核对结果）

PM 在派 TASK-011 前已 ripgrep 核对所有真实路径 / public method / 参数类型：

| ripgrep 结果 | 真相 | TASK-007 §四 Day 3 PM 之前写过 |
|---|---|---|
| `packages/codeflow-runtime/src/review/ReviewEngine.ts` (class line 209) | ✅ 存在 | ✅ |
| `packages/codeflow-runtime/src/review/ReviewWriter.ts` (class line 150) | ✅ **独立类**，PM 之前误以为在 ReviewEngine 内 | ❌ |
| `ReviewWriter.write(verdict, body)` (line 170) | ✅ 双参，方法名是 `write` 不是 `writeReview` | ❌ |
| `packages/codeflow-runtime/src/review/NeedsHumanGate.ts` (class line 102) | ✅ 存在 | ✅ |
| `NeedsHumanGate.push(req): Promise<HumanApproval>` (line 131) | ✅ 已有 method（stub 模式）| 部分 ✅ |
| `FcopProjectClient.writeReview(spec)` (line 651) | ✅ Day 1 ship 已暴露 | ✅ |
| `FcopProjectClient.markHumanApproved(reviewId, spec)` (line 696) | ✅ Day 1 ship 已暴露 | ✅ |
| `FcopProjectClient.inspectTask()` | ❌ **Day 1 未暴露 public method** | 隐含 ❌ |
| `FcopProjectClient.readReview()` | ❌ **Day 1 未暴露 public method**（DEV-009 §五已预告 Day 3 加）| 隐含 ❌ |

**PM 第 13 + 14 次错误自披露**：
- #13：TASK-007 §四 Day 3.1「ReviewEngine.writeReview()」三处错（位置/method/参数）→ 实际改造点是 **ReviewWriter.write()**
- #14：隐含 `inspectTask()` / `readReview()` Day 1 已 ship — 实际未暴露，Day 3 需 DEV 加 public method

## 三、Day 3 真实范围（PM 修正版）

### 3.1 主交付（必做）

#### 3.1.1 `ReviewWriter.write()` 走 fcop（同 Day 2 TaskParser 路径 A 改良模式）

- 加 instance API: `new ReviewWriter({ fcopClient?, reviewsDir })`
- instance method `write(verdict, body)`: fcop-first，FcopClientError → yaml fallback
- static API 不动（v0.2.0-beta.3 已 closed BUG-SDK-001~007 回归保护）
- 调用前**先 ripgrep + inspect.signature 核对 fcop `Project.write_review$` 参数顺序 + 字段名**（PM 不虚构）
- DEV-009 §五已 prep：fcop.Review 是**完全 top-level 不嵌套**（与 fcop.Task 嵌套不同）→ Day 1 ship 的 `FcopReview` interface 可能要重写（同 Day 2 D2-S1 模式）

#### 3.1.2 `NeedsHumanGate.push()` 加 `markHumanApproved` 调用

- 保留 stub 模式（pushed_to/pushed_at 由 NeedsHumanGate 自写）
- 新增：当 `approved_by` / `approved_at` 真实回填时（**v0.3 路径**），调 `fcopClient.markHumanApproved(review_id, spec)` 落 audit
- v0.3 也允许 stub-only 模式（fcopClient = null 时 fallback）

#### 3.1.3 `ReviewEngine.extractText()` 不动

PM TASK-007 §四 Day 3.3 明令保留：4 probe 解析 SDK message（BUG-SDK-004 H4 修复回归基线）。

**不要在本 sprint 任何时刻动 `ReviewEngine.extractText()`**。

#### 3.1.4 `FcopProjectClient` 加 2 个 public method

- `readReview(filenameOrId): Promise<FcopReview>` — 同 Day 2 readTask 模式
- `inspectTask(filenameOrId): Promise<{valid, errors}>` — 给 InboxWatcher 用

DEV 在 fcop-client.ts 写之前**必须** `py -3 -c "import inspect, fcop; print(inspect.signature(fcop.Project.read_review))"` + `inspect.signature(fcop.Project.inspect_task)` 核对 — PM 不虚构。

### 3.2 次交付（DEV 自决推迟）

| 子任务 | PM 建议 | 备注 |
|---|---|---|
| **InboxWatcher 接 inspectTask 启动期 schema gating** | Day 3 内一起做，**但允许 DEV 推到 Day 4** | DEV-009 §五说「校验失败时是 reject 还是 needs_human review」需要先想清；若 DEV 认为 Day 3 时间紧 → 推到 Day 4（同时 NeedsHumanGate 改造）|
| **重写 `FcopReview` interface 走 top-level**（vs Day 1 嵌套） | Day 3 必做（若 inspect.signature 验证 fcop.Review 确实 top-level）| 同 Day 2 D2-S1 模式 — 不修则 writeReview 跑真实 fcop 时返回值平铺 |

### 3.3 不动（明令）

- ❌ 不动 `AgentSdkAdapter.ts` / `SessionManager` / `RuntimeBootstrap` / `atomic-write.ts`
- ❌ 不动 `ReviewEngine.extractText()`（BUG-SDK-004 回归基线）
- ❌ 不动 `TaskParser.ts`（Day 2 已 ship；本 sprint 不再动）
- ❌ 不动 `TaskDispatcher.ts`（仍保 0 修改纪录）
- ❌ 不动 Runtime.ts 高层接口（同 Day 2 — 加 optional 字段 OK，重构接口不 OK）
- ❌ 不引入 archive 概念（Day 5）
- ❌ 不删 v0.1 5 schemas（TS interface）— 推到 Day 5
- ❌ 不动 PWA / relay-bridge / install.ps1（P3 / P5 范围）
- ❌ 不 commit（PM 决定 Day 3 EOD 后派 OPS）
- ❌ 不 bump 版本号（仍 `0.2.0-beta.3`）

## 四、与 Day 2 集成方式

DEV-009 Day 2 ship 的 `RuntimeCreateOptions.fcopClient?` 字段 + scheduler layer 注入模式：

- Day 3 同样在 `Runtime.create()` 内：若 `fcopClient` 已传 → 构造 `new ReviewWriter({ fcopClient, reviewsDir })` 注入 ReviewEngine
- codeflow-shell main.ts 已 ship 构造 FcopProjectClient（Day 2.4）— Day 3 不需要再改 shell main.ts

## 五、SLA

PM TASK-007 §四 Day 3-4 给的是 2.5 工作日（Day 3 = 1.5d）= 12-14h SLA。

DEV 历史节奏：
- spike 30min, Day 1 38min, Day 2 80min — 算术平均 50min, 中位数 38min
- **乐观**：90-120min（多 1 个 inspect.signature 核对 + 1 个新 method）
- **保守**：3-4h
- **不超**：5h

**预期完工时间**：今天 (5/11) 16:00-18:00 之间。

若 DEV 卡住 ≥ 90min → in-progress REPORT，PM 决定路径调整。

## 六、回执要求

DEV 写 **同一文件 `REPORT-20260511-007-DEV-to-PM.md` 追加 `## Day 3 (5/11 EOD)` 段**（按 TASK-007 §八约定）。

最低必含：
- §一 一句话结论（路径选择 + ship-ready 与否 + 关键改造点）
- §二 完工证据（diff stat + 126→新 tests 数 + tsc clean × 3 + smoke 输出）
- §三 surprise 揭示（Day 3 至少 1 段）— **特别关注 fcop.Review top-level vs FcopReview Day 1 interface 是否需要重写**
- §四 与 PM §3.2 次交付的对照（InboxWatcher 推迟 Day 4 OR Day 3 完成）
- §五 给 PM 的 Day 4-5 启动方向预告（AgentRegistry + schema 清理）
- §六 self-justification（修改的文件 / 影响范围 / 是否需要重启）
- §七 SLA 兑现（与 spike/Day 1/Day 2 对照）
- §八 自决审计
- §九 Day 6 release notes 累积清单（Day 3 新增项）

## 七、与其他 lane 的关系

| lane | 状态 | 备注 |
|---|---|---|
| OPS | idle（Day 3 EOD 后派 OPS-012 commit）| 不抢跑 |
| QA | sign-off 完成 | Day 6 才验 v0.3.0-alpha |
| PM | 与 DEV 并行干 `docs/internal/emergence-log.md`（13+14 次自披露入档）| 不阻塞 |
| ADMIN | 接 PM 通报，仍是 push 总线 | Day 3 需通知 DEV 一次 |

## 八、Charter / 自约束遵守证明

| 项 | 立场 |
|---|---|
| Charter 5「CodeFlow = FCoP 应用」 | ✅ Day 3 是 Charter 5 第三步落地 — 删 CodeFlow 自有 review write 代码 |
| 自约束 7「不主动设计协议需求」 | ✅ `inspect_task$` schema gating「校验失败时 reject 还是 needs_human」DEV 内部消化，不向 fcop 报 |
| 自约束 9 path 版三件套 | ✅ **首次正式落地** — 本 TASK §二已 ripgrep 核对 9 项（含 PM 第 13/14 次自披露）|
| 自约束 10「不承诺 ADMIN 不需通知」 | ✅ §七明示「Day 3 需通知 DEV 一次」 |

## 九、PM 自决审计

| 决策 | 性质 | 处置 |
|---|---|---|
| 接 ADMIN「加速推进」指令 = 选 C | ADMIN 明示战略 | ✅ 执行 |
| Day 3 今天启动（不等 5/12）| 工程节奏 | ✅ 自决（DEV 三连节奏健康 + DEV-009 §五主动请求 + ADMIN 加速指令 = 三符合）|
| 主动暴露 PM 第 13/14 次错误自披露 | Charter 5 + 自约束 9 path 版 | ✅ 自决（透明优于掩饰，已成 PM 习惯）|
| InboxWatcher gating 允许 DEV 自决推 Day 4 | DEV 自由度 + DEV-009 §五指出「校验失败处理」需先想清 | ✅ 自决 |
| FcopReview interface 可能重写允许 Day 3 内做 | DEV-009 §五已预 inspect.signature 验证 | ✅ 自决 |
| 仍不 commit Day 3 | 累积到 Day 3 EOD 后派 OPS-012 | ✅ 自决 |

## 十、本 TASK 暗含的下一波派单（PM 内部备录，不强制 DEV 知道）

- **Day 3 EOD 后**：派 OPS-012 (Day 3 snapshot commit，同 Day 1+2 模板)
- **Day 4 开始**：AgentRegistry 改造 + InboxWatcher gating（如 DEV 推迟）
- **Day 5**：schema 清理（v0.1 5 TS interfaces）
- **Day 6**：全量回归 + smoke + release notes + 版本 bump 到 0.3.0-alpha + 派 OPS 打 tag

若 DEV 三连节奏保持 → **v0.3.0-alpha 5/12-5/13 EOD 出厂**（提前 4-5 天），但 PM **不承诺**（自约束 10）。

---

PM-01
2026-05-11 14:25 (UTC+8)
