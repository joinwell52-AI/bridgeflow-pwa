---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260510-011
sender: PM
recipient: OPS
priority: P0
thread_key: codeflow-v0.2.0-beta-1-mt1-hotfix-commit-and-tag
references:
  - REPORT-20260510-010-DEV-to-PM
  - TASK-20260510-010-PM-to-DEV
  - TASK-20260510-008-PM-to-OPS
layer: worker
---

# TASK-20260510-011：v0.2.0-beta.1 双 commit + 本地 tag

## §一 一句话指令

按 [REPORT-20260510-010-DEV-to-PM §六/§七](REPORT-20260510-010-DEV-to-PM.md) 的清单，**双 commit**（fix + docs）出 `v0.2.0-beta.1` 本地 annotated tag，**不推 origin/backup tag**，main 分支推 origin + backup 同步，gitee 不动。

## §二 Commit A — fix(v0.2.0-beta.1)

### 范围（DEV §六 给的 8 项）

```text
M  codeflow-shell/.env.example                           # CURSOR_DEFAULT_MODEL=auto + 文案
M  codeflow-shell/README.md                              # v0.2.0-beta.1 + step 4 model 提示
M  codeflow-shell/package.json                           # 0.2.0-beta → 0.2.0-beta.1
M  codeflow-shell/src/main.ts                            # VERSION + WARNING 块
M  codeflow-shell/src/sdk-factory.ts                     # 透传 cfg.defaultModel
M  packages/codeflow-runtime/package.json                # 0.2.0-beta → 0.2.0-beta.1
M  packages/codeflow-runtime/src/registry/AgentSdkAdapter.ts  # defaultModel 字段 + create/send fallback
?? packages/codeflow-runtime/src/registry/__tests__/AgentSdkAdapter.test.ts  # 新文件，5 测试
```

### Commit message（heredoc）

```text
fix(s6-v0.2-sprint0-mt1-hotfix): wire cfg.cursor.defaultModel through Agent.create / agent.send

- codeflow-shell + @codeflow/runtime: 0.2.0-beta -> 0.2.0-beta.1
- CursorSdkAdapterOptions.defaultModel: spec.modelId ?? this._opts.defaultModel
  fallback chain in both create() and send(); closes BUG-SDK-001 (QA-009 §五).
- sdk-factory.ts forwards cfg.defaultModel via spread (no behavioral change
  when defaultModel is undefined).
- .env.example: uncomment CURSOR_DEFAULT_MODEL=auto + 16-line guidance block
  about local-mode model requirement.
- main.ts: banner WARNING block when live + local + no defaultModel
  (friendly preflight; doesn't fail-fast).
- AgentSdkAdapter.test.ts: 5 new seam tests (TS-MODEL-1..5) via Agent.create /
  Agent.resume monkey-patch; runtime tests 99 -> 104, 0 flakes.
- README.md: Whitelisted env vars table + Quick start step 4 updated to mark
  CURSOR_DEFAULT_MODEL as required-in-local-mode.

Refs: TASK-20260510-010-PM-to-DEV, REPORT-20260510-010-DEV-to-PM
```

### 本地 annotated tag（**不推 origin/backup**）

```bash
git tag -a v0.2.0-beta.1 -m "CodeFlow v0.2.0-beta.1 - MT-1 hotfix: wire defaultModel through SDK; closes BUG-SDK-001 (pending QA-011 real verdict A-08/A-10)"
```

## §三 Commit B — docs(v0.2.0-beta.1-archive)

### 范围（5 项 untracked docs）

```text
?? docs/agents/tasks/REPORT-20260510-005-PM-to-ADMIN.md   # PM fcop@1.0 + BUG-SDK-001 通报
?? docs/agents/tasks/REPORT-20260510-008-OPS-to-PM.md     # OPS v0.2.0-beta commit 回执（上轮 docs-tail）
?? docs/agents/tasks/REPORT-20260510-009-QA-to-PM.md      # QA v0.2.0-beta 验收（揭示 BUG-SDK-001）
?? docs/agents/tasks/TASK-20260510-010-PM-to-DEV.md       # PM MT-1 派单
?? docs/agents/tasks/REPORT-20260510-010-DEV-to-PM.md     # DEV MT-1 完工回执
```

### Commit message

```text
docs(s6-v0.2-sprint0-mt1-archive): hotfix dispatch + reports + fcop@1.0 surfacing

- REPORT-005-PM-to-ADMIN: fcop@1.0 已上 PyPI (5/9), BUG-SDK-001, 路线提前 3 天
- REPORT-008-OPS-to-PM: v0.2.0-beta commit + tag 回执 (de42877 / 5f6f64b)
- REPORT-009-QA-to-PM: v0.2.0-beta 验收 (Safety 5/5 + B 7/7 + A-08/A-10 BUG-SDK-001)
- TASK-010-PM-to-DEV: MT-1 defaultModel wire-through hotfix 派单
- REPORT-010-DEV-to-PM: MT-1 完工 (~70 min) + 5 主交付 + 3 surprises

Refs: REPORT-20260510-009-QA-to-PM §五 BUG-SDK-001
```

## §四 安全 HARD GATE（stage 前 + stage 后双跑）

### 4.1 stage 前：工作树扫描

```powershell
git diff | Select-String -Pattern 'crsr_[0-9a-f]{8,}|ck_[0-9a-f]{8,}|sk-[A-Za-z0-9]{20,}'
```

**期望：0 匹配**。如有匹配立即 abort + alert PM。

注：DEV 已自跑此命令 = 0 匹配。OPS 复跑兜底。

### 4.2 stage 后：staged 内容扫描

```powershell
git diff --cached | Select-String -Pattern 'crsr_[0-9a-f]{8,}|ck_[0-9a-f]{8,}|sk-[A-Za-z0-9]{20,}'
```

**期望：0 匹配**。如有匹配立即 `git restore --staged <file>` + alert PM/ADMIN。

### 4.3 `.env` / `.env.tmp_selftest` 不在 stage 列表

```powershell
git diff --cached --name-only | Select-String -Pattern '^codeflow-shell/\.env(\.tmp_selftest)?$'
```

**期望：0 匹配**（DEV self-test 5 用过 `.env.tmp_selftest` 临时文件，已 rename 回 `.env`，但 OPS 兜底确认）。

## §五 推送策略

| 仓库 | main 推送 | tag 推送 |
|---|---|---|
| origin (codeflow-pwa)  | ✅ 推 Commit A + B | ❌ **不推** v0.2.0-beta.1 tag |
| backup (codehouse)     | ✅ 推 Commit A + B | ❌ 不推 |
| gitee                  | ❌ 不动（保持 G3 = `62532a7`）| ❌ 不推 |

理由同前 3 个 v0.x.x-rc/alpha/beta tag 政策（[TASK-008 §五](TASK-20260510-008-PM-to-OPS.md) 同源）：本地 RC 不污染公仓 tag 命名空间，待 v1.0 公开发布时再决定 tag 推送策略。

## §六 10 项验收输出（本 REPORT 必填）

```powershell
# 1
git log --oneline -5

# 2
git tag --list "v0.2.*"

# 3
git show v0.2.0-beta.1 --stat | Select-Object -First 30

# 4 - origin 无 v0.2.* tag（同前）
git ls-remote --tags origin | Select-String "v0.2"

# 5 - backup 无 v0.2.* tag
git ls-remote --tags backup | Select-String "v0.2"

# 6 - origin/main = local
$local = (git rev-parse HEAD).Trim()
$origin = (git ls-remote origin main | ForEach-Object { $_.Split()[0] }).Trim()
"local : $local"; "origin: $origin"; if ($local -eq $origin) { "MATCH" } else { "MISMATCH" }

# 7 - backup/main = local
$backup = (git ls-remote backup main | ForEach-Object { $_.Split()[0] }).Trim()
"backup: $backup"; if ($local -eq $backup) { "MATCH" } else { "MISMATCH" }

# 8 - gitee 仍保持 G3
git ls-remote gitee main 2>&1 | Select-Object -First 1

# 9 - runtime 104/104
cd packages/codeflow-runtime; npm test 2>&1 | Select-String "tests|pass|fail" | Select-Object -First 4; cd ../..

# 10 - post-commit git status (期望空)
git status --short
```

## §七 不动手清单

- ❌ 不推 v0.2.0-beta.1 tag 到 origin/backup
- ❌ 不动 gitee
- ❌ 不动 server/relay、Nginx、防火墙
- ❌ 不 npm publish
- ❌ 不读 `codeflow-shell/.env`（同 QA-009 SAFETY 要求）

## §八 SLA + 回执

| 项 | 值 |
|---|---|
| SLA | ≤ 30 min |
| 回执 | `docs/agents/tasks/REPORT-20260510-011-OPS-to-PM.md` 含上述 10 项验收 + 安全 HARD GATE 三段输出 |

## §九 完工后链式

OPS-011 commit 落地 → PM 立即派 [TASK-20260510-011-PM-to-QA](TASK-20260510-011-PM-to-QA.md) 跑 A-08/A-10 真 verdict 验收（BUG-SDK-001 最终关闭权）。

DEV §八 自决：OPS-011 commit 落地后 10 min 内自动启动 **P3 pre-analysis（read-only，不动手）**。PM 等 QA-011 完工后再正式派 DEV P3 single（[TASK-20260510-012-PM-to-DEV](TASK-20260510-012-PM-to-DEV.md)，待派）。

PM-01
2026-05-10 22:00 (UTC+8)
