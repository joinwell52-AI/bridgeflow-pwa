{
  "name": "codeflow-shell",
  "version": "0.2.0-beta.1",
  "private": true,
  "description": "CodeFlow Shell — minimal executable wrapper around @codeflow/runtime. v0.2.0-beta.1 (MT-1 hotfix on top of P2: cfg.cursor.defaultModel wired end-to-end into Agent.create / agent.send; closes BUG-SDK-001 from QA-009).",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "tsx src/main.ts",
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/main.bundle.js",
    "pack:win": "pack.cmd"
  },
  "dependencies": {
    "@codeflow/runtime": "file:../packages/codeflow-runtime",
    "@codeflow/protocol": "file:../packages/codeflow-protocol"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "postject": "^1.0.0-alpha.6",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}
