---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-011
sender: PM
recipient: OPS
priority: P0
thread_key: s3-launch-commit-bundle-and-dev-env
references:
  - REPORT-20260509-013-PM-to-ADMIN
  - TASK-20260509-009-PM-to-DEV
  - TASK-20260509-010-PM-to-QA
  - HANDOFF-20260509-001-PM-session-close-to-next-sprint
layer: worker
---

# Sprint S3 启动：dev env 检查 + 9 文件 commit bundle + push origin/backup（gitee 按 G3 暂不动）

## ADMIN 5/9 13:05 决策

= D 选项 = **commit-bundle**：把 5 个本地未追加文件 + 本回合 PM 新增的 4 个文件（REPORT-013 + TASK-009/010/011）合并一次 commit，避免 git history 出现"小 commit 噪音"。

**重要**：本任务**最后才执行**——先确认 PM 已经把 4 个文件全部落盘 + DEV/QA 已能从各自接单文件读到内容（自检确认能开工），OPS 再按下方步骤 commit。

## 待 commit 的 9 个文件清单

```
A. 历史未追加（HANDOFF + 上一会话遗留，5 个）
   docs/agents/tasks/HANDOFF-20260509-001-PM-session-close-to-next-sprint.md
   docs/agents/tasks/REPORT-20260509-008-OPS-to-PM.md
   docs/agents/tasks/REPORT-20260509-011-PM-to-ADMIN.md     ← HANDOFF 漏算的第 5 个
   docs/agents/tasks/REPORT-20260509-012-PM-to-ADMIN.md
   docs/agents/tasks/TASK-20260509-008-PM-to-OPS.md

B. 本回合 PM 新增（4 个）
   docs/agents/tasks/REPORT-20260509-013-PM-to-ADMIN.md
   docs/agents/tasks/TASK-20260509-009-PM-to-DEV.md
   docs/agents/tasks/TASK-20260509-010-PM-to-QA.md
   docs/agents/tasks/TASK-20260509-011-PM-to-OPS.md         ← 本文件
```

执行前 OPS 必须先跑 `git status --short` 验证待加的就是这 9 个，不多不少。

## 高危操作判定

按 [`ops-bridge.mdc`](../../.cursor/rules/ops-bridge.mdc) "高危操作必须二次确认" 4 类：
- 重启生产服务 ❌ 不涉及
- 修改 Nginx 配置 ❌ 不涉及
- 清理数据库或日志 ❌ 不涉及
- 变更防火墙规则 ❌ 不涉及

→ **本任务不属于高危**，无需二次确认。但 push 是远程仓库写操作（仍属运维范畴 + 可逆，最坏情况 `git revert` 即可），按 ops-bridge 规则可执行。

按 [HANDOFF §6.3 git safety](./HANDOFF-20260509-001-PM-session-close-to-next-sprint.md)：
- 不擅自 force-push / hard reset / push --all / 动 .git/config — 本任务**全部遵守**（只 push 一次 main 普通 fast-forward）
- gitee 按 HANDOFF §4 #1 G3 = 暂不动（gitee 仍在 v2.12.0 时代分叉的 `62532a7`）

## 执行命令（按顺序）

### Phase 1：dev env 健康检查（≤ 3 min）

```powershell
# 1.1 git 工作区干净度
git status --short
# 期望：恰好 9 个 ?? 行，与上方文件清单完全一致

# 1.2 Node / npm 版本（v0.1 路线图要求 Node ≥ 20）
node --version
npm --version
# 期望：node v20.x 或 v22.x；npm 任意（≥ 10 即可）

# 1.3 packages/ 编译能过（pre-commit 健康检查）
cd packages/codeflow-protocol
npm install
npm test
# 期望：5 valid + 3 invalid fixtures 全部如预期

cd ..\codeflow-runtime
npm install
npx tsc --noEmit
# 期望：0 报错（S2 设计骨架本来就 typecheck 通过）

cd ..\..
```

如果 Phase 1 任一项失败，**停下不 commit**——立刻在 `REPORT-011-OPS-to-PM` 里描述失败现象，等 PM 协调 DEV 修复后重试。

### Phase 2：commit bundle（≤ 1 min）

```powershell
# 2.1 加 9 个文件
git add docs/agents/tasks/HANDOFF-20260509-001-PM-session-close-to-next-sprint.md `
        docs/agents/tasks/REPORT-20260509-008-OPS-to-PM.md `
        docs/agents/tasks/REPORT-20260509-011-PM-to-ADMIN.md `
        docs/agents/tasks/REPORT-20260509-012-PM-to-ADMIN.md `
        docs/agents/tasks/REPORT-20260509-013-PM-to-ADMIN.md `
        docs/agents/tasks/TASK-20260509-008-PM-to-OPS.md `
        docs/agents/tasks/TASK-20260509-009-PM-to-DEV.md `
        docs/agents/tasks/TASK-20260509-010-PM-to-QA.md `
        docs/agents/tasks/TASK-20260509-011-PM-to-OPS.md

# 2.2 commit
git commit -m "docs(s3-launch): handoff close + dispatch S3 phase A — AgentRegistry impl + test strategy"

# 2.3 验证 commit 落地
git log --oneline -1
# 期望：新 hash（与 04b3cdf 不同），message 正确
git show --stat HEAD
# 期望：恰好 9 个 .md 文件被 added，0 modified，0 deleted
```

**若 commit 失败**（如 pre-commit hook 拦截 / lint 错误）：不要 amend、不要 force——直接修问题后再做 *新* commit；按 git safety，amend 仅在 PM 显式授权时才允许。

### Phase 3：push origin + backup（≤ 2 min；gitee 跳过）

```powershell
# 3.1 fetch 检查 ahead 状态
git fetch --all
git status -sb
# 期望：包含「ahead 1」（本地领先 origin 1 commit）

# 3.2 push origin（github.com/joinwell52-AI/codeflow-pwa）
git push origin main

# 3.3 push backup（github.com/joinwell52-AI/codehouse）
git push backup main

# 3.4 gitee 按 G3 暂不动 — 显式跳过
echo "gitee push skipped per HANDOFF-001 §4 #1 G3 decision (gitee remains diverged at 62532a7)"

# 3.5 三仓 HEAD 对比
$localHead = git rev-parse HEAD
$originHead = git ls-remote origin main | ForEach-Object { ($_ -split "`t")[0] }
$backupHead = git ls-remote backup main | ForEach-Object { ($_ -split "`t")[0] }
$giteeHead  = git ls-remote gitee main  | ForEach-Object { ($_ -split "`t")[0] }
Write-Host "local : $localHead"
Write-Host "origin: $originHead"
Write-Host "backup: $backupHead"
Write-Host "gitee : $giteeHead   (expected to lag at 62532a7… per G3)"
```

期望：
- local / origin / backup 三者 HEAD 一致（新 commit hash）
- gitee 仍是 `62532a7`（前 7 位）—— 本任务**不**期望它和本地一致

## 失败处理

| 错误 | 处理 |
|---|---|
| Phase 1.3 npm install 失败 | 报告失败，停手，让 PM 协调 DEV 处理依赖问题 |
| Phase 1.3 typecheck 失败 | 同上（不应该发生 — 因为 commit 6595427 已经过 typecheck）|
| Phase 2 commit 被 pre-commit hook 拦 | 看 hook 输出 → 报告 PM；不 `--no-verify` |
| Phase 3.2 origin `non-fast-forward` | `git fetch origin` → 看 origin 是否有新 commit；如有，**停手**报告 PM（不擅自 rebase）|
| Phase 3.3 backup 鉴权失败 | 重试 1 次；仍失败 → 报告，但不阻塞总任务（origin 成功 = 主备份已就位）|
| 网络超时 | 重试 1 次；仍失败 → 报告 |

## 验收（OPS 必出 8 条验证）

| # | 验收项 | 命令 |
|---|---|---|
| 1 | git 工作区在 commit 前恰好 9 个未追加 | `git status --short` 行数 = 9 |
| 2 | Node 版本 ≥ 20 | `node --version` major ≥ 20 |
| 3 | `@codeflow/protocol` 测试通过 | `cd packages/codeflow-protocol && npm test` exit 0 |
| 4 | `@codeflow/runtime` typecheck 通过 | `cd packages/codeflow-runtime && npx tsc --noEmit` exit 0 |
| 5 | commit 含 9 个 added 文件 | `git show --stat HEAD` 文件计数 = 9 added / 0 modified / 0 deleted |
| 6 | commit message 正确 | `git log -1 --pretty=%s` = `docs(s3-launch): handoff close + dispatch S3 phase A — AgentRegistry impl + test strategy` |
| 7 | origin / backup HEAD = local | `git ls-remote origin main` + `git ls-remote backup main` 头部 hash = local HEAD |
| 8 | gitee 仍在 `62532a7…` | `git ls-remote gitee main` 头部 hash 前 7 位 = `62532a7` |

## 不动手清单

- ❌ 不创建 tag（v2.13.0 等不在本任务范围）
- ❌ 不 push gitee（G3 决策）
- ❌ 不创建 GitHub Release / Gitee Release
- ❌ 不打包 EXE
- ❌ 不动 CHANGELOG.md / main.py VERSION
- ❌ 不 push 任何分支以外的 ref（如 `--all` / `--mirror` / `--tags`）
- ❌ 不修改 `.git/config`
- ❌ 不 commit 包含 `_ignore/` / `private/` / `.env*` 内容（这些被 .gitignore 排除，正常 add 不会进，但 OPS 应在 `git show --stat HEAD` 时再扫一眼确认零误入）

## 时间预算

≤ 6 分钟（Phase 1: 3min + Phase 2: 1min + Phase 3: 2min）

## 启动条件（OPS 等什么）

OPS 在以下两件都为真后再开干：

1. ✅ PM 已落盘 4 个新文件（REPORT-013 + TASK-009/010/011 全部存在于 `docs/agents/tasks/` 下）—— 用 `Test-Path` 4 项验证
2. ✅ DEV-01 / QA-01 已分别看到自己的 TASK 文件（不需要他们回执，只需他们"接到单"——这一步可由 ADMIN 或 PM 在群里通知后再触发 OPS）

## 完成后回执要求

按 ops-bridge 规则写 `REPORT-20260509-011-OPS-to-PM.md`，**必须**包含：

1. **8 条验收命令的实际输出**（粘贴 stdout）
2. **三仓最终 HEAD hash 对比表**（local / origin / backup / gitee，见 Phase 3.5）
3. **任何 push / commit 失败的根因**（如有）
4. **本次 commit 的 SHA-1**（前 7 位 + 完整 hash）
5. **下一步建议**：origin/backup 是否双备份就绪？gitee 是否可在本任务后启动单独的"分叉决议任务"？

不能只写"已完成"——必须附验证命令输出（按 ops-bridge "回执必须含验证结果"）。

---

PM-01 派单。OPS-01 等 PM 落盘完毕信号 + DEV/QA 接单确认信号后立即执行。回执请落 `REPORT-20260509-011-OPS-to-PM.md`。
