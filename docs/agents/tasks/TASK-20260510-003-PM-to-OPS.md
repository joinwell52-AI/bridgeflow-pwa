---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260510-003
sender: PM
recipient: OPS
priority: P1
thread_key: codeflow-v0.2-relay-health-check-and-deploy-prep
references:
  - REPORT-20260510-002-PM-to-ADMIN
  - TASK-20260510-002-PM-to-DEV
  - server/relay/server.py
  - web/pwa/config.js
  - scripts/deploy_public_relay.py
layer: worker
---

# TASK-20260510-003：公网 relay 健康验证 + v2 部署预案

## §一 一句话指令

ADMIN 已下「加速」令；**DEV-002 P3 (relay-bridge) 启动前需先 OPS 确认公网 relay 在线 + 协议未变 + room_key 隔离正确**，并准备 v2 codeflow-shell 部署到公网试用机的预案。

## §二 交付（3 步串行）

### Step 1：公网 relay 健康验证（≤ 15 min）

| 命令 | 期望 |
|---|---|
| `Test-NetConnection ai.chedian.cc -Port 443` | TcpTestSucceeded : True |
| `npx wscat -c wss://ai.chedian.cc/codeflow/ws/` | 连接成功 |
| 发 `{"room_key":"codeflow-ops-health-test","event_type":"ping","payload":{}}` | 收到 `{"event_type":"pong"}` |
| 发 `{"room_key":"codeflow-ops-health-test","event_type":"hello","sender":"OPS","client_type":"ops_health","payload":{"device_id":"ops-test","device_name":"OPS Health Test","owner_role":"OPS"}}` | 收到 `device_roster` 帧 |
| `git log -- server/relay/server.py \| Select-Object -First 5` | 列出最近 5 次 server.py 修改（确认协议未变）|

### Step 2：公网 relay 部署版本核对（≤ 10 min）

| 维度 | 命令 / 期望 |
|---|---|
| 公网部署 commit hash | （从 ai.chedian.cc 服务器取 — OPS 用 ssh 或 scripts/deploy_public_relay.py 内含的方式）|
| 本地 server/relay/server.py 当前 commit | `git log -1 server/relay/server.py` |
| 二者是否一致 | 如不一致，记录差异（OPS 不动手部署，等 PM 决策）|
| ALLOWED_EVENTS 是否含 `task_event` / `agent_status` | 应含（v1 时代加的）|

### Step 3：v0.2 / v1.0 部署预案 SPIKE（≤ 30 min）

输出 `docs/design/spike-v2-deploy.md`，含：

- **方案 A**：v2 codeflow-shell 仅本地跑（ADMIN 自己 PC），不部署公网 — 简单，**PM 推荐 v0.2 阶段用此**
- **方案 B**：v2 codeflow-shell 用 codespace / 公网 VPS 跑，PWA 远程下达 — 复杂，v1.0 后阶段
- **方案 C**：v2 codeflow-shell 内嵌一个本地 relay（127.0.0.1:5252），PWA 通过公网 relay 中转 → 本地 relay → v2 — 中等，v1.0 阶段
- 三方案对比矩阵：部署复杂度 / 安全性 / ADMIN 体验

## §三 不动手清单

- ❌ **不**修改公网 relay 部署（任何变动需 ADMIN 二次确认 — 高危规则）
- ❌ **不**部署 v2 codeflow-shell 到公网（仅 SPIKE 文档，不实操）
- ❌ **不**清理 ai.chedian.cc 上的房间数据 / 用户数据
- ❌ **不**改 nginx / 防火墙
- ❌ **不**给 v0.2.0-* / v1.0-* tag 推 origin（与 v0.1.0-rc.1 同策略）

## §四 回执（≤ 1 小时完工）

```
docs/agents/tasks/REPORT-20260510-003-OPS-to-PM.md
```

含 §二 三步全部命令输出 + spike-v2-deploy.md 链接。

## §五 SLA

预计 ≤ 1 小时；OPS 完工后 PM 启动后续基于此的 DEV-002 P3 (relay-bridge) 编码。

PM-01
2026-05-10 01:00 (UTC+8)
