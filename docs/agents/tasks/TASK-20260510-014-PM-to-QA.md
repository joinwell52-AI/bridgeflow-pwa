---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260510-014
sender: PM
recipient: QA
priority: P0
thread_key: codeflow-v0.2.0-beta-2-acceptance-and-4-bug-closed-judgement
references:
  - REPORT-20260510-013-DEV-to-PM
  - REPORT-20260510-012-DEV-to-PM
  - REPORT-20260510-011-QA-to-PM
  - TASK-20260510-014-PM-to-OPS
layer: governance
---

# TASK-20260510-014：v0.2.0-beta.2 完整真 verdict 验收 + 4 BUG closed 判定

## §一 一句话指令

OPS-014 commit + 本地 tag `v0.2.0-beta.2` 落地后，跑完整 A-07~A-10 真 verdict 验收，**正式 closed BUG-SDK-001/002/003/004**。如撞 BUG-SDK-006 间歇性 race（reviewer 不 dispatch / 任务重复 dispatch），不算 v0.2.0-beta.2 fail，记录现象交后续 micro-task。Safety HARD GATE 与 QA-009/QA-011 同源不变。

## §二 SAFETY HARD GATE（沿用 QA-009/011，不变）

| 检查 | 操作 | 期望 |
|---|---|---|
| 全程不读 `.env` 内容 | 无 `cat .env` / `Get-Content .env` / `type .env` | ✅ 合规 |
| 全程不 echo 真 key | banner 仅显示 `apiKey from config` | ✅ 合规 |
| REPORT 不含真 key / 含 key 的 stack | review/transcript 仅贴 frontmatter / event_type 元数据 | ✅ 合规 |
| `.env.example` git diff 0 真 key | `git diff codeflow-shell/.env.example \| Select-String "crsr_[0-9a-f]{8,}"` → 0 | ✅ PASS |
| `.env` 仍 gitignored | `git status --short codeflow-shell/.env` 无显示 | ✅ PASS |

## §三 验收 A-07 ~ A-10（**实战门**）

### A-07 — banner live + model

```powershell
cd D:\Bridgeflow\codeflow-shell
node -e "console.log(require('./package.json').version)"
# 期望：0.2.0-beta.2

git tag --contains HEAD
# 期望：v0.2.0-beta.2

# 启动观察 banner
npm start
```

期望 banner 关键字：
- `CodeFlow v0.2.0-beta.2`
- `Cursor SDK     : live (CursorSdkAdapter; ... defaultModel="<value>")`
- 如 ADMIN `.env` 含 `CURSOR_DEFAULT_MODEL` → 该值；如未含 → fallback 到 `.env.example` 默认 `default`（MT-3 改的）

### A-08 — 真 verdict（核心目标）

```powershell
# 在另一窗口
Copy-Item codeflow-shell\sample-task.md codeflow-shell\inbox\TASK-20260510-998-PM-to-DEV.md
# 等 90~180s（真 LLM 调用）

# 观察 main shell stdout
# 期望关键字：
#   [TaskDispatcher] received TASK-20260510-998...
#   [SessionManager] startSession session_id=...
#   [SessionManager] session_ended ...
#   [ReviewEngine] dispatching reviewer for subject...
#   [ReviewEngine] decision=<one of: approved/rejected/needs_changes/abstained>
```

⚠️ **如撞 BUG-SDK-006 race**（reviewer 0 dispatch / 任务重复 dispatch）：
1. **不算 A-08 fail**，记录现象到 REPORT
2. **重试 1 次**（rename `.smoke-*` 全 dataDir + 重启 shell + 重 drop）
3. 第 2 次仍 race → 试 prior path：用 `.smoke-claude-rerun/` + 等更长（240s）
4. 仍不行 → REPORT 标 `A-08 PARTIAL: BUG-SDK-006 race observed`，不阻塞 v0.2.0-beta.2 ship

### A-09 — UUID `sdk_agent_id`

期望：`sdk_agent_id = agent-<UUID>` 格式。同 QA-011 已有证据。

### A-10 — 真 transcript

期望：
- `data/transcripts/<sdk_agent_id>.jsonl` size > 100 bytes
- 至少 1 行 `{type: "assistant", message: { role: "assistant", content: [{ type: "text", text: ... }] }}`（H4 修复后的 SDK 真形态）
- 不贴 content 字段进 REPORT，仅贴 `event_type` + `sdk_agent_id` + size

## §四 4 BUG closed 判定（QA 正式 sign-off）

| BUG | DEV 视角 | QA 视角（本任务确认）| 关闭判定 |
|---|---|---|---|
| BUG-SDK-001 | ✅ closed by MT-1 | A-07 banner 含 `defaultModel=...` ✅ | **closed** |
| BUG-SDK-002 | ✅ closed by MT-2 | A-08 stdout 0 次 `already has active run` | **closed**（必证）|
| BUG-SDK-003 | ✅ closed by MT-3 | A-07 如 ADMIN `.env` 未设 model，fallback 到 `default` 不 `auto`；或 `.env.example` `git diff` 含 `auto → default` | **closed** |
| BUG-SDK-004 | ✅ closed by MT-4 unit test | A-08 如成功跑 → `decision ∈ {approved, rejected, needs_changes, abstained}`（**非** `verdict_parse_failed`）| **closed**（如撞 BUG-SDK-006 race，QA 仍可 sign off — H4 unit test 已强证 extractText 路径正确）|

## §五 BUG-SDK-006 race 现象记录（重要）

如观察到：
- reviewer 0 dispatch
- 同一 sdk_agent_id 多次 dispatch
- transcripts/ 多文件但 reviews/ 空

记录到 REPORT §六，含：
- task_id 字符串（`mt2-smoke` 短 vs prior `claude-sonnet-4-mt2` 长 — DEV §6.2 给的差别）
- task body 长度（行数）
- run_id 列表（同 sdk_agent_id 是否多 run）
- 是否触发 chokidar change（如能 enable verbose log）

PM 后续派 BUG-SDK-006 micro-task 时用此现象对 DEV 假设 A/B/C 选优先级。

## §六 验收结论模板（REPORT 必含）

```text
Safety HARD GATE：✅ 5/5 全通 / ❌ FAIL

A-07 banner live：    ✅ / ❌（细节）
A-08 真 verdict：     ✅（decision=<value>） / 
                     PARTIAL（BUG-SDK-006 race，第 N 次重试，现象详 §六） /
                     ❌（FAIL，详细错误）
A-09 UUID sdk_id：    ✅（agent-<UUID-prefix>...）/ ❌
A-10 transcript：     ✅（size=<N>，content[] 形态符合 H4）/ ❌

BUG-SDK-001 closed：  ✅ / ❌
BUG-SDK-002 closed：  ✅ / ❌
BUG-SDK-003 closed：  ✅ / ❌
BUG-SDK-004 closed：  ✅（unit + smoke）/ ✅（unit 强证，smoke 受 SDK-006 race 阻塞）

BUG-SDK-005 noise：   ⚠️ 观察到 / 未观察 — stderr 是否含 "Ripgrep path not configured"
BUG-SDK-006 race：    ⚠️ 观察到（详 §六 现象） / 未观察 — reviewer 是否成功 dispatch

v0.2.0-beta.2 ship-ready 判定：
  → ✅（4 BUG closed + race 不阻塞）
  → ⏸ 需 BUG-SDK-006 修后再 ship（仅当 race 100% 复现且 4 个 BUG closed 也无法证）
```

## §七 SLA + 回执

| 项 | 值 |
|---|---|
| SLA | ≤ 60 min（含 BUG-SDK-006 race 重试 2 次余量）|
| 回执 | `docs/agents/tasks/REPORT-20260510-014-QA-to-PM.md` |

## §八 完工后链式

QA-014 ✅（4 BUG closed）→ PM 立即：

1. 写 [REPORT-20260510-007-PM-to-ADMIN](REPORT-20260510-007-PM-to-ADMIN.md)：v0.2.0-beta.2 出厂里程碑 + 4 BUG closed + 2 informational + 路线锁定
2. 派 **TASK-20260510-015-PM-to-DEV**：BUG-SDK-006 调研 micro-task（read-only，DEV 看 InboxWatcher / chokidar / atomic-write retry 联动），SLA 60 min；以及/或者 P3 (relay-bridge) 正式实施单（DEV §九 read-only 已暖机）

PM-01
2026-05-10 23:36 (UTC+8)
