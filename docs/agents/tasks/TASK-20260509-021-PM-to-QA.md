---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-021
sender: PM
recipient: QA
priority: P1
thread_key: codeflow-v2-sprint-s3-phase-c-regression-test-and-final-verdict
references:
  - REPORT-20260509-019-QA-to-PM
  - REPORT-20260509-018-DEV-to-PM
  - TASK-20260509-019-PM-to-QA
  - TASK-20260509-020-PM-to-OPS
layer: governance
---

# Sprint S3 Phase C 工作 3 — Phase B 回归测试 + S4 启动推荐

## 背景

QA 工作 1+2（[`REPORT-20260509-019-QA-to-PM.md`](./REPORT-20260509-019-QA-to-PM.md)）已完成 — §3.5 13 场景 + §5c Phase C 验收清单全部就绪。
工作 3 启动条件 = OPS 完成 [`TASK-20260509-020-PM-to-OPS.md`](./TASK-20260509-020-PM-to-OPS.md) Phase C done checkpoint commit。本任务派 QA 跑工作 3。

## 工作内容

### 工作 3.1：Phase A/B/C 全套回归（基线 commit = OPS-020 后的 SHA）

```powershell
cd packages/codeflow-runtime
npm test
```

- 期望：`tests 54 / pass 54 / fail 0`（PM 已独立验过；QA 在 OPS 第四轮 commit 之后再独立验一次）
- 重点核 Phase B 22 个测试（TS-4.1~4.5 + SessionStore 8 + TranscriptWriter 6 + onEvent 隔离 1+ 等）跟 REPORT-013-DEV 一致

### 工作 3.2：把回归结果写进 `test-strategy-s3.md §6`

按 §5a / §5b / §5c 的格式风格新增 §6 节：

```markdown
## §6 Phase B 回归测试结果（Phase C 完成时点）

| 维度 | 数量 | 状态 |
|---|---|---|
| Phase A registry/* | 18 | ✅ 全过 |
| Phase B session/*  | 22 | ✅ 全过 |
| Phase C scheduler/*| 14 | ✅ 全过 |
| 合计              | 54 | ✅ 0 fail |

| Phase B 测试 # | 状态 | 备注 |
|---|---|---|
| TS-4.1  | ✅ | startSession on unknown agent → AgentNotFoundError |
| TS-4.1b | ✅ | startSession on running agent → InvalidAgentStatusError |
... (22 行逐项 ✅) ...
```

可以直接把 `npm test` 的关键 stdout 粘贴进文档。

### 工作 3.3：给 S4 启动推荐

按 [`REPORT-20260509-019-QA-to-PM.md`](./REPORT-20260509-019-QA-to-PM.md) §四 暗示，工作 3 完成时 QA 给定论。
推荐内容包含：

1. ✅ / ❌ 是否推荐进入 Sprint S4（Skill Runtime）
2. 主要依据（Phase C 接口稳定 / 测试覆盖 / 无回归 等）
3. **决策 B' 是否需要 S4 入手**：DEV REPORT-018 §决策 B' 提到「SessionManager.startSession 成功后把 agent.status 设为 running，settle 时设回 idle」是 v0.1 集成层钩子。QA 评估这是 S4 工作还是 v0.2？
4. **TS-4.6 编号悬空**：QA REPORT-019 §1.1 表里 Phase B 列了 6 个但只有 TS-4.1~4.5 5 个完整，TS-4.6 TBD。是否需要在 §3.4 末尾补完 TS-4.6 占位的实际场景？还是确定 v0.1 不需要？

### 工作 3.4：DEV §十 TS-5.x 编号差异确认（顺手）

DEV REPORT-018 §十 提到原 `test-strategy-s3.md §3.5` 旧占位（TS-5.1~5.4 4 个）跟 PM TASK-018 §单元测试覆盖（TS-5.1~5.12 12 个）编号 + 内容不一致。
QA 确认在 §3.5 改写时是否已经按 TASK-018 最新分配落地（PM 看 REPORT-019 已经认为对齐了，但请 QA 二次确认 — 防止漏一个旧占位编号）。

## 不做（out-of-scope）

- ❌ 不写自动化测试代码（DEV 写）
- ❌ 不修改 `@codeflow/protocol` schema
- ❌ 不修改任何 docs/agents/tasks/ 现有 task 文件
- ❌ 不擅自给"决策 B' 钩子"开 v0.1 单——QA 只评估它**应当**在哪个 sprint，不直接派活
- ❌ 不写 v0.2 测试场景（v0.2 整体在 ADMIN 拍板后才进规划）

## 验收标准（5 项）

| # | 项 | 验证方式 |
|---|---|---|
| 1 | npm test 在 OPS-020 commit 后跑过且 54/54 PASS | stdout 截图入回执 |
| 2 | §6 回归测试结果节落入 test-strategy-s3.md | 文档查 |
| 3 | S4 启动推荐 ✅ / ❌ + 主要依据 | 回执明文 |
| 4 | 决策 B'（agent.status 钩子）的归属判断 | 回执明文（S4 / v0.2 / 其他）|
| 5 | TS-4.6 + DEV §十 TS-5.x 编号差异的最终确认 | 回执明文 |

## 时间预算

- 预计 **1-2 小时**（跑测试 + 写 §6 + 评估推荐）
- 超 1.5x = 3h 无进展时，写 `REPORT-021-QA-to-PM` 求救

## 启动条件

QA 等 OPS 完成 `TASK-020` Phase C done checkpoint commit 后立即执行。

## 完成后回执要求

按 `qa-bridge` 规则写 `REPORT-20260509-021-QA-to-PM.md`，必须包含：

1. npm test 实测 stdout（≥ Phase B 22 项 + Phase C 14 项 验收点都体现）
2. 缺陷描述（如有；预期应为 0）
3. S4 启动推荐 + 主要依据
4. 决策 B' 归属判断
5. TS-4.6 + TS-5.x 编号最终对齐确认
6. 是否建议立刻派 TASK-022 (S4 DEV) — yes/no + 理由

---

PM-01 派单。QA-01 等 OPS-020 commit 后立即执行。回执请落 `REPORT-20260509-021-QA-to-PM.md`。
