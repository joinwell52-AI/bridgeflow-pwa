---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-008
sender: PM
recipient: OPS
priority: P0
thread_key: r2-three-remote-backup
references:
  - REPORT-20260509-011-PM-to-ADMIN
layer: worker
---

# R2 纯备份 — push 当前 main 到 3 个 remote

## ADMIN 5/9 11:47 决策

= R2「纯备份（推荐）」 — 跳过 EXE 打包 + GitHub/Gitee Release，只 push 让今天 2 commit 备份到 3 仓。

## 待 push 的 commit

```
04b3cdf chore(s3-prep): pre-Sprint-S3 housekeeping — fcop guide + roles map + tasks archive
6595427 feat(v2): launch CodeFlow AI Runtime — design, schemas, charter, hard rule #5 purge
```

## 执行命令（按 release-process.md 顺序）

```powershell
# 0. 预检：确保本地是 ahead，避免被拒
git fetch --all

# 1. push 主仓（origin = github.com/joinwell52-AI/codeflow-pwa）
git push origin main

# 2. push 国内镜像（gitee = gitee.com/joinwell52/cursor-ai）
git push gitee main

# 3. push 纯备份（backup = github.com/joinwell52-AI/codehouse）
git push backup main
```

**不**触发：tag 创建 / GitHub Release / Gitee Release / EXE 打包。

## 为什么 OPS 而不是 DEV

按 ops-bridge.mdc 规则：远程仓库写操作 = 运维范畴。
不属于 ops-bridge 4 类高危（重启生产 / 改 Nginx / 清数据库 / 改防火墙），故无需二次确认。

## 验收（OPS 必出 7 条验证）

| # | 验收项 | 命令 |
|---|---|---|
| 1 | 本地 HEAD | `git rev-parse HEAD` → 应为 `04b3cdf` |
| 2 | origin 远程 HEAD = local | `git ls-remote origin main` 头部 hash = local HEAD |
| 3 | gitee 远程 HEAD = local | `git ls-remote gitee main` 头部 hash = local HEAD |
| 4 | backup 远程 HEAD = local | `git ls-remote backup main` 头部 hash = local HEAD |
| 5 | origin 上 commit 6595427 可见 | `git log origin/main --oneline -3` |
| 6 | gitee 上 commit 6595427 可见 | `git log gitee/main --oneline -3` |
| 7 | backup 上 commit 6595427 可见 | `git log backup/main --oneline -3` |

## 失败处理

| 错误 | 处理 |
|---|---|
| `non-fast-forward` | 远程有新 commit → `git pull --rebase <remote> main` 后重试 |
| `Authentication failed` (gitee) | gitee 凭据可能失效 → 跳过 gitee，记录失败，继续 backup |
| 网络超时 | 重试 1 次；仍失败则记录，标失败但不阻塞其他 push |

## 不动手清单

- ❌ 不创建 tag（v2.13.0 等不在本任务范围）
- ❌ 不创建 GitHub Release
- ❌ 不创建 Gitee Release
- ❌ 不打包 EXE
- ❌ 不动 CHANGELOG.md
- ❌ 不动 main.py VERSION
- ❌ 不 push 任何分支以外的 ref（如 `--all`、`--mirror`）

## 回执要求

`REPORT-20260509-008-OPS-to-PM.md`，含：
- 7 条验收命令的实际输出
- 任何 push 失败的原因
- 三仓最终 HEAD hash 对比表

## 时间预算

≤ 5 分钟（看网速）

OPS-01 立即执行。
