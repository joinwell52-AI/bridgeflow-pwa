---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-006
sender: PM
recipient: DEV
priority: P0
thread_key: codeflow-pwa-rule5-purge-fcop-definitions
references:
  - REPORT-20260509-003-PM-to-ADMIN
  - TASK-20260509-005-PM-to-DEV
layer: worker
---

# 按硬规则 #5 — 立即删除本仓所有 fcop 定义/分发类内容

## ADMIN 直接指示（5/9 10:42）

> 「我们现在这个码流项目，就是应用 fcop-mcp；不是定义 fcop！」
> 「作为整个码流项目文件夹，由于 fcop 已经独立，涉及到 fcop 的升级内容，是另一个项目文件了。」
> 「就是删除现在项目中有关 fcop 定义的内容？」（反问 = 确认）

按硬规则 #5（待落 §8.0）：本仓 = 应用方，fcop 定义/分发归 D:\FCoP。本仓不该 ship 任何"定义 / 安装 / 分发 fcop"类内容。

## 删除清单（in-scope，立即执行）

| 路径 | 删除理由 |
|---|---|
| `fcop-mcp/` 整个目录 | fcop-mcp 包源码 LEGACY 副本（D:\FCoP 已发 PyPI 0.6.2） |
| `codeflow-plugin/src/` 整个目录 | fcop Python 包源码副本 + _data + server.py |
| `codeflow-plugin/mcp.json` | fcop-mcp 在 Cursor 的集成模板（应由 D:\FCoP README 提供） |
| `codeflow-plugin/scripts/install-fcop.ps1` | 安装脚本（同上） |
| `codeflow-plugin/scripts/install-fcop.sh` | 安装脚本（同上） |
| `codeflow-plugin/pyproject.toml` | 占着 PyPI 包名 `fcop` |
| `codeflow-plugin/.cursor-plugin/plugin.json` | Cursor plugin 占着 `fcop` 名字 |
| `codeflow-plugin/requirements.txt` | fcop 包的 Python 依赖 |
| `codeflow-plugin/README.md` | "如何安装 fcop MCP" 教程 |

## 保留（out-of-scope）

按 §8.2 "保留并扩展" 与 §5.c "本仓可以 ship 接入指引"：

- `codeflow-plugin/agents/` — role briefs 素材库
- `codeflow-plugin/templates/` — 模板说明
- `codeflow-plugin/skills/file-protocol/SKILL.md`
- `codeflow-plugin/commands/switch-team.md`
- `codeflow-plugin/hooks/hooks.json` + `codeflow-plugin/scripts/on_file_edit.py`（hooks.json 仅引用 on_file_edit.py，不引用被删文件）
- `docs/integrations/fcop-standalone-zh.md` 暂留（按 §5.c 是接入指引，但内容措辞落后于 v2 身份，下次 sprint 修订）

## 后续遗留（backlog，本任务不做）

- `docs/design/codeflow-v2-on-fcop-sdk.md` §8.0 加硬规则 #5
- `docs/design/codeflow-v2-on-fcop-sdk.md` §8.2 表中 `fcop-mcp/` / `codeflow-plugin/src/fcop/` 行更新为"已删除"
- `docs/integrations/fcop-standalone-zh.md` 措辞按 v2 身份重写
- `codeflow-plugin/` 剩余资产是否搬到 `.codeflow/` 标准目录
- `_ignore/` 下与 fcop 相关的 audit 脚本是否清理

## 验收

| 项 | 验收 |
|---|---|
| 9 项删除全部完成 | `Test-Path` 返回 false |
| 保留资产未受影响 | `agents/` / `templates/` / `hooks/` / `commands/` / `skills/` 完整 |
| 不破坏现有测试 | `npm test --filter @codeflow/protocol` 通过 |

## 时间预算

≤ 10 分钟。

DEV-01 立即执行。回执 `REPORT-20260509-006-DEV-to-PM.md`。
