---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-025
sender: PM
recipient: QA
priority: P1
thread_key: codeflow-v2-sprint-s5-test-strategy-and-ts-5-12-13-restoration
references:
  - REPORT-20260509-021-QA-to-PM
  - REPORT-20260509-022-DEV-to-PM
  - TASK-20260509-024-PM-to-DEV
  - packages/codeflow-runtime/docs/test-strategy-s3.md
layer: governance
---

# Sprint S5 测试场景设计 + TS-5.12/5.13 顺序修正重做 + Phase D 回归

## 背景

按第 5 句宪法 PM 自约束「测试场景设计 = 自决」+「同 sprint 内接续派单 = 自决」。

DEV-022 完成 S4 时发现 QA TASK-021 §六对 `docs/test-strategy-s3.md` §3.5 TS-5.12/5.13 顺序的修正在工作树里，DEV 用 `git checkout HEAD --` 还原以保持 Phase D git diff scope 干净（详见 [`REPORT-20260509-022-DEV-to-PM.md`](./REPORT-20260509-022-DEV-to-PM.md) §一）。

**结果**：QA 之前的修正未进 OPS-023 commit。本任务让 QA 重做 + 顺手做 S5 测试场景设计 + Phase D 回归确认。

⚠️ **这不是 DEV 的错也不是 QA 的错**——是 PM 派单时没有显式 lock 同一 doc 文件的并发。S5 不再发生此类冲突（DEV-024 §不做明确写"不动 docs/test-strategy-s3.md"，QA 独占）。

## 工作内容（3 件）

### 工作 1：重做 TS-5.12/5.13 顺序修正

按 [`REPORT-021-QA-to-PM.md`](./REPORT-20260509-021-QA-to-PM.md) §六 6.2 已经定的对齐：

```
TS-5.12 = session_ended emits → state_history `dispatched → ended`（与 DEV TaskDispatcher.test.ts 实际实现一致）
TS-5.13 = reject_busy 验收 #5（与 DEV `TS-5.13 (validation #5)` 实际命名一致）
```

修正位置：`packages/codeflow-runtime/docs/test-strategy-s3.md` §3.5。

### 工作 2：起草 §3.6 / §3.7 Sprint S5 测试场景

参照 §3.5 13 场景的格式风格，起草 S5 全套测试场景设计：

| 必填场景 | 验收点 |
|---|---|
| **TS-7.1** SkillRegistry 正常加载 N skill | load() 返回 loaded=N / skipped=[] |
| **TS-7.2** SkillRegistry 跳过 schema 不合的 skill | logger.warn 命中 + 不阻塞其他 skill 加载 |
| **TS-7.3** SkillRegistry 跳过 .tmp / 非 .json / 损坏 JSON | listAll skip pattern 跟 SessionStore 一致 |
| **TS-7.4** SkillRegistry 索引正确（getById / listForRole）| O(1) 查询 + 反查命中 |
| **TS-7.5** KernelDependencyValidator 接受 fcop@>=1.0 | validateAgent 返回 null（无 failure） |
| **TS-7.6** KernelDependencyValidator 拒缺 fcop | reason="no_fcop_skill" + detail 含 agent_id |
| **TS-7.7** KernelDependencyValidator 拒不存在的 skill_id | reason="skill_not_found" |
| **TS-7.8** KernelDependencyValidator 拒不兼容 local runtime | reason="no_compatible_runtime" |
| **TS-7.9** MCPInjector stub 不 spawn | logger.info 命中 + 无子进程残留 |
| **TS-7.10** MCPInjector live mode v0.1 eager-throw | ctor 抛 MCPInjectorLiveModeNotImplementedError |
| **TS-7.11** RuntimeBootstrap 集成：缺 fcop agent 进 failed | report.failed.length > 0 / kernel_failures 出现 |
| **TS-7.12** AgentRegistry.register 前置 hook 拒缺 fcop | throw KernelDependencyError |
| **TS-7.13（bonus）** | agent.skills=[] 触发 no_fcop_skill |

QA 可以加 bonus 场景（不少于 12 即可）。

### 工作 3：起草 §5d Phase E 验收清单

仿 §5a / §5b / §5c 格式。15 项 acceptance 对照 `TASK-024-PM-to-DEV` §验收标准。

### 工作 4：Phase D 回归确认（OPS-023 commit 后跑）

跑 `npm test` 确认 71/71 仍然 PASS（OPS-023 commit 后 git HEAD 推进，DEV S5 实施前的 baseline）。
写 `§7` 节 Phase D 22 项 + Phase A/B/C 56 项总计 **78 项无回归**（71 + Phase D 13 = 84 — 但实际是 71 含全部，注意 DEV REPORT-022 §二的口径是 "Phase A 18 + B 22 + C 14 + D 13 + 4 跨阶段 sanity = 71"）。

按 DEV REPORT-022 §二的口径写。

## 不做（out-of-scope）

- ❌ 不写自动化测试代码（DEV 写）
- ❌ 不修改 `@codeflow/protocol` schema
- ❌ 不修改任何 docs/agents/tasks/ 现有 task 文件
- ❌ 不擅自降低或扩展 DEV 决策（决策 B'/J/K/L/O 已在 PM REPORT-021 接受，QA 不动）
- ❌ 不写 v0.2 测试场景（`mcpInjector.mode="live"` 实现是 v0.2 范围）
- ❌ 不动 S5 实施代码

## 验收标准（5 项）

| # | 项 | 验证方式 |
|---|---|---|
| 1 | TS-5.12/5.13 顺序修正按 REPORT-021 §六 落档 | grep `TS-5.12.*session_ended` 命中 + grep `TS-5.13.*reject_busy` 命中 |
| 2 | §3.6 / §3.7 S5 至少 12 个场景全填好 | 输入 / 步骤 / 期望 / 通过标准 4 列完整 |
| 3 | §5d Phase E 验收清单格式跟 §5a/§5b/§5c 一致 | 视觉对照 |
| 4 | §7 Phase D 回归报告（OPS-023 commit 后跑）71/71 通过 | stdout 截图入回执 |
| 5 | 没引入新依赖 / 不动 src 代码 | git diff --stat 仅 docs |

## 时间预算

- 预计 **2-3 小时**
- 工作 1 + 2 + 3 可立即开始（不依赖 OPS-023）
- 工作 4 等 OPS-023 commit 落地后跑

## 启动条件

QA 立刻开始工作 1 + 2 + 3（read-only 依赖）；工作 4 等 OPS-023 commit。

## 完成后回执要求

按 `qa-bridge` 规则写 `REPORT-20260509-025-QA-to-PM.md`，必须包含：

1. 测试用例列表（§3.5 修正 + §3.6/3.7 S5 场景 + §5d 验收清单 + §7 Phase D 回归）
2. 通过/失败数量（§7 应 71/71 通过）
3. 缺陷描述（如有；预期应为 0）
4. 是否建议 S6 立刻启动 + 主要依据
5. 是否建议 v0.1-alpha 现可发布给 ADMIN 试用 + 主要依据

---

PM-01 派单。QA-01 立即开始工作 1+2+3，等 OPS-023 后跑工作 4。回执请落 `REPORT-20260509-025-QA-to-PM.md`。
