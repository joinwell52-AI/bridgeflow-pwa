---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-023
sender: PM
recipient: OPS
priority: P0
thread_key: s4-phase-d-done-checkpoint-commit
references:
  - REPORT-20260509-022-DEV-to-PM
  - REPORT-20260509-021-QA-to-PM
  - REPORT-20260509-020-OPS-to-PM
  - TASK-20260509-022-PM-to-DEV
layer: worker
---

# Sprint S4 Phase D done checkpoint commit + push origin/backup（gitee G3 跳过）

## 背景

DEV 3.5h 内交付 S4 全部主交付，**71/71 测试全过**（Phase A 18 + B 22 + C 14 + **D 13** + 4 跨阶段 sanity）。
PM 独立复核 `tsc --noEmit` exit 0 + `npm test` = `tests 71 / pass 71 / fail 0 / 5609ms`，跟 REPORT-022-DEV 一致。

按第 5 句宪法 PM 自约束「commit 节奏 = PM 自决」，立刻派 OPS 第五轮 commit。

## 待 commit 文件清单（≈ 14 项展开后）

按当前 `git status --short`：

### A. `packages/codeflow-runtime/` 改动 — 13 项（7 M + 11 ?? 展开 = 18 项；详细见下）

```
7 M:
   packages/codeflow-runtime/README.md                 (Phase D 完成态 + 5 决策记录)
   packages/codeflow-runtime/examples/hello-world.ts   (升级 Phase C+D demo + REVIEW-01 注册)
   packages/codeflow-runtime/package.json              (0.1.0-alpha.3 → 0.1.0-alpha.4，无新 deps)
   packages/codeflow-runtime/src/Runtime.ts            (11 子系统装配 + start/stop 顺序)
   packages/codeflow-runtime/src/index.ts              (barrel + review/* + AgentStatusReconciler)
   packages/codeflow-runtime/src/registry/errors.ts    (+ ReviewWriteError + ReviewerNotFoundError + VerdictParseError + UnsupportedHumanPushSinkError)
   packages/codeflow-runtime/src/registry/index.ts     (barrel 加 AgentStatusReconciler)

11 ?? 展开:
   packages/codeflow-runtime/src/registry/AgentStatusReconciler.ts   (决策 B' 闭环)
   packages/codeflow-runtime/src/review/ReviewWriter.ts
   packages/codeflow-runtime/src/review/NeedsHumanGate.ts
   packages/codeflow-runtime/src/review/ReviewEngine.ts
   packages/codeflow-runtime/src/review/index.ts
   packages/codeflow-runtime/src/review/__tests__/ReviewWriter.test.ts
   packages/codeflow-runtime/src/review/__tests__/NeedsHumanGate.test.ts
   packages/codeflow-runtime/src/review/__tests__/ReviewEngine.test.ts
   packages/codeflow-runtime/src/review/__tests__/AgentStatusReconciler.test.ts
   packages/codeflow-runtime/src/review/__tests__/helpers.ts
```

### B. `docs/agents/tasks/` 流程文档 — 1 项（??）

```
?? docs/agents/tasks/REPORT-20260509-022-DEV-to-PM.md   (DEV S4 完成回执)
?? docs/agents/tasks/TASK-20260509-023-PM-to-OPS.md     (本任务 — 自包含)
?? docs/agents/tasks/TASK-20260509-024-PM-to-DEV.md     (S5 Skill Runtime 派单)
?? docs/agents/tasks/TASK-20260509-025-PM-to-QA.md      (S5 测试场景设计 + TS-5.12/5.13 修正)
?? docs/agents/tasks/REPORT-20260509-021-PM-to-ADMIN.md (S4 完工里程碑通报)
```

OPS 执行前用 `git status --short` 验证 = 7 M + 11 ?? 折叠（实际 8 项 docs ?? + 6 项 src ?? 展开）= **约 18 行**折叠后；展开 ~22 项。

## 加文件原则

```powershell
git add packages/codeflow-runtime/
git add docs/agents/tasks/

git diff --cached --name-only
```

期望：staged 文件位于 2 个目录之下，**不**包含：
- `_ignore/` / `private/` / `.codeflow/state/`
- `node_modules/` / `examples/inbox/` / `examples/.codeflow-state/`（demo 运行时产物，已被 examples/.gitignore 排除）
- `packages/codeflow-protocol/`（本批不应有改动）
- `docs/design/`（S4 不动 design doc）

## 高危操作判定

按 `ops-bridge.mdc` 4 类高危：全部不涉及 → **不属于高危**。

## 执行命令（按顺序）

### Phase 1：dev env + S4 实证回归（≤ 5 min）

```powershell
git status --short
node --version

cd packages/codeflow-protocol
npm install
npm test
cd ../..
# 期望：5 valid + 3 invalid 全过

cd packages/codeflow-runtime
npm install
npx tsc --noEmit
# 期望：exit 0

npm test
# 期望：tests 71 / pass 71 / fail 0
cd ../..
```

如 Phase 1 任一项失败 → 报告 PM；不 commit。

### Phase 2：commit bundle（≤ 1 min）

```powershell
git add packages/codeflow-runtime/
git add docs/agents/tasks/

git diff --cached --name-only

git commit -m "feat(s4-phase-d): ReviewEngine + ReviewWriter + NeedsHumanGate + AgentStatusReconciler + Runtime 11-subsystem composition + Phase D demo (71/71 tests) + REPORT-018 §决策 B' closure"

git log -1 --oneline
git show --stat HEAD | Select-Object -First 5
```

### Phase 3：push origin + backup（gitee G3 跳过；≤ 2 min）

```powershell
git fetch --all
git status -sb

git push origin main
git push backup main

Write-Host "gitee push skipped per HANDOFF-001 G3 decision"

$localHead  = git rev-parse HEAD
$originHead = (git ls-remote origin main) -split "`t" | Select-Object -First 1
$backupHead = (git ls-remote backup main) -split "`t" | Select-Object -First 1
$giteeHead  = (git ls-remote gitee  main) -split "`t" | Select-Object -First 1
Write-Host "local : $localHead"
Write-Host "origin: $originHead"
Write-Host "backup: $backupHead"
Write-Host "gitee : $giteeHead"
```

期望：
- local / origin / backup 三者 HEAD 一致
- gitee 仍 `62532a7…`

## 验收（OPS 必出 9 条验证）

| # | 验收项 | 命令 |
|---|---|---|
| 1 | commit 前文件全在 2 个目录之下 | `git status --short` |
| 2 | Node 版本 ≥ 20 | `node --version` |
| 3 | protocol 测试通过 | `cd packages/codeflow-protocol; npm test` 8/8 PASS |
| 4 | runtime typecheck 通过 | `npx tsc --noEmit` exit 0 |
| 5 | runtime 71 测试通过 | `npm test` 显示 `tests 71 / pass 71 / fail 0` |
| 6 | commit 文件数符合预期（≈ 22）| `git show --stat HEAD` |
| 7 | commit message 正确 | `git log -1 --pretty=%s` 含 `feat(s4-phase-d)` |
| 8 | origin / backup HEAD = local | `git ls-remote` 头部 hash 对得上 |
| 9 | gitee 仍 `62532a7…` | `git ls-remote gitee main` |

## 不动手清单

- ❌ 不创建 tag（v0.1 整体发布在 S6 收尾时统一 tag）
- ❌ 不 push gitee（G3）
- ❌ 不创建 GitHub Release / Gitee Release
- ❌ 不修改 `.git/config`
- ❌ 不 commit `_ignore/` / `private/` / `node_modules/` / `.codeflow/state/` / `examples/inbox/` / `examples/.codeflow-state/`

## 时间预算

≤ 8 分钟。

## 启动条件

OPS 见到本任务即可立即执行。**不需要等 ADMIN 单独拍板**——按 ADMIN 5/9 16:36 第 5 句宪法永久授权。

## 完成后回执要求

按 `ops-bridge` 规则写 `REPORT-20260509-023-OPS-to-PM.md`，必须包含：

1. 9 条验收命令的实际输出
2. 三仓最终 HEAD hash 对比表
3. 本次 commit 的 SHA-1（前 7 位 + 完整 hash）
4. **特别确认**：`git show --stat HEAD` 完整 file list

---

PM-01 派单。OPS-01 立即执行。回执请落 `REPORT-20260509-023-OPS-to-PM.md`。
