---
protocol: fcop
version: 1
kind: task
task_id: TASK-20260509-030
sender: PM
recipient: OPS
priority: P0
thread_key: s6-codeflow-shell-commit-and-internal-rc-tag-no-publish
references:
  - REPORT-20260509-028-DEV-to-PM
  - REPORT-20260509-029-OPS-to-PM
  - REPORT-20260509-024-PM-to-ADMIN
  - https://github.com/joinwell52-AI/FCoP/issues/2#issuecomment-4412811192
layer: worker
---

# Sprint S6 codeflow-shell commit + push origin/backup + 本地 internal RC tag（不推 origin tag）

## 背景

DEV-028 已完成 S6 codeflow-shell MVP（约 30 分钟内交付）：
- 5 件主交付完成（codeflow-shell 骨架 + main.ts + bootstrap.ts + Hello World demo + release notes）
- **94/94 tests 仍 PASS / 0 回归 / tsc 0 错**（PM 独立复核一致）
- **5 个 surprise 全部主动 disclosed**（详见 [REPORT-028 §三](./REPORT-20260509-028-DEV-to-PM.md)）
- Node SEA EXE 走 fallback 路径（PM 在 TASK-028 §三 已 bless）

按第 5 句宪法 PM 自决（sprint 阶段 commit 节奏 + sub-decision 接受 = PM 自决），立即派 OPS 第七轮 commit。

## §一 关键决策：本地 tag，**不推 origin**

按 ADMIN 5/9 23:14「按推荐」覆盖 A.1：
> v0.1-alpha **改语义** — 完工标准从「PyPI/npm publish」改为「**internal RC tag** + S6 EXE bundle + ADMIN 试用」。**不**做公开发布。

→ **本任务建本地 tag `v0.1.0-rc.1`**（标记 internal RC 出厂时刻），**不**推送到 origin/backup（不让 GitHub Releases 页面显示 v0.1.0-rc.1）。

理由：
1. A.1 明确「不公开发布」 — push tag = 公开行为（GitHub Releases 立刻显示）
2. CodeFlow v0.1 用了 5-schema + needs_human enum 这些 v0.2 即将 deprecated 的 shape，发出去会让外部看到 confusion
3. v0.2 sprint 0 完工后再正式 push tag `v0.2.0-alpha.1`（首次公开），届时 align fcop@1.0 charter

## §二 待 commit 清单

```
4 M:
   M README.md                                            (顶部加 v1.0 alignment block)
   M packages/codeflow-protocol/README.md                 (v1.0 alignment block)
   M packages/codeflow-runtime/README.md                  (顶部摘要 rewrite + v1.0 alignment block)
   M packages/codeflow-runtime/package.json               (version 0.1.0-alpha.5 → 0.1.0-rc.1)

4 ?? + 子目录展开:
   codeflow-shell/                  (DEV S6 全新子项目，展开 ~12 文件)
       ├── .gitignore
       ├── README.md
       ├── package.json
       ├── package-lock.json
       ├── tsconfig.json
       ├── sea-config.json
       ├── pack.cmd
       ├── src/
       │   ├── main.ts
       │   ├── bootstrap.ts
       │   └── sdk-factory.ts
       └── examples/hello-world/
           ├── README.md
           └── sample-task.md

   docs/releases/                   (DEV 全新目录)
       └── v0.1.0-rc.1.md           (release notes ~230 行)

   docs/agents/tasks/REPORT-20260509-028-DEV-to-PM.md   (DEV S6 完工回执)
   docs/agents/tasks/REPORT-20260509-029-OPS-to-PM.md   (OPS docs commit 回执)

PM 即将新加：
   docs/agents/tasks/TASK-20260509-030-PM-to-OPS.md     (本任务)
   docs/agents/tasks/TASK-20260509-031-PM-to-QA.md      (E2E acceptance — 等 OPS-030 后跑)
   docs/agents/tasks/REPORT-20260509-025-PM-to-ADMIN.md (v0.1.0-rc.1 出厂里程碑通报 ⭐)
```

= **4 M + ~17 项** ≈ **总计 ~21 项**

## 加文件原则

```powershell
# Modified files
git add README.md
git add packages/codeflow-protocol/README.md
git add packages/codeflow-runtime/README.md
git add packages/codeflow-runtime/package.json

# New files / directories
git add codeflow-shell/
git add docs/releases/
git add docs/agents/tasks/REPORT-20260509-028-DEV-to-PM.md
git add docs/agents/tasks/REPORT-20260509-029-OPS-to-PM.md
git add docs/agents/tasks/TASK-20260509-030-PM-to-OPS.md
git add docs/agents/tasks/TASK-20260509-031-PM-to-QA.md
git add docs/agents/tasks/REPORT-20260509-025-PM-to-ADMIN.md

# 验证
git diff --cached --name-only
```

期望 staged **不**包含：
- `_ignore/` / `private/` / `.codeflow/state/`
- `node_modules/` / `*.smoke-test-state/` / `*.codeflow-state/` / `*/inbox/`
- `packages/codeflow-runtime/src/` 任何文件（DEV 本刷不动 src/）
- `packages/codeflow-protocol/src/` 任何文件
- `docs/design/`（design doc 本刷不动）

期望 staged 包含 **`codeflow-shell/.gitignore`** + 不含 `codeflow-shell/node_modules/` 或 `codeflow-shell/.smoke-test-state/`。

## 高危操作判定

按 `ops-bridge.mdc` 4 类高危：本任务**不涉及生产/Nginx/数据库/防火墙**。

`git tag` 是创建标签操作 — 这是低风险的。**`git push` tag 是高风险**（公开发布），但本任务**明确不 push tag**，只本地建。

## 执行命令（按顺序）

### Phase 1：dev env + S6 baseline 复核（≤ 3 min）

```powershell
git status --short
node --version

# protocol 测试
cd packages/codeflow-protocol
npm test
cd ../..

# runtime typecheck + 94 测试
cd packages/codeflow-runtime
npx tsc --noEmit
npm test 2>&1 | Select-String "tests \d+|pass \d+|fail \d+"
cd ../..

# codeflow-shell typecheck（DEV 已自测，OPS 二次确认）
cd codeflow-shell
npm install
npx tsc --noEmit
cd ..
```

期望：所有 typecheck exit 0 + runtime 94/94 tests 通过 + protocol 8/8 通过。

### Phase 2：commit bundle（≤ 2 min）

```powershell
# Add 见 §加文件原则

git diff --cached --name-only | Measure-Object -Line
git diff --cached --name-only

git commit -m "feat(s6-codeflow-shell): v0.1.0-rc.1 internal preview MVP — codeflow-shell skeleton + main.ts + bootstrap + Hello World demo + Windows SEA pack recipe (fallback to npm start) + v1.0 alignment block (B.1+E.1) + release notes + runtime version bump (0.1.0-alpha.5 → 0.1.0-rc.1) — 94/94 tests pass / 30x 0 flakes / no regression"

git log -1 --oneline
git show --stat HEAD | Select-Object -First 5
```

### Phase 3：本地 tag（**不**推 origin；≤ 30 sec）

```powershell
# 创建 annotated tag（带 message，便于 git show v0.1.0-rc.1）
git tag -a v0.1.0-rc.1 -m "CodeFlow v0.1.0-rc.1 — internal preview RC; v1.0 alignment pending (FCoP issue #2 ref)"

# 验证本地 tag 创建成功
git tag --list "v0.1.0-rc.1"
git show v0.1.0-rc.1 --stat | Select-Object -First 8

# ⚠️ 不要 git push origin v0.1.0-rc.1
# ⚠️ 不要 git push origin --tags
# ⚠️ 不要 git push backup --tags
```

### Phase 4：push commit only（**不**推 tag；≤ 1 min）

```powershell
git fetch --all
git status -sb

# 仅推 main 分支，不带 tag
git push origin main
git push backup main

# 验证：origin/backup 应只看到 commit，不看到 v0.1.0-rc.1 tag
git ls-remote --tags origin | Select-String "v0.1"
# 期望：空输出 OR 仅历史 tag（如有）

git ls-remote --tags backup | Select-String "v0.1"
# 期望：空输出

Write-Host "gitee push skipped per HANDOFF-001 G3 decision"

$localHead  = git rev-parse HEAD
$originHead = (git ls-remote origin main) -split "`t" | Select-Object -First 1
$backupHead = (git ls-remote backup main) -split "`t" | Select-Object -First 1
$giteeHead  = (git ls-remote gitee  main) -split "`t" | Select-Object -First 1
Write-Host "local : $localHead"
Write-Host "origin: $originHead"
Write-Host "backup: $backupHead"
Write-Host "gitee : $giteeHead"
```

期望：
- local / origin / backup HEAD 一致
- gitee 仍 `62532a7…`
- ⚠️ origin/backup 看不到 `v0.1.0-rc.1` tag
- 本地 `git tag --list` 能看到 `v0.1.0-rc.1`

## 验收（OPS 必出 11 条验证）

| # | 验收项 | 命令 |
|---|---|---|
| 1 | commit 前 status 符合预期 | `git status --short` |
| 2 | Node 版本 ≥ 20 | `node --version` |
| 3 | protocol 测试通过 | `cd packages/codeflow-protocol; npm test` |
| 4 | runtime typecheck + 94 测试通过 | `npx tsc --noEmit` exit 0 + `npm test` 显示 `tests 94 / pass 94 / fail 0` |
| 5 | codeflow-shell typecheck 通过 | `cd codeflow-shell; npm install; npx tsc --noEmit` exit 0 |
| 6 | commit 文件数符合预期（≈ 21）| `git show --stat HEAD` |
| 7 | commit message 含 `feat(s6-codeflow-shell)` 前缀 | `git log -1 --pretty=%s` |
| 8 | **本地 tag `v0.1.0-rc.1` 创建成功** | `git tag --list "v0.1.0-rc.1"` 显示 1 行 |
| 9 | **origin/backup 仍无 `v0.1.0-rc.1` tag**（关键 — internal RC 不公开）| `git ls-remote --tags origin \| Select-String "v0.1.0-rc.1"` 应空输出 |
| 10 | origin / backup HEAD = local | `git ls-remote` 头部 hash 对得上 |
| 11 | gitee 仍 `62532a7…` | `git ls-remote gitee main` |

## 不动手清单

- ❌ **绝对不 push tag 到 origin / backup**（违反 A.1 internal RC 语义）
- ❌ 不 push gitee（G3）
- ❌ 不创建 GitHub Release / Gitee Release
- ❌ 不 npm publish
- ❌ 不修改 `.git/config`
- ❌ 不 commit `_ignore/` / `private/` / `node_modules/` / `.smoke-test-state/` / `examples/.codeflow-state/`
- ❌ 不修改任何 src 文件

## 时间预算

≤ 8 分钟。

## 启动条件

OPS 见到本任务即可立即执行。

## 完成后回执要求

按 `ops-bridge` 写 `REPORT-20260509-030-OPS-to-PM.md`，必须包含：

1. 11 条验收命令的实际输出
2. 三仓最终 HEAD hash 对比
3. 本次 commit 的 SHA-1
4. **本地 tag `v0.1.0-rc.1` 创建截图** + commit hash 对应
5. **特别确认**：`git ls-remote --tags origin` + `git ls-remote --tags backup` 输出 — 应**不**含 `v0.1.0-rc.1`

---

PM-01 派单。OPS-01 立即执行。回执请落 `REPORT-20260509-030-OPS-to-PM.md`。

> **本任务完成 = v0.1 Backend Kernel 出厂时刻**。OPS-030 push 完毕后，ADMIN 即可：
> 1. `cd codeflow-shell && npm install && npm start` 试用 v0.1.0-rc.1
> 2. drop `examples/hello-world/sample-task.md` 到 inbox 看 governance loop
> 3. Ctrl+C 退出
>
> 详见 [`REPORT-20260509-025-PM-to-ADMIN.md`](./REPORT-20260509-025-PM-to-ADMIN.md) 给 ADMIN 的试用引导。
