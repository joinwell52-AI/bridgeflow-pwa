{
  "name": "@codeflow/runtime",
  "version": "0.2.0-beta.1",
  "private": true,
  "description": "CodeFlow AI Runtime — Agent Registry + Session Manager + Task Scheduler + Review Engine + Skill Runtime. v0.2.0-beta.1 (MT-1 hotfix): 14 subsystems, 104/104 tests, 0 flakes; CursorSdkAdapter wires defaultModel into Agent.create / agent.send (BUG-SDK-001). MT-2 atomic-write retry-on-EPERM still active. Backed by codeflow-shell. Not published to npm — for ADMIN test only.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/__tests__/*.test.ts\""
  },
  "dependencies": {
    "@codeflow/protocol": "file:../codeflow-protocol",
    "@cursor/sdk": "^1.0.12",
    "chokidar": "^4.0.3",
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}
