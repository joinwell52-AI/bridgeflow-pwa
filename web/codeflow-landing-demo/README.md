# 码流（CodeFlow）静态说明页（本仓库内示例）

本目录是 **当前主仓库** 里的一份 **独立 HTML 示例**，用于展示品牌与叙事（中英、角色、文件名协议等）。

## 与「历史仓库」的关系

- **`joinwell52-ai/joinwell52`**（及对应 GitHub Pages）是 **历史站点**，**不要求、也不建议** 用本目录去覆盖或同步那边——两边 **仓库独立**，历史站可原样保留。
- 当前产品主线在本仓库与 **`codeflow-pwa`** 等；需要对外说明时，以 **PWA / 主仓文档** 为准。

## 本目录用途（可选）

- 本地打开 `index.html` 预览样式与文案；
- 若将来要在 **其它** 托管处放一页静态说明，可复制使用（与历史 `joinwell52` 站 **无绑定关系**）。
