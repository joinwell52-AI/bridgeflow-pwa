# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
*.egg-info/
*.egg
.eggs/
dist/
build/
.installed.cfg
pip-selfcheck.json
*.pyo

# ── 测试 & 覆盖率 ──
.pytest_cache/
.coverage
htmlcov/
.tox/
nosetests.xml
coverage.xml

# ── 类型检查 ──
.mypy_cache/
.dmypy.json
.pyright/

# ── 构建产物 ──
dist/
build/
*.whl
*.tar.gz

# ── Node / PWA ──
node_modules/
.nuxt/
.next/
.output/
dist/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo
*~

# ── Cursor ──
.cursor/cache/

# ── 运行时 ──
.bridgeflow/runtime/
*.log
*.tmp

# ── 配置文件（含敏感信息，不提交） ──
bridgeflow_config.json
.env
.env.*

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── 私有文档（内部使用，含敏感信息，不发布到 GitHub） ──
private/

# ── 临时文件 ──
_patch_*.py
_rebuild*.py
*.bak
_commit_msg*.txt

# ── 本地快照 / 多人杂项（勿提交）──
_pages_tmp/

# ── bridgeflow-nudger 调试与测试产物 ──
bridgeflow-nudger/_*.png
bridgeflow-nudger/test_*.png
bridgeflow-nudger/cursor_screenshot.png
bridgeflow-nudger/cursor_vision_report.json
bridgeflow-nudger/_test_layout.py

# PyInstaller 命令行生成的 spec（日常以 build.spec、pack.cmd 为准）
bridgeflow-nudger/CodeFlow-Desktop.spec
bridgeflow-nudger/BridgeFlow-Desktop.spec
